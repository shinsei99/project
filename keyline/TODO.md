# TODO — KeyLine（NFC鍵・備品貸出管理）

## ⏸ いまここ（2026-09-04 夕）— **build 5 を上げ終え、提出の一歩手前で止めてある**

App Store Connect 側は**［審査へ提出］を押すだけ**の状態（版 1.0 / `PREPARE_FOR_SUBMISSION`・
build 5 ひも付け済み・スクショ5枚 COMPLETE・説明1,760字・審査ノート1,308字）。

**押す前に残っていること**

- [x] **実機で動いた。新品（未フォーマット）のタグも通った**（2026-09-04 夜・オーナー確認）。
      `NDEF` を entitlement から外しても実機の読み書きは成立する＝ altool 90778 への
      対処は正しかった。build 2→3 で踏んだ2件と同じ場所が通ったので、NFC実装は担保済み
- [ ] **提出はスクラップメモ 1.0.5 が動いてから**（1件ずつの約束）。押すのはオーナー
      ← **これだけが残り**

やり直したいときは `keytag-native/screenshots/shoot.sh`（スクショ）と
`keytag/push-metadata.py --apply` / `push-screenshots.py ../keytag-native/screenshots --apply`（反映）。

## ✅ 完了（2026-09-04）— KeyTag を **SwiftUI ネイティブ**に作り替える

**目的**: Guideline 4.3(a) の3つの引き金のうち、**①バイナリの類似**を根本から消す。
いまの KeyTag は **Capacitor の殻 + www/ 1,553行**で、ネオンブロックス・ゲーム各種と
**実行ファイルの大半が同一**。Apple はバイナリの指紋を取るので、文章では反論できない。
（②掲載文の使い回し → 別途 store-text.md を作り直す ③連投 → 1件ずつに変更済み）

**やること**

- [ ] `keyline/keytag-native/` に SwiftUI + Core NFC で作り直す（**既存の keytag/ は消さない**。
      通るまで残し、提出するのは片方だけ）
- [ ] バンドルIDは **`com.shinsei99.keytag` のまま**。App記録も同じものを使う
      （4.3の直後に新記録を作るのは最悪手）。次のビルドは **build 5**
- [ ] NDEFの書式は**サーバーの `ndef.py` と完全一致**させる（URIレコード＋テキストレコード
      `物件名|鍵の名称|鍵番号|ボックス-位置`・削る順番も同じ）。ここがずれると
      アプリで書いたタグをサーバーが読めない
- [ ] 画面は4つとも移植: 読み取り／書き込み／台帳／設定＋貸出・返却
- [ ] サーバー連携（pair / ping / register / asset / checkout / return）も移植。
      **サーバーが無くても全機能が動く**という設計の芯は変えない（審査員は社内LANに入れない）
- [ ] お試しサンプル投入も残す（タグを持たない審査員が評価できるようにするため。**必須**）
- [ ] シミュレータでビルド・画面確認（**NFCはシミュレータで動かない**）
- [ ] **★台帳を Excel で書き出し・読み込みできるようにする**（2026-09-04 オーナー提案・新機能）
      - 書き出し: `.xlsx`（物件名/鍵の名称/鍵番号/ボックス/位置/状態/貸出先/返却予定/タグID）
      - 読み込み: `.xlsx` と `.csv` の両方。**照合はタグID（uid）→ 無ければ 物件名＋鍵の名称**。
        既存は中身だけ更新し**貸出状態と履歴は保持**、無いものは「タグ未書込」で新規追加
      - 使い道: ①Excelで一覧を作っておき現場でタグに書く ②台帳を渡す・受け取る
      - **外部ライブラリを足さない**（依存を増やすと「テンプレで組んだ」印象に戻る）。
        xlsx は ZIP+XML なので、書き出しは自前のZIP生成、読み込みは Compression の
        deflate 展開＋XMLParser で処理する
- [ ] **実機でのNFC読み書き確認＝オーナーしかできない**（build 2→3 で踏んだ不具合の範囲を再確認）

**完了条件**: シミュレータで4画面とも動き、`xcodebuild` が通り、実機でタグの読み書きができる。
**提出はしない**（スクラップメモ 1.0.5 の結果が出るまで。1件ずつの約束）。


**分類:** 不動産 / **port:** 8534 / **本体:** メインPC（Mac mini 192.168.1.105）/ **社内LAN共有:** する（0.0.0.0）

## 確定した方針（2026-08-17）

- **社内LAN限定の社内ツール。** SaaS・マルチテナント・Supabase は使わない
- **NFCは「タグにURLを書く」方式。** iPhone のバックグラウンドタグ読み取りで Safari が開く。
  **アプリのインストール不要**（Safari は Web NFC 非対応なので、これ以外に道がない）
- 鍵ボックス横に**鍵管理用スマホ1台**（共用）。**画面は2つだがアプリは1本**
- 借りる人は `borrowers` に統一（社員・業者・内見客）。社外貸出があるため
  「操作する人（users）」と分けた。**操作した社員は記録しない**
- OCR は `claude` CLI。**画像は返却から30日で自動削除**
- サーバーは **FastAPI + SQLite**（Streamlit不可の理由は README）

## Phase 進捗

- [x] Phase 0: 現状確認
- [x] Phase 1: DB設計・SQL実装（**テスト61件成功**）
- [x] Phase 2: 認証（PBKDF2・sessions・Cookie 365日）
- [x] Phase 3: NFCトークン発行・タグURL・タグ交換
- [x] Phase 4: 貸出・返却（二重貸出を3層で防止）
- [x] Phase 5: 画面（貸出／管理）＋SSE＋OCR＋画像自動削除（**通しテスト35件成功**）
- [x] KeyTag（iOSアプリ）を作成し **App Store へ提出**（1.0.0 / build 2・2026-08-18）
- [x] **KeyTag（アプリ）の実機検証**（2026-09-04・build 5。**新品タグも読み書きできた**）
- [ ] **KeyLine 本体（8534）側の実機検証 ← いまここ**。下の「タグURLを Safari が開くか」は
      **アプリとは別の話**で、まだ確かめていない（アプリが動いたことでは代替できない）

## 🔴 次にやること（KeyLine 本体側・アプリとは別）

**平文 `http://192.168.1.105:8534/t/<token>` を、iOSのバックグラウンドタグ読み取りが
開いてくれるか確認する。** 5分で白黒つく。ここが黒なら方式ごと作り直し。

1. 管理画面で管理対象を開き、タグURLをコピー
2. iPhone（XS以降）の「NFC Tools」等で、NDEFのURLレコードとしてタグに書き込む
3. アプリを閉じた状態でタグにかざす → 通知バナーが出るか
4. タップして貸出画面が開くか

**黒だった場合の代替**（調査済み・行き止まりではない。詳細は README「3.」）
1. `keyline.daikyocorp.co.jp` を 192.168.1.105 に向けて Let's Encrypt（DNS-01）→ LAN内で正規HTTPS
2. 自己署名証明書＋構成プロファイル（端末ごとに1回の手作業）

→ HTTPS化したら `app.py` の `set_cookie` に `secure=True` を付けること

## その後

- [ ] 実機検証が通ったら `_launchd/install.sh` で常駐登録（**メインPCのみ**）
- [ ] 本番の管理対象・ボックス・鍵番号を登録し、タグを貼る
- [ ] Desktop の `.app` と Dropbox共有フォルダの `.url` を置くか判断
      （※オーナー情報ではないが貸出先の氏名・電話を含むため、`chatwork-ai-manager` と
        同じく**社内には配らない**選択もあり得る）

## 未着手（MVPの外）

- 利用者（users）の追加・パスワード変更の画面。いまは `seed.py` と CLI のみ
- 返却期限の通知（Chatwork/LINE）。ご指示30で将来拡張とされているもの
- CSV出力・利用統計・複数拠点

## 落とした仕様（社内LAN限定化に伴い・2026-08-17に承認済み）

| ご指示 | 状態 |
|---|---|
| 16. マルチテナント / RLS | ❌ 自社1社のみ。**ただし `organization_id` 列は全テーブルにあり**、テナント分離はトリガーで担保。将来 Postgres+RLS へ移せる |
| 17. Supabase Auth | ⚠️ PBKDF2 + sessions テーブル + Cookie に置換 |
| 18. Supabase / Flutter / React Native | ❌ 使わない |
| 27. Supabase Realtime | ⚠️ SSE で代替（実装・動作確認済み） |
| 全体. SaaS | ❌ 社内ツール |

そのまま実現したもの: 2〜15・19〜26・28・29（貸出・返却・他人が借用中・履歴・
ダッシュボード・一覧・未登録タグ登録・二重貸出防止・エラー処理・テスト）
