# restoration-calculator（原状回復費用自動精算・port 8508）TODO

## 完了

- [x] **精算書に「ガイドラインのどこに書いてあるか」を出す**（2026-08-30 完了）

  **★測って分かった一番大事なこと: このアプリの計算は元からガイドラインどおりだった。**
  ガイドライン本文から読み取った耐用年数・負担方式でアプリの表を上書きしたところ、
  **変わった部材が1つも無かった**（`GUIDELINE_OVERRIDES` が空）。**金額は1円も変わっていない。**
  つまり今回やったのは「計算を直すこと」ではなく、次の3つ。

  1. **証明できるようにした** … 引用を索引の本文と1文字ずつ照合する。
     ガイドラインが改訂されて本文が変われば**生成が落ちて気づける**（黙って古い根拠が残らない）
  2. **根拠を示せるようにした** … ページ番号（例 P27・P30）と本文の引用を画面に出す
  3. **定めが無い部材を区別できるようにした** … ソフト巾木・ペンキ・塗装・その他・諸経費は
     「ガイドラインに定めなし・当社の既定」と明記する。今までは区別がつかなかった

  **やったこと**: `bookshelf/make_basis_table.py`（生成・照合）→ `guideline_basis.json`（生成物）
  → `services/depreciation_engine.py` が読み込み、**ガイドラインの値を正とする**
  → `app.py` の `render_guideline_basis()` が画面に出す

  **検証**: `smoke_test.py` 全通過（金額同一）/ Streamlit AppTest 例外0 /
  17部材すべてで描画関数が例外なく動く / `--check` で照合17件すべて一致 / 本番 8508 が HTTP 200

  **はまり所**:
  - PDF本文は**改行位置に空白が入る**（実物は `(また は曲線)`）。空白を1つに潰すだけでは
    照合が全部落ちる。**空白を全部消して比べる**（日本語なので安全）
  - 別表2（P26-28）と別表3（P30）は**同じ趣旨だが文言が違う**。1つの引用で両方は照合できないので、
    場所ごとに引用を持たせた
  - 生成物を `data/` に置いてはいけない。**入居者名を含むCSVがあるため `.gitignore` で除外**されており、
    置くと他PCへ渡らず**黙ってアプリ既定に落ちる**。アプリ直下に置いた

## 進行中

- [ ] （下記の「未着手」参照）

<details><summary>当初の計画（記録として残す）</summary>

  **目的**: 退去者と揉めたときに、担当者がその場で根拠を示せるようにする。
  いまの `basis`（算出根拠メモ）は**計算の内訳**しか書いていない
  （例: `部分補修原価¥123,456に適用`）。「なぜ壁クロスは6年なのか」が書いていない。
  索引には国交省ガイドライン本文が**ページ番号つき**で入っている（310チャンク）のに使っていない。

  **実装内容**:
  1. `bookshelf/make_basis_table.py` … 知識索引（chatwork-ai-manager/data/app.db）を
     **読み取り専用**で引き、`MATERIAL_POLICY` の各部材について
     ガイドラインの該当ページと引用文を取り出して JSON に書き出す
  2. `data/guideline_basis.json` … 生成物。**アプリは実行時にDBを見ない**（このJSONだけ読む）
     ＝サブPCでも動く・起動が速い・中身を人がレビューできる
  3. 画面に「根拠」を出す（明細ごと）。**Excelの様式は当面変えない**（本番運用中のため）

  **完了条件**:
  - 21種別すべてに、ページ番号つきの引用が付いている（付かないものは「該当なし」と明記）
  - `smoke_test.py` が通る（計算結果は1円も変わらない）
  - 画面を `va.sh` で見て、根拠が読める形で出ている

  **検証方法**: `python3 smoke_test.py` ＋ `--server.address 127.0.0.1` で起動して目視

  **状態**: 進行中

  **関連ファイル**: `services/depreciation_engine.py`（MATERIAL_POLICY）/ `app.py` /
  `models/restoration_data.py`（basis）/ `bookshelf/make_basis_table.py`

</details>

## 未着手

- [ ] **★オーナー判断が要る: 換気扇・ドアクローザーを按分に変えるか。**
      ガイドライン P28 は**設備機器を「耐用年数経過時点で残存価値1円となるよう按分」**とするが、
      **換気扇の耐用年数はガイドライン本体にも資料8（P172）にも載っていない**。
      数字が無いので**こちらで作らず、いまは従来どおり（故意過失なら全額）で動かしている**。
      精算書には「ガイドライン P28 は按分とするが、この部材の耐用年数は示されていない」と出る。
      按分するなら年数を決める必要がある（GL P28 の例: 流し台5年 / 冷暖房機器6年 /
      金属製以外の家具8年 / 便器・洗面台等15年 / ユニットバス等は建物の耐用年数）。
- [ ] **フローリングの全体張替え**に経路が無い。ガイドラインは「全体を張り替えた場合は
      **当該建物の耐用年数**で按分」とするが、このアプリは常に「経過年数を考慮しない」。
      該当時は手で調整するしかない（画面の根拠欄に注意書きは出している）
- [ ] **畳床と畳表を分けていない**。ガイドラインは畳表＝考慮しない／畳床＝6年で分けている

- [ ] 判例（RETIO 173件）から、争いになりやすい部材（タバコのヤニ・日焼け・画鋲）の
      実例を引けるようにする。まずガイドラインの引用が終わってから
- [ ] Excel（精算書）にも根拠を載せるか。**様式変更は本番影響があるのでオーナー判断**

## メモ

- このアプリの計算ロジックは `services/depreciation_engine.py` に集約されている。
  `MATERIAL_POLICY: {部材種別: (耐用年数, 負担方式)}` が本体で、
  方式は `depreciable`（6年償却・部分補修按分）/ `full_fault`（耐用年数なし・故意過失時のみ全額）/
  `apportion`（諸経費の按分）の3つ。
- **ガイドラインの原則は「故意・過失が証明されない限りオーナー負担(0%)」**（app.py:299）。
