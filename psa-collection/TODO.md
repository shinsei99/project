# TODO — psa-collection（PSAカード管理）

## 進行中

### ワンピースカード図鑑も PSA管理から開けるようにする（2026-08-24）

**目的**: ポケモンカード図鑑と同じやり方で、`onepiece-dex`（4,962枚）も
PSA管理の中から開き、アルバムへ「⭐ 欲しい」を入れられるようにする。

**方針**: ポケカのときの決まりをそのまま踏襲する。図鑑の実体は動かさない・
単独起動（8537）も残す・無いPCでは選択肢を出さない・保有カードとの自動突き合わせはしない。
違いは「図鑑が1本から2本になる」ことだけなので、**PSA側は1本決め打ちをやめて登録制にする**。

**実装内容**

- [x] `onepiece-dex/app.py` に `album_ui` の差し込み口を付ける（ポケカと同じ形）
- [x] `onepiece-dex/app.py` に `card_no_label()` を足す（PSA側がアルバムの見出しに使う）
- [x] **session_state / ウィジェットキーを `op_` で名前空間化する。**
      2本の図鑑が同じ `tab` `detail` `pack` `other` を使っており、画面を行き来すると
      選択が混ざる（タブ構成も 3つ vs 4つで違う）
- [x] PSA側の `DEX_DIR` 1本決め打ちを**図鑑レジストリ**に変える
      （フォルダ・接頭辞・モジュール名・表示名を1件ずつ持つ）
- [x] アルバムの目印を図鑑ごとに分ける。**ポケカは `dex:` のまま**（既存 albums.json 互換）、
      ワンピは `opdex:`
- [x] サイドバー「表示対象」に **🏴‍☠️ ワンピースカード図鑑** を追加（入っているPCだけ）

**完了条件**

- [x] 8527 からワンピ図鑑が開き、画像（サムネ・詳細・パック表紙）が出る
- [x] 8537 の単独起動も今までどおり動く
- [x] 2本の図鑑を行き来してもタブ・詳細の選択が混ざらない
- [x] アルバムにポケカとワンピの「欲しいカード」が同居し、並べ替え・削除ができる
- [x] 既存のアルバム（cert のみ・`dex:` のみ）が壊れていない

**検証**（2026-08-24 メインPC・実機）

- 8527 / 8537 とも HTTP 200。8537 単独起動は拡張パック23件の表紙と OP-17 の
  収録カード169枚が今までどおり出る（`.see/0824-190259` / `0824-190313`）
- 8527 の中でワンピ図鑑を開き、**パック表紙・サムネ・詳細の大画像がすべて出た**
  （`.see/0824-190350` / `0824-190405` / `0824-191101`）。`_p()` の cwd 解決が効いている
- 「⭐ 欲しい」→ `albums.json` に `opdex:OP17-001` が入ることを実測。続けてポケカ側でも
  `dex:M6/M6_001` を入れ、**2種類が同じアルバムに同居**（`.see/0824-190722`）
- 欲しい枠の中で並べ替え（順序が入れ替わる）・✕での削除（確認ダイアログに
  「🏴‍☠️ ワンピースカード図鑑 OP17-001」と出る）まで実機で確認
- **検証で入れた2枚は外し、`albums.json` は検証前とバイト単位で同一に戻した**
- ワンピで「👑 リーダー」タブ・カード詳細を開いたままポケカへ移っても例外にならず、
  ポケカは自前の3タブで開く。戻るとワンピの詳細が保たれる（名前空間化の効果）
- `./va.sh check`: 横スクロール0・画面外0・画像切れ0・文字重なり0／Console エラー0件

**状態**: 完了（2026-08-24 メインPC）

**運用の決まり**（2026-08-24 オーナー判断）: **常設するのは PSAカード管理（8527）だけ**。
**図鑑2本（ポケカ 8531・ワンピ 8537）はどちらも常駐させない**。PSA管理は図鑑の
`app.py` を**モジュールとして直接読み込む**ので、**8531 / 8537 が起動していなくても
中から使える**（両方を落とした状態で一覧・詳細が出ることを実機で確認）。
単独の画面が要るときだけ各アプリの `run.sh` を都度叩く。

- `com.shinsei.pokecard-dex` は `launchctl bootout` で外し、**`launchctl disable` も
  掛けた**（plist が `~/Library/LaunchAgents` に残っていても次回ログインで復活しない）。
  戻すときは `launchctl enable gui/$(id -u)/com.shinsei.pokecard-dex` → `bootstrap`
- `_launchd/install-launchd.sh` の APPS から図鑑2本を外した（流し直しても再登録されない）


### ポケモンカード図鑑を PSA管理から開けるようにする（2026-08-19）

**目的**: 図鑑（`pokecard-dex`）を**そのフォルダに置いたまま**、PSA管理の中の
オプション画面として開けるようにする。あわせて**アルバムに「欲しいカード」を
図鑑から入れられる**ようにする。

**確定した方針**（2026-08-19・オーナー）

- 図鑑の実体（`~/pokecard-dex`・data 4.3GB・収集スクリプト30本）は**動かさない**
  ※ いったん「`psa-collection/dex/` へ移す」で着手したが、本人判断で撤回（実体は元どおり）
- 図鑑の単独起動（port 8531）も**そのまま残す**
- 連携は**アルバムのみ**。PSA管理にはポケモン以外のカード（ONE PIECE等）もあるため、
  保有カードと図鑑カードの自動突き合わせ（保有済みバッジ）は**やらない**
- アルバムは「保有中のカード」に加えて「**図鑑のカード＝欲しいカード**」も入れられる

**実装内容**

- [x] 図鑑の画面コードを1本のまま2つの入口から使えるようにする
      （`pokecard-dex/app.py` の `main()` を `render()` と分ける。8531 は今までどおり動く）
- [x] **DBの画像パスは相対**（`data/myca_large/…`）。PSA管理から呼ぶと cwd が変わって
      全滅するので、図鑑フォルダ基準で解決する `_p()` を通す
- [x] サイドバー「表示対象」に **📖 ポケモンカード図鑑** を追加
- [x] 図鑑が無いPCでは選択肢を出さない（＝オプション扱い。PSA管理は単体で成立する）
- [x] アルバムを混在対応にする。図鑑のカードは `dex:<dex_key>` の形で持つ
- [x] 図鑑のカードに「⭐ 欲しい（アルバムへ追加）」を付ける
- [x] **バインダーは保有カードと欲しいカードを混ぜず上下2枠に分ける**（オーナー指示・後から変更）
- [x] 図鑑を launchd 常時起動に登録（`com.shinsei.pokecard-dex`）

**同日の画面調整（オーナー指示）**

- [x] タイトル「PSA保有カード管理」→「PSAカード管理」／左の絵文字を削除
- [x] グレードでの絞り込みを削除（表示・並べ替え・集計のグレードは残す）
- [x] セット・年を「さらに絞り込む」の中へ移動

**完了条件**

- [x] 8527 から図鑑が開け、画像（サムネ・詳細・パック表紙）が出る
- [x] 8531 の単独起動も今までどおり動く（HTTP 200・launchd 常駐）
- [x] アルバムに保有カードと欲しいカードを別枠で並べ、並べ替え・削除ができる
- [x] 既存のアルバム5件（cert のみ）が壊れていない

**検証**: 127.0.0.1 で起動して HTTP 200／`./va.sh shot` で目視（図鑑の検索・拡張パック表紙・
アルバム2枠・サイドバー）／`./va.sh check` で横スクロール0・画像切れ0・重なり0／
「⭐ 欲しい」→ albums.json に `dex:` 行が入り、枠内で移動・削除できることを実機で確認

**状態**: 完了（2026-08-19）

## 次にやる候補

- [ ] 図鑑側の残作業は `pokecard-dex/TODO.md` にある（透かし2枚・参考画像4枚の差し替え等）
- [ ] 「欲しいカード」を集計・書き出しできるようにするか判断
