# TODO — psa-collection（PSAカード管理）

## 進行中

### ポケモンカード図鑑を PSA管理から開けるようにする（2026-08-19）

**目的**: 図鑑（`pokecard-dex`）を**そのフォルダに置いたまま**、PSA管理の中の
オプション画面として開けるようにする。あわせて**アルバムに「欲しいカード」を
図鑑から入れられる**ようにする。

**確定した方針**（2026-08-19・オーナー）

- 図鑑の実体（`~/pokecard-dex`・data 4.3GB・収集スクリプト30本）は**動かさない**
  ※ いったん「`psa-collection/dex/` へ移す」で着手したが、本人判断で撤回（実体は元どおり）
- 図鑑の単独起動（port 8531）も**そのまま残す**
- 連携は**アルバムのみ**。PSA管理にはポケモン以外のカード（ONE PIECE等）もあるため、
  保有カードと図鑑カードの自動突き合わせ（保有済みバッジ）は**やらない**
- アルバムは「保有中のカード」に加えて「**図鑑のカード＝欲しいカード**」も入れられる

**実装内容**

- [x] 図鑑の画面コードを1本のまま2つの入口から使えるようにする
      （`pokecard-dex/app.py` の `main()` を `render()` と分ける。8531 は今までどおり動く）
- [x] **DBの画像パスは相対**（`data/myca_large/…`）。PSA管理から呼ぶと cwd が変わって
      全滅するので、図鑑フォルダ基準で解決する `_p()` を通す
- [x] サイドバー「表示対象」に **📖 ポケモンカード図鑑** を追加
- [x] 図鑑が無いPCでは選択肢を出さない（＝オプション扱い。PSA管理は単体で成立する）
- [x] アルバムを混在対応にする。図鑑のカードは `dex:<dex_key>` の形で持つ
- [x] 図鑑のカードに「⭐ 欲しい（アルバムへ追加）」を付ける
- [x] **バインダーは保有カードと欲しいカードを混ぜず上下2枠に分ける**（オーナー指示・後から変更）
- [x] 図鑑を launchd 常時起動に登録（`com.shinsei.pokecard-dex`）

**同日の画面調整（オーナー指示）**

- [x] タイトル「PSA保有カード管理」→「PSAカード管理」／左の絵文字を削除
- [x] グレードでの絞り込みを削除（表示・並べ替え・集計のグレードは残す）
- [x] セット・年を「さらに絞り込む」の中へ移動

**完了条件**

- [x] 8527 から図鑑が開け、画像（サムネ・詳細・パック表紙）が出る
- [x] 8531 の単独起動も今までどおり動く（HTTP 200・launchd 常駐）
- [x] アルバムに保有カードと欲しいカードを別枠で並べ、並べ替え・削除ができる
- [x] 既存のアルバム5件（cert のみ）が壊れていない

**検証**: 127.0.0.1 で起動して HTTP 200／`./va.sh shot` で目視（図鑑の検索・拡張パック表紙・
アルバム2枠・サイドバー）／`./va.sh check` で横スクロール0・画像切れ0・重なり0／
「⭐ 欲しい」→ albums.json に `dex:` 行が入り、枠内で移動・削除できることを実機で確認

**状態**: 完了（2026-08-19）

## 次にやる候補

- [ ] 図鑑側の残作業は `pokecard-dex/TODO.md` にある（透かし2枚・参考画像4枚の差し替え等）
- [ ] 「欲しいカード」を集計・書き出しできるようにするか判断
