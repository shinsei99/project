# TODO — 加東 貸家チラシメーカー（kato-flyer）

## 完了（2026-08-15）

**紙面の「型」を増やし、配色を切り替えられるようにする。**
マルチプロダクション（`../agent-platform`）で作った紙面の仕組みを、こちらの機能として使う。

- [x] `engine.py` … `../agent-platform` の紙面エンジンを読み込む橋渡し
      （`core.flyer_build.render` が唯一の出口。`core.layouts` の型・`core.palettes` の配色）
- [x] 写真の下ごしらえ … CR2をjpgへ、長辺2400pxへ縮小してキャッシュ
      （エンジンは画像をdata URIで埋め込むので、原寸のままだとHTMLが数十MBになる）
- [x] `app.py` に **型の選択**と**配色の選択**を足す
- [x] ~~`.venv` に playwright を入れる~~ → **不要だった**。向こうの `.venv/bin/python` を
      別プロセスで呼ぶ形にしたので、こちらの `.venv` には何も足していない
- [x] 型ごとに実際に1枚描いて目視確認する（型は完成形なので、描かないと崩れが見えない）

### 決めていること

- **配色の既定は今のまま**。橙 `#f07c1e` × 濃紺 `#1b2340`。
  現地写真に重ねて検証し「木立にも壁にも負けず、工事看板にも見えない唯一の配色」と
  HANDOFF.md に記録があるため、似た色（エンジンの `sunset` = `#e2701a`）で代用しない。
  `flyer_build.render` は色を直接渡すとエンジン側の配色定義より優先されるので、
  **エンジンには手を入れずに**既定を維持できる
- **コピーしない。**エンジンの実体は `../agent-platform` の1つだけにする。
  同リポジトリ内の再利用は `kaitori-dm-maker` → `baikai-generator/services/` に前例がある
- 商号・住所・免許番号は従来どおり `properties.py` の `COMPANY` / `ADDRESS` / `LICENSE` を使う
  （マルチプロダクション側の `config/` は見ない）
- 既存の `flyer.py`（PIL版）は**消さない**。`build_site.py` が `load_image` と `font` を
  使っている。従来の型も「これまでの型」として選べるようにする

### 分かっている差分（目視確認で判断する）

- エンジンの連絡先帯は**社名を文字で出す**。いまのPIL版は SPMロゴ（白版）画像。
  ロゴを出したい場合はエンジン側に部品追加が必要（今回はやらない）
- PDFが**画像PDF（300dpi）→ ベクター**になる。印刷品質は上がる

## 次にやること

- [ ] 掲載名義が未決（新誠PM 06-6935-7267 ／ 大京商事 06-6353-0418）。いまは新誠PM
- [ ] 秋津戸建て11の月額が所有物件台帳で空欄（サイト・ホームズは59,000円で組んである）
- [ ] 上三草の浄化槽管理費が「管理費要」と「管理費なし」で矛盾したまま
- [x] メインPCへの引き継ぎ（2026-08-15・コードと文書17ファイルをgitに登録）

### メインPCで動かすときに別途要るもの（gitに入れていない）

| もの | 中身 | 無いとどうなる |
|---|---|---|
| `.stats_key` | 集計ページの閲覧キー1行（`stats.php?k=…`） | 集計ページを誰も開けない（安全側に倒してある） |
| `.venv` | `python3 -m venv .venv && pip install -r requirements.txt` で作る | 起動しない |
| `data/maisoku/` | 賃貸資料xlsから取り出した素材。`maisoku.py` で作り直せる | 秋津9の間取り図が出ない |
| `site/` | 物件サイトの生成物。`build_kato.py` で作り直せる | 作り直せばよい |
| `../agent-platform` | 増やした型と配色のエンジン（gitにあるが `.venv` は各PCで作る） | 「これまでの型」だけになる |

写真・間取り図はDropboxとGoogleドライブの同期フォルダを直接読むので、
**メインPCで実体が降りていれば設定は不要**（`properties.py` のパスはそのまま使える）。

詳細な経緯・注意点はすべて `HANDOFF.md` にある。**写真の扱い（身分証の混入対策3段構え）は外さないこと。**
