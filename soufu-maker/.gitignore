# 担当マスタ（名前・メールアドレスの個人情報を含む）。公開リポジトリに含めない。
# 既定の担当はコード内 DEFAULT_TANTOU から生成される。
tantou.json

# 記入済みサンプル（顧客名を含む・アプリからは未参照）。公開リポジトリに含めない。
template.xlsx

# 破損していた旧テンプレートのローカルバックアップ
template.docx.orig-corrupt.bak
