# TODO — AI重説アシスタント（旧: AI重説調査〜Excel自動入力）

## 🔧 進行中の大改修: 4書式の書類自動作成へ（2026-08-21 着手・オーナー決裁済み）

> 複数セッション・複数ファイル・**稼働中アプリ（8513）に影響**するため様式で書く。

### 目的

**基本4種（賃貸重説 / 売買重説 / 賃貸契約 / 売買契約）を、できるだけ自動で作成する。**
あわせて、いま**同じ物件・売主・買主を3つのアプリに3回入力している**無駄をなくす。
`PropertyData` を単一の真実にし、謄本PDFを1回読ませれば全書式・特約・検算の初期値が埋まる。

### ★ 現状の実測（2026-08-21・ここが出発点）

**調査で25項目を取っているのに、書式へ流し込んでいるのは基本情報の8項目だけ。**
用途地域・建ぺい率・容積率・洪水浸水想定・土砂災害・津波・最寄駅・駅距離・人口・世帯数・
路線価・公示地価の**12項目は、取得済みなのに1つも書式に入っていない**。
自動化率を上げる一番の近道は `format_export_service.FORMATS` の `map` を増やすこと。

| 基本4種 | 手元の書式 | 流し込んでいる項目 |
|---|---|---|
| **賃貸重説** | ✅ `rental_building_template.xlsx`（建物賃貸借用A4） | **3項目だけ**（所在地・床面積・所有者） |
| **売買重説** | △ `sale_mansion_jyuusetsu_template.xlsx`（**区分所有のみ**。土地・戸建て版が無い） | 8項目 |
| **賃貸契約** | ❌ **書式が1本も無い** | — |
| **売買契約** | ✅ `sale_landbuilding`（土地建物）＋ `sale_mansion_contract`（区分所有） | 9項目 |

> **`templates/*.xlsx` は gitignore**（公開リポジトリのため）。別PCへは Dropbox で運ぶ。

### 「全部自動」の正しい線引き（先に決めておく）

- **自動で埋まる**: 物件の表示（謄本）／法令制限（用途地域・建ぺい率・容積率）／災害／
  周辺環境／価格指標 — **調査で取れるもの**
- **人が打つ**: 賃料・敷金・礼金・売買代金・手付金・引渡日・特約の個別事情 — **取引条件**
  （どのAPIにも無い。ここを自動と言うと嘘になる）
- したがって目標は「全自動」ではなく **「調査で取れるものは全部埋まっていて、
  人は取引条件だけ打てばよい」** 状態。完了条件もこの線で測る。

### 完成後の画面（取引種別で出し分ける）

| タブ | 内容 | 由来 |
|---|---|---|
| ① 物件調査 | 住所＋謄本PDF → 用途地域/最寄駅/人口/ハザード | jyuusetsu-research |
| ② 書類作成 | **4書式から選んで Excel 出力**（重説2・契約2） | jyuusetsu-research を拡張 |
| ③ 特約条項 | 31カテゴリ170項目 → `claude` CLI → Word **【売買のみ・任意】** | **tokuyaku-generator** |
| ④ クロスチェック | 重説+契約書+謄本+行政データの4点検算 **【売買のみ・任意】** | **legal-crosscheck** |

### 実装内容

1. **書式の穴を埋める**（ここが最優先。**人に実書式をもらう必要がある**）
   - **賃貸契約書（建物賃貸借契約書）の白紙書式** … 手元に無い。要提供
   - **売買重説（土地・戸建て用）** … 区分所有版しか無い。要提供
2. **`FORMATS` の `map` を8項目 → 全25項目へ広げる**。取得済みの法令制限・災害・周辺環境・
   価格指標を各書式の該当セルへ流し込む。**賃貸重説は3項目しか入っていないので最優先**
3. **共通モジュールの切り出し**（実装は jyuusetsu 側が新しいので**そちらを正**にする）
   - 謄本PDF解析: `registry_service.py`(46行) ← `legal-crosscheck/registry_parser.py`(96行) を突き合わせ
   - ジオコーディング: `address_service.py`(137行・Google併用) が正。`geo_service.py`(56行) は捨てる
   - 用途地域: `zoning_service.py`(XKT002・100m上限) が正。`admin_research_service.py` は捨てる
4. **③ 特約条項タブを載せる**。**8513 は恒久的に残す**ので、`tokuyaku-generator/clauses.py`
   （170項目・38KB）と本文生成ロジックは **絶対にコピーしない・共有する**。
   コピーすると170項目のカタログが2箇所に分岐し、**片方だけ直した特約が契約書に載る**事故になる。
   → 特約のコアを共有モジュールに置き、`tokuyaku-generator/app.py`（8513）と
   新アプリの③タブが**同じ1本を読む**形にする（`flyer-creator` が `agent-platform/core` を
   使っているのと同じやり方）。**8513側も共有モジュールを読むように書き換える**（ここが作業の肝）。
   売買契約書を作ったら、その特約をそのまま続けて作れる導線にする
5. **④ クロスチェックタブを載せる**。`law_validator.py`(311行) の3レイヤー検閲はそのまま活かす
6. **legal-crosscheck のモックfallbackを廃止**する（`_MOCK_MASTERS`）。
   キー未設定時に「新宿→商業地域80/600」等の**架空の値が"行政の正解"として赤字判定の基準になる**のは
   検閲アプリとして事故の芽。取得できないときは「判定不可」を返す
7. 表示名を **「AI重説アシスタント」** に変更（調査だけのアプリではなくなるため）。
   port は **8536**（8535 まで使用済み。8513 は現役のまま残るので取らない）

### 完了条件（2026-08-21 時点の実測。**10/11 達成**）

- [x] **基本4種すべてに書式がある** … 全宅連の公式書式200本を取得・分類済み
- [x] **調査で取れた項目が書式に入る** … 主要書式で **17/17**。実物の謄本＋住所調査で **12項目**が入る
- [x] 賃貸重説の自動入力が 3項目 → **9〜10項目**（該当欄のあるものは網羅）
- [x] **謄本を1回読ませると ②③④ の初期値が埋まる** … 実測で特約タブに物件・売主が引き継がれた
- [x] **賃貸を選ぶと ③④ が出ない／売買なら出る** … サイドバー①の取引種別で出し分け（実測）
- [x] ③ から Word が、④ から検閲報告書Excel が出る
- [x] 行政データが取れないとき ④ が**モックではなく「判定不可」**と出る
- [x] **8513（特約・社内稼働中）が今までどおり動く** … import・生成とも実測で確認
- [x] **特約カタログの実体が1本だけ** … 直下 `tokuyaku_clauses.py` に集約（8513は薄い入口）
- [x] `smoke_test.py` が通る
- [x] **4書式それぞれを出力して確認** … 2026-08-21 実施。**バグを2件見つけて修正**
      （賃貸重説で所在地がハザード欄に入っていた／1セルに2項目の二重割り当て）。
      **見出しと値の突き合わせ**で検証した（QuickLook は1ページ目のみ、Excel の
      AppleScript PDF 化は -50 で不可のため）
- [ ] **印刷イメージの目視だけ残っている**。LibreOffice を入れるか、メインPCで人が開いて見る
- [x] **自動入力を増やした**（2026-08-23・その2）。①自社情報12欄（`agent_fields`＋直下
      `company_profile.py`。**自社の立場＝媒介／売主で入れる欄を出し分ける**。宅建業者売主版は
      A＝売主・B/C＝媒介で、媒介なのにAへ入れると「媒介なのに売主として署名した書面」になる）
      ②区域指定7種（`legal_area_service`）で64法令のうち3つを機械で■
      ③公示地価（`landprice_service`・XPT002）④追加資料の任意アップロード5種（`document_intake`）
- [x] ~~宅建士の登録番号~~ → **2026-08-23 解決**。鷲見　慎一（大阪）第088651号 /
      塚本　亮介（大阪）第104183号。**契約書にも宅建士の記名がある**ことに気づいて
      対象を重説だけから広げたら見つかった。実書式へ流し込んで実物と一致を確認済み
- [x] ~~自社情報の空欄を埋めてもらう~~ → **2026-08-23 完了**。過去に交付した実物の重説
      （2022年 K1ビル事業用賃貸借）から取り、白紙書式へ流し込んで**12欄が実物と一致**を確認。
      `地方本部` は実物でも空欄なのでそのまま。`免許年月日` は2026年4月版で欄ごと消えており、
      e-Govで確認したところ法35条・37条・施行規則のどこにも無い＝法定記載事項ではない
- [ ] **追加資料は画面表示まで**。書式のどのセルに入れるかは項目ごとの割り当てが残っている
      （区分所有の管理費・修繕積立金の行は作りが規則的なので `checkbox_fill` と同じ手が使える）
- [ ] 8517 の会社マスタ（`baikai-generator/services/company_store.py`）を
      `company_profile.py` に寄せる（いまは別物。二重管理になっている）
- [x] **災害3項目を実装した**（2026-08-23）。洪水 XKT026 / 土砂 XKT029 / 津波 XKT028。
      **書式へはテキストを入れない**（割り当て先が□のチェック欄だった）。
      土砂災害の「内」だけ自動で■にする

### 検証方法

- `.venv/bin/python smoke_test.py`
- `--server.address 127.0.0.1 --server.port 8536` で起動して HTTP 200
- `./va.sh shot` で ①〜④ の4タブを目視（Consoleエラー0件）
- **4書式それぞれを実際に出力し、Excelを開いて目視**（セル座標のズレは目で見ないと分からない）
- 8513 を別に起動し、統合前と同じ Word が出ることを確認（**共有モジュール化の前後で比較する**）
- 実物の謄本PDFで1度通す

### 人にお願いすること（これが無いと進まない）

- [ ] **🔴 最優先: `templates/*.xlsx` 4本が白紙ではなく他社の実案件だった**（2026-08-21実測）。
      直すまで**実務に使わない**。詳細と証拠は README の同名の節。差し替え先の白紙雛形は
      社内 Dropbox の `_雛形・マスター様式/` にある（**実データ0件を実測で確認済み**）が、
      **6ページ分割型でセル座標が全く違うため `FORMATS` は作り直し**になる
- [x] ~~`.doc` 14本の .docx 化~~ → **2026-08-21 解決。`doc2docx.py` を作って14本すべて変換**
      （文字化け0・6本に入力表あり）。Word の AppleScript `save as` は使えず、
      `textutil` は docx 書き出しで表を壊すため、**textutil で RTF → pandoc で docx** の
      2段構えにした。CP932 の生バイトだけ Unicode エスケープに直して文字化けも解消
- [ ] ~~**賃貸契約書の白紙雛形は見つかった**~~（済）（`_雛形・マスター様式/契約書/★★★★★★★賃貸借契約証書
      （特殊詐欺追加）クリーン版（2020民法改正版).doc`）。ただし **`.doc`（旧Word）**。
      **人の作業: Word で開いて `.docx` として保存し直す**（このPCに LibreOffice は無い。
      `textutil` はレイアウトを保てない）。契約書は Excel ではなく `python-docx` の別ルートになる
- [ ] 売買重説（土地・戸建て用）の白紙書式 … `売買/井高野駐輪場/重説-土地建物の売買交換用(A4)
      （20130828）1.xls` がマスター様式の命名に見えるが、**案件フォルダの中なので白紙かは未確認**

- [x] ~~賃貸契約書の白紙書式~~ / ~~売買重説（土地・戸建て用）の白紙書式~~
      → **2026-08-21 解決。ハトサポから全宅連の公式書式200本を取得**し、社内 Dropbox
      `契約・書類/書類雛形/` に仕分けた（すべて2026年4月改訂の最新版・白紙・Excel優先）。
      詳細は `AUTOMATION_MAP.md` の第5節。**アプリの記入済みテンプレ問題もこれで差し替えられる**

### 2026-08-21（その3）で追加した仕様 — 住所の扱いと複数書式

- **`所在地`（住居表示）と `登記所在`（謄本の所在）は別項目**。混ぜない。
  謄本に住居表示は載っていないので、住居表示の出どころは**マイソクか人の入力だけ**
- **住所の入力欄は2つ**（町名まで／丁目・番・号）。日本郵便で確認できるのは**町名まで**
  （丁目すら持っていない。実測で確認済み）
- **入力の順番はマイソク → 謄本 → 住所**。分かる部分は自動で出し、足りない分だけ人が打つ
- **書式は複数選べる**（＋で1本でも6本でも。分類をまたいでも消えない。2本以上はZIP）
- **取引種別（売買/賃貸）で書式を絞る**。売買 11分類139本 / 賃貸 9分類87本。
  種別に関係ない書類（犯収法・取引台帳など）は「共通」で両方に出す
- **分類の下の小分類（フォルダ）を出す**。既定は本体（契約書なら契約書そのもの）
- **同じ小分類の中は1つだけ選ぶ**。増やすときは分類を選び直して「＋」で足す
- **基本セットが先、追加書類は畳む**。セットは 3種別（土地・建物／区分所有／土地のみ）
  × 売主の立場（一般売主／宅建業者売主／消費者契約用）。**既定は一般売主**（媒介が大半）
- **マイソクは鵜呑みにしない**。所在地・最寄駅・種目を謄本と公的データに突き合わせ、
  🟡 があれば「そのまま書類に載せない」と警告する
- **謄本は確定として扱う**。見るのは発行日だけ（30日以内=🟢／90日超=🟡取り直し）
- マイソク解析は**この重説アプリ側に持つ**（`maisoku-converter` には触らない。役割が別のため。
  ただし claude CLI の場所解決・向き補正は共有 `registry_parser.py` を使う）

### 抵当権の扱い（2026-08-21 調査 → **2026-08-23 修正済み**）

**土地建物の `AH112` が建物欄で、土地の抵当権が建物欄に入っていた件は直した。**
`土地抵当権` / `建物抵当権` に分け、権利部(乙区)の □抵当権・□根抵当権 と
その行の詳細欄へ、それぞれの側だけに入れる（`checkbox_fill.detect_rights`）。
**実物の謄本での確認は未了**（共有パーサのスキーマを変えたので、AIが土地/建物を
正しく振り分けるかは実物で見る必要がある）。

土地の売買・交換用に抵当権の「欄が無い」件は、下のとおり**取りこぼしではない**ので
そのまま（自由記入欄には機械が書かない）。

#### 元の調査メモ（2026-08-21）

- **土地の売買・交換用には「抵当権」欄が無い**。様式が違い、「所有権にかかる権利に関する
  事項」のチェック＋自由記入欄（H90/M90）で書く形。**取りこぼしではない**。
  自由記入欄に機械が文章を書くと、地役権・賃借権など他に書くべきことと衝突しうるので、
  自動入力は保留した
- **土地建物の売買・交換用の `AH112` は建物の欄**。謄本から取った抵当権が
  **土地のものでも建物欄に入る**。土地と建物で分けて持つべき（`抵当権` を
  `土地抵当権` / `建物抵当権` に分けるか、書式側の土地欄を特定する）

### 状態

**進行中**（残りは**印刷イメージの目視**と、**抵当権の実物謄本での確認**）。
2026-08-23 に災害3項目・防火地域・抵当権の土地建物分離を実装。

### 関連ファイル

- 母体: `jyuusetsu-research/app.py` / `services/format_catalog.py`（現行の出力経路）
  / `services/xlsx_patcher.py`（無損失パッチ） / `models/property_data.py`
- 2026-08-23 追加: `services/reinfolib_client.py`（不動産情報ライブラリ共通クライアント）
  / `services/hazard_service.py`（災害3項目） / `services/checkbox_fill.py`（□→■）
- 書式: `jyuusetsu-research/templates/*.xlsx`（**gitignore**。別PCへは Dropbox で運ぶ）
- 吸収元: `tokuyaku-generator/app.py`(375行) + `clauses.py`(428行)
- 吸収元: `legal-crosscheck/app.py`(201行) + `services/law_validator.py`(311行)

### 決着した判断（再提案しないこと）

- **母体は jyuusetsu-research**（重説作成がベース、チェックはオプション）。2026-08-21 オーナー判断
- **8513 は恒久的に残す**（2026-08-21 オーナー判断）。畳まない。社内の `.url` / `.app` /
  launchd はそのまま。特約だけ使いたい人が重説アプリを開かずに済む入口として残す。
  → **新アプリは「特約の機能を持つ」だけで、8513 の置き換えではない**
- **したがって統合で消えるのは `legal-crosscheck` の1本だけ。** 全53本 → **52本**
  （不動産32 → 31）。`tokuyaku-generator` は一覧に残したまま
- **`claude` CLI の利用は方針に反しない。** jyuusetsu の「有料LLM不使用」はローカルCLIには当たらない

---

## 進行中・未了（統合とは別件）


- [x] ~~ストリートビューが社内画面で 403~~ → **2026-08-21 解決。**
      Google Cloud Console で **Maps Embed API だけに制限したキー `maps-embed-internal`** を
      新規作成し、直下 `.env.google-maps` に `GOOGLE_MAPS_EMBED_KEY=` を追記した。
      アプリケーションの制限は**なし**（社内画面はポートが変わるためリファラ制限だと再び403になる。
      Maps Embed API は無制限・無料なので、この構成でも課金は発生しない）。
      検証: 埋め込みURL HTTP 200 ／ 画面で「撮影時期 2024-09」表示 ／
      ブラウザのネットワークログで `maps/embed/v1/streetview` が 200、403は消えた

- [x] ~~防火地域の入手経路が無い~~ → **2026-08-23 解決。XKT014 で取れる**
      （`fire_prevention_ja` に「防火地域」「準防火地域」）。2026-08-20 に「レイヤが無い」と
      書いたのは XKT001〜007 しか見ていなかったため
- [ ] **高度地区は依然として取れない**。XKT024 は「高度**利用**地区」で別の制度なので使わない。
      自治体の都市計画図しか無いなら「要確認」で固定する方針を決める

## 完了（2026-08-20）

- [x] 用途地域を XKT001 → **XKT002** に修正し、プロパティ名・単位も実測に合わせた
- [x] 最寄りポリゴンでの代用に 100m の上限（2.9km 先を返す事故を防止）
- [x] ジオコーディングを Google 併用（ROOFTOP のときだけ採用）＋ 画面に出典を表示
- [x] ストリートビューの節を追加（撮影時期の表示・印刷不可の注意）
- [x] **e-Stat の人口・世帯数を実装**（appId 受領。統計表 `0000020101` / `A1101`・`A7101` /
      市区町村コードは国土地理院の逆ジオコーディング）。画面で表示を確認済み
