# TODO — スクラップメモ（scrapmemo-petapeta）

## 進行中（2026-08-16）

- [ ] スクラップ編集シートの改良
  - [ ] 開いたときにメモの一番上が表示されない → 直す
        （原因: textareaのfocusハンドラが「textareaの下端」を見せるよう編集エリアを
        スクロールする。textareaは min-height:480px で編集エリアより高いため必ず下へずれる）
  - [ ] 「キャンセル」「完了」を常時表示（画面下固定）から、
        編集内容の一番下（スクロールした末尾）に置く形へ変更
  - [ ] 長いメモでtextareaが内部スクロールするのをやめ、内容に合わせて伸ばす（autoSize）
  - [ ] iOSシミュレータで表示確認（スクショ）→ 問題なければ build 7 / 1.0.3 で再配信

## 分かっている課題（未着手）

- データがすべて localStorage（`snb6p` ほか）にJSON文字列で入っており、画像はdataURL。
  **約5MBの上限に当たると保存が丸ごと失敗する**。書き出し／取り込み（バックアップ）も無い
- タグ・分類が無く、ページが増えると探しづらい
- Web版（GitHub Pages）とiOS版でデータが行き来しない
