# TODO — スクラップメモ（scrapmemo-petapeta）

## 進行中（2026-08-19）

- [x] **保存容量の問題を根本解決: 画像だけ IndexedDB へ移した**（localStorage 5,100KB は
      WebKit固定で広げられない／IndexedDB は quota 9,830MB）。旧データは起動時に自動移行。
      実測: 大きい写真 **30枚・合計77.3MB** を追加して再起動しても全部残る（以前は1枚で上限）
- [x] **`save()` の `QuotaExceededError` 握りつぶしを解消**（今はトーストとconsole.errorで知らせる）
- [x] **参照されていない画像の掃除（`gcOrphanImages`）を追加**。別ストアにした以上、
      孤児は誰も消さないため。ゴミ箱を空にした時・30日で自動削除された時も実体を消す
- [x] **1.0.4 / build 8 へ繰り上げ、Archive まで完了**（`ios-build-guard.sh` で衝突なしを確認）
- [ ] **人の判断待ち: App Store Connect へアップロード → 審査提出**
      （Organizer と ASC は開いてある。**アップロードはまだしていない**）
- [ ] **未確認（前日からの持ち越し）: 1.0.3 / build 7 が ASC で審査提出まで進んだか。**
      1.0.4 を上げるなら 7 の扱いを先に確認する
- [ ] Web版（GitHub Pages）へ push すれば反映される。IndexedDBはブラウザでも効く

## 分かっている課題（未着手）

- **書き出し／取り込み（バックアップ）が無い。** 画像を別ストアに分けたので、
  バックアップを作るなら imgId の実体も一緒に固める設計が要る（今回で前提が変わった）
- タグ・分類が無く、ページが増えると探しづらい
- Web版（GitHub Pages）とiOS版でデータが行き来しない

## 進行中（2026-08-17）

- [x] スクラップ編集シートの改良（開いたとき先頭が出ない／ボタンを末尾へ）
- [x] 1.0.3 / build 7 で Archive 成功・commit f47056c を push（Web版もActionsで反映）
- [x] Organizer から App Store Connect へアップロード（ユーザー実施）
- [ ] **残: ASCで新バージョン 1.0.3 を作成 → ビルド7 を選択 → 「最新情報」記入 → 審査へ提出**
      （提出まで進んだかは未確認。通過したら CLAUDE.md のアプリ一覧を「1.0.3 配信済み ✅」に更新）
- [ ] 未確認: iOS実機・シミュレータでの表示（今回の確認はSafari 390×844のみ）。
      特にソフトキーボードが出た状態の見え方

## 分かっている課題（未着手）

- データがすべて localStorage（`snb6p` ほか）にJSON文字列で入っており、画像はdataURL。
  **2026-08-18に実測して確定**: 上限は **5,100KB**、超過時は `QuotaExceededError`。
  写真は縮小も圧縮もせず元ファイルのままbase64（約1.37倍）なので **iPhoneの写真1枚で上限**。
  さらに `save()` が `catch(e){}` で例外を握りつぶすので、**画面上は貼れて見えるのに
  警告も出ないまま次回起動で消える**。書き出し／取り込み（バックアップ）も無い
- タグ・分類が無く、ページが増えると探しづらい
- Web版（GitHub Pages）とiOS版でデータが行き来しない
