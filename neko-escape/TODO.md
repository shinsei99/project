# にゃんこ大脱出 — TODO

## 進行中（2026-08-28 メインPC・夜）: iOSアプリ化 → App Store 提出直前まで

**目的**: にゃんこアイス／カラー重力／サイボーグ防衛軍と同じところまで持っていく
（＝**人がやるのは「App 記録を作る」と「提出」だけ**の状態）。

| # | 実装内容 | 状態 |
|---|---|---|
| 1 | 本体を `www/` へ移す（Capacitor の webDir）。**同じ push で `deploy.yml` を `neko-escape:www` に直す**（直さないと gh-pages が落ちる。今日それで全体が止まった） | |
| 2 | Capacitor 8（SPM）で iOS プロジェクト作成。`com.daikyo.nekoescape`・1.0/build1・**MinimumOSVersion 15.0** | |
| 3 | アイコンと起動画面を自前生成（本編と同じネコの顔。**アルファ無し・角丸を付けない**） | |
| 4 | `www/support.html` / `www/privacy.html`（**Appleの必須URL**。gh-pages に出す） | |
| 5 | `store-text.md`（**先に App Store を実測して 4.3(a) スパム判定を避ける**。KeyTag で実際に食らった） | |
| 6 | スクリーンショット iPhone 1284×2778 ×5／iPad 2048×2732 ×5（シミュレータから・タップ不要） | |
| 7 | Archive → ipa 書き出し → 配信物の実測（Frameworks・最低OS・通信の有無） | |
| 8 | `finish-release.py`（記録ができた後は1コマンド）＋ `RELEASE.md` | |

**完了条件**: `build/export/App.ipa` ができ、`altool --validate-app` が
「App 記録が無い」以外の理由で落ちないこと／必須URLが 200／スクショが規定寸法。

**★人にしかできないこと**: App Store Connect で App 記録を作る（`POST /v1/apps` は
403 で API 不可・他アプリで実測済み）／価格・Appのプライバシー・年齢制限・提出。

**このアプリの前提**: 通信なし・課金なし・広告なし・第三者SDKなし＝「データを収集しません」。

## 完了（2026-08-28 メインPC・夜）: 遊びごたえを上げる3点（オーナー選択）

**結果（実測）**: 全30面・解けない面0／★評価は最短手数でクリアすると★3（実機で確認）／
**21面以降は 最短8.5手・勝ち筋率55.2%**（1〜20面は 5.8手・64.5%）＝ねらいどおり後半が重い。
解析器と本体のルールが同じことを **236手ぶん突き合わせて食い違い0件**（わざと1文字ずらすと
ちゃんと検出することも確認済み＝この試験は空振りしていない）。

<details><summary>着手時に書いた計画（そのまま残す）</summary>

実測で「適当に押すと負けるが、全20面を10分で完走できる浅さ」だったので、次の3つを入れる。
**複数セッションにまたがる／複数ファイルを触る**ので様式で書く。

**目的**: 一度解いた面に戻る理由をつくり、後半に本当の手応えと新しい遊びを置く。

**実装内容**

1. **★評価**: ★3=最短手数でクリア／★2=最短+2手以内／★1=クリア。最短手数は
   `tools/analyze-stages.js` が出す値をステージデータに `opt` として持たせる（人が数えない）。
   ステージ選択とクリア画面に出す。保存は既存の `best`（最少手数）から導く＝保存形式は変えない。
2. **新ギミック2種**（後半に新要素が来るようにする）
   - **ねこのぬけ道**: ねこだけ通れる道。ロボットは通れない（＝ソファの「道」版）
   - **スイッチとドア**: ねこがスイッチを踏むと、閉じていたドアが開いて道がつながる（一度きり）
3. **ステージを10面追加**（21〜30面）: 格子から自動生成 → `tools/analyze-stages.js` の
   エンジンで採点し、**最短8〜12手・勝ち筋率35〜60%・解ける**ものだけ採用する。

**完了条件**

- [ ] 30面すべて「解ける」（解析器で確認。1面でも解無なら不採用）
- [ ] 21面以降の平均が **最短8手以上・勝ち筋率60%未満**（＝後半のほうが重い）
- [ ] ★評価がステージ選択とクリア画面に出て、最短手数でクリアすると★3になる
- [ ] 新ギミックが凡例に出て、絵で意味が分かる
- [ ] Console エラー0件／`va.sh check` 0件／3幅ではみ出し0件

**検証方法**

```bash
node tools/analyze-stages.js                 # 30面の難易度表（解無0・カーブの確認）
node tools/crosscheck.js                     # ★解析器と実装のルールが同じか突き合わせる
../va.sh console / check / responsive        # 画面
```

**★ルールの二重実装に注意**: 解析器（node）と本体（index.html）で同じルールを2回書いている。
新ギミックを足すと**ズレたことに気づけない**ので、`tools/crosscheck.js` で
「同じ手順を両方に流して同じ状態になるか」を突き合わせること。

**状態**: 完了 ／ **関連ファイル**: `index.html` / `tools/engine.js` / `tools/analyze-stages.js` /
`tools/gen-stages.js` / `tools/crosscheck.js`

</details>

## 完了（2026-08-28 メインPC）: 見た目と手触りを作り替える（にゃんこアイスと同じやり方で）

**目的**: 絵文字を並べただけの画面をやめ、「おじいちゃんの家から逃げる」情景が
一目で分かる画面にする。にゃんこアイス（`nyanko-ice`）で効果があった3点を同じ順で入れる。

| # | 実装内容 | 状態 |
|---|---|---|
| 1 | 盤面を canvas の手描きに（床・道・座布団マス・ネコ・ロボット・キャットタワー・ソファ・毛糸・さかな・落とし穴） | 完了 |
| 2 | 書体 Zen Maru Gothic（SIL OFL）を**使う文字だけ**同梱（`tools/fetch-font.py`） | 完了 |
| 3 | 音を WebAudio で合成（効果音9種＋BGM＋オン/オフ）。音のファイルは同梱しない | 完了 |
| 4 | 動きを付ける（猫は弧を描いて跳ぶ／ロボットは傾いて滑る／しっぽ・まばたき・紙吹雪・画面ゆれ） | 完了 |
| 5 | タイトル画面・ステージセレクト・クリア状況の保存（localStorage）・手数表示 | 完了 |
| 6 | ロボットが次に来られるマスを薄く示す（子どもが詰まないように） | 完了 |

**完了条件**: `./va.sh` で開いて **Console エラー0件**、タイトル→ステージ1クリア→
ステージ選択の3画面を目視、iPhone幅（390px）で崩れていないこと。

**検証方法**: `./va.sh console neko-escape/index.html` ／ `./va.sh responsive`

**関連ファイル**: `index.html`（1枚で完結）／`tools/fetch-font.py`／`assets/fonts/*.woff2`

**検証の結果（2026-08-28 実測）**: Console エラー0件／`va.sh check` 指摘0件／
390・768・1440 の3幅ではみ出し0件／全20ステージを読み込んで3手ずつ打って例外0件。
詳細は `SESSION_LOG.md` の同日の節。

## 残っている

- [ ] **音を一度聴く（オーナー）。** 鳴ることは確認済みだが、心地よいかは未確認。気に入らなければ
      `assets/audio/` に効果音ラボ等（商用可・再配布可）のファイルを置いて `USE_FILES = true`
- [x] **公開する**（2026-08-28 オーナー判断）→ `deploy.yml` の `DEPLOY_FOLDERS` に `neko-escape` を追加済み。
      **main へ push した時点で https://shinsei99.github.io/project/neko-escape/ が出る**

## わかっていること

- **公開に切り替える前は gh-pages に無かった**（2026-08-28 実測。`git ls-tree origin/gh-pages` に
  `neko-escape` が無かった）。`CLAUDE.md` の一覧では以前から「GitHub Pages」と書かれていたが、
  実態は未公開だった。**一覧の表記と実際の公開状態は一致しないことがある**ので、
  他のアプリでも公開状態は `git ls-tree origin/gh-pages` で確かめること。
