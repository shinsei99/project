> ## 📁 引き継ぎ（2026-09-05 メインPC）— **Dropbox整理。大京本社ビルが見本として完成**
>
> **詳細は `chatwork-ai-manager/DROPBOX_整理計画.md`。ここは再開点だけ。**
>
> ### 今日やったこと（夕方の続き）
>
> | | |
> |---|---|
> | **20件超の物件の直下をゼロに** | 100件→0件。仕分け率10%→52%。拡張子の大文字・全角スペースで割れた語・書式名を人名と誤読する不具合を修正（架空のテナントの箱2つが実在していた） |
> | **巡回写真 9枚→58枚** | 84枚中9枚しか入っていなかった。実体が消えていた75枚をChatworkから取り直し。`properties.aliases` 未参照・id型の食い違い（文字列↔数値）・同名別写真の握り潰しを修正。**週次ジョブへ組込み済み** |
> | **属人的なフォルダ名を10か所解消** | 会長預り／専務預り／吉浦／塚本／HIRAKI を機能名へ。業務書類21件は内容で分類し直し、私物1件のみアーカイブへ |
> | **会長預り案内書101件を再配置** | フォルダごと解体。7件は既存の仲介案件へ合流 |
> | **年度計画表に4色の状態** | 実施済み／そろそろ／★超過／まだ先。★超過15件・そろそろ11件。**全体チャットワークへ投稿し担当へ確認依頼済み** |
> | 未収録一覧のフォルダ無し | 10件→**0件**（名寄せの穴6種）。台帳の競合コピーも統合 |
> | 一覧・バックアップ | 全ファイル一覧 43,171行／社内バックアップ 38,283件・20G（削除0件） |
>
> ### 午前までにやったこと
>
> | | |
> |---|---|
> | **`（★必読★）新共有フォルダ` を外した** | 38,151件を1階層上げ、件数完全一致。コード8本＋道具13本＋DB設定3つ＋索引11,930行＋社員の`.url`24件を修正 |
> | 重複の整理 | 171件・920MB をアーカイブへ。Dropboxの競合コピー8件も |
> | アーカイブ統合 | 同名2つを1つに（8.9GB）。共有フォルダ直下はスッキリ |
> | 仲介→物件 | 自社物件の契約書346件を物件フォルダへ |
> | **大京本社ビル** | **179件を整理し、見本として完成**（棚5つ・直置き0件） |
> | 検針表 | 16件を `検針関連/各メーター検針表/` へ集約。旧版は `過去分/` へ |
> | 1ファイルフォルダ | 名前が同じ31個を畳んだ |
>
> ### ★確立した法則（`DROPBOX_整理計画.md` に全部ある）
>
> 1. **フォルダはバラさない**（バラして正本が分からなくなり159件を全戻しした）
> 2. **持ち主を表す箱は中が1件でも残す**（`入居者/` `解約・精算/` の下）
> 3. **判定は物件フォルダの直下だけ**（本社では98%が自動で決定）
> 4. **誰の書類かはレントロールで決める**（載っていなければ `解約・精算/` に箱を作る）
> 5. **新旧は更新日でなく中身で決める**（「開いて保存しただけ」に2度騙されかけた）
> 6. **ファイル名を信じない**（中身が別物件の契約書だった例あり）
> 7. 判定できないものは動かさない ／ 深さ7〜8は許容（オーナー了承）
>
> ### 次にやること
>
> 1. **マンションと駐車場を1つずつ試す**（`メゾンドール都島` / `十三駐車場`）。
>    本社はビルなので素直だった。他の種別で法則が通じるか確かめてから98物件へ
> 2. **メールBOX・宅配BOXの番号**（検針と同じ構図）。正本 `物件・管理/郵便受・宅配BOX一覧.xlsx`
>    があるのに専用フォルダに11件・物件フォルダに11件が散在。
>    **★ただし宅配BOXの暗証番号が載っているので、全社員が見える場所でよいかは別途判断**
> 3. `その他` 棚の誤判定を潰す（`㈱日立ビルシステム` が「連絡」に反応して落ちる等716件）
>
> ### ★人にしかできないこと（未着手）
>
> - **社員への案内**。フォルダの場所が変わり、各自のショートカットは切れている。
>   検針表の置き場も変わった。あわせて2026-09-03の案内（**ファイル探しは Chatwork の claude へ**）もまだ
> - `~/Desktop/削除予定_20260904_Dropbox重複/` の空の殻を消す（`rm` は Claude の deny）
>
> ### バックアップ
>
> - **日曜6:00に走るが `MAX_DELETE=50` で自動中断する**（今日585件動かしたため）。勝手に上書きされない
> - 整理前の姿を `社内バックアップ/共有フォルダ_20260826_整理前/`（22,491件・12GB）に保存済み
> - 中身を確認して正しいと判断したら: `MAX_DELETE=99999 bash /Users/apple/dropbox-backup/backup.sh`
>
> ---

> ## 📁 引き継ぎ（2026-09-04 メインPC）— **Dropboxの整理をどうするか。結論は出ていて、次の一手は「測ること」**
>
> **並行セッション向けの引き継ぎ。この節だけ読めば続きから入れる。**
> きっかけは「OCR→種別判定→物件紐づけ→要約→CSV出力→電帳法対応の保管、という仕組みで
> いまのDropboxの整理を改善できるか」という相談。**調べた結果、大半はもう持っていた。**
>
> ### 1. いま持っているもの（実測）
>
> | 記事の工程 | 現状 |
> |---|---|
> | ①アップロード | **ある**（書類キャビネット 8528 → Dropbox `書類取込/`） |
> | ②OCR | **ある。二段構え**（macOS Vision 2.3秒/ページ → 読めない分だけ claude。毎晩01:00に300件） |
> | ③種別の判定 | **半分**。`category` はあるが**表記が割れている**（`契約・書類` 2,112件 と `01_取引・契約` 1,258件が別物として並ぶ） |
> | ④物件への紐づけ | **★ここが穴。** 全文検索では出るが、**文書と物件マスタが構造として繋がっていない** |
> | ⑤要約 | AIに聞けば出る。**要約ファイルは作っていない** |
> | ⑥構造化CSV（賃貸革命へ） | **ほぼ無い** |
>
> 索引は **18,809文書・164,934チャンク**（`chatwork-ai-manager/data/app.db`）。
>
> ### 2. 「新しく整理したフォルダを作る」案 — **もう一度やると3代目になる**
>
> 会社Dropboxの共有フォルダ直下は**2つだけ**だった（実測）。
>
> ```
> （★必読★）新共有フォルダ        12GB   ← 現役（2代目）
> _アーカイブ（2027年7月削除予定）  7.5GB  ← 凍結した旧（1代目）
> ```
>
> **旧を凍結して削除期限を切る**という、いちばん正しいやり方で一度移行済み。
> **崩れているのはトップの分類ではなく、中の深さ**（実測）。
>
> | フォルダ | ファイル | 最大の深さ |
> |---|---:|---:|
> | **物件・管理** | **8,856** | **10層** ← 迷子はほぼここ |
> | _アーカイブ | 4,201 | 8層 |
> | 経理・請求 | 3,232 | 5層 |
> | 営業・募集 | 2,499 | 6層 |
> | 契約・書類 | 2,334 | 7層 |
> | 業者・設備 | 1,086 | 5層 |
> | 社内・総務 | 489 | 4層 |
>
> **引っ越しても10層は直らない**（構造を写すだけ）。だから3代目は勧めない。
>
> ### 3. 取りうる案は2つ
>
> - **A: 生成物として毎晩作り直す読み取り専用フォルダ**（正本は今のまま＝二重管理にならない）。
>   ただし **Dropboxはシンボリックリンクを実体としてコピーする**ので、中に置くと本当に複製される
>   （全部で+12GB）。やるなら**契約・書類＋経理・請求のPDFだけ（5,566件）**に絞る
> - **B: フォルダを増やさず「見え方」だけ作る**（`全ファイル一覧.xlsx` を物件別・種別・年で引けるよう厚くする）。
>   **複製ゼロ・社員の覚え直しゼロ・1ファイル追加だけ**
>
> **推奨は B が先、効かなければ A。**
>
> ### 4. ★次の一手 → **2026-09-04 に測り終えた。計画書ができている**
>
> **測定結果と計画は `chatwork-ai-manager/DROPBOX_整理計画.md`。** 以下は要点だけ。
>
> - **名寄せは 63.7%**（パス45.6% ＋ 本文18.0%）。8割には届かなかったが、
>   **外れた原因は文書側ではなく台帳側**だった
> - 物件フォルダは **88個・4,706文書**。マスタと突き合わせると
>   **そのまま一致52(59%) / 表記ゆれ13(15%) / マスタに存在しない23(26%・954文書)**。
>   **別名表13行とマスタ追加23件で 95%以上に上がる見込み**
> - **`properties` に `aliases` 列が無い**のが根本原因（`shinsei_properties` にはある）
> - **「10層」の正体は棚名の割れ**。`解約清算書`/`解約精算書`/`解約用紙`、
>   `リフォーム見積・請求書`/`リホーム等請求書`（打ち間違いが定着）など。
>   `賃借人資料（メゾンドール都島）` のように**物件名を棚名に埋めている**ものもある
> - **標準の棚9つを決めると 97.0% が自動で入る**。ただし **51.6% が複数該当**＝
>   **棚の優先順位は人が決める必要がある**
> - 重複 **312組・余分346件**（宅建更新資料が3フォルダに丸ごと重複）。
>   **10年超が26.6%（3,086件）、5年超で40.7%**
>
> **計画は Phase 0（台帳の穴埋め・DBに列と行を足すだけ）→ Phase 1（B案・`全ファイル一覧.xlsx` に
> 物件/棚/種別/年/重複/古さの列を足す）→ Phase 2（掃除の一覧を出す。消すのは人）。**
> **Phase 1 まで Dropbox の実物は1バイトも触らない。**
>
> 測り直す道具（読むだけ）: `chatwork-ai-manager/tools/measure_dropbox_{match,folders,shelves}.py`
>
> **★人に決めてもらうことが3つ**（計画書の第4節）: ①棚の優先順位 ②マスタ未登録23物件の扱い
> （`konami`320件・`隆生福祉会（ゆめ）`227件・`角屋産業`156件が何なのか）③Phase 2 を実行するか
>
> - **単純な部分一致は誤爆する**。物件名の癖に注意（**SBP＝シェローバイクパーク**の頭文字、
>   オーナー名が括弧で付く、住所がそのまま名前）。[[reference_daikyo_naming]] を先に読むこと
>
> ### 5. 触ってはいけない・やらないこと
>
> - **Dropboxのフォルダを自動で並べ替えない。** 全社員が毎日開く実物。リンクも記憶も切れる
> - **`全ファイル一覧.xlsx` を止めない**（週次・日曜5:00。社員が直接開く実物でもある）
> - **電帳法は全部やらない。** 義務は**電子取引データ**（メール添付の請求書・領収書）で、
>   原本は**すでにメールアーカイバ 8535／社内 8538 に入っている**。紙のスキャンは任意。
>   やるなら**検索3項目（取引年月日・取引金額・取引先）に絞る**。事務処理規程を備え付ければ
>   タイムスタンプは不要。**法令の細部は `一次資料/` の国税庁「電子帳簿保存法 一問一答」で裏を取ること**
>
> ---

> ## 🗑 2026-09-03: アプリを4本廃止した（56本 → **52本**）。**社内リンク2本が切れている＝要対応**
>
> オーナー判断で、**AI業務マネージャーと重複していた4本**を廃止した。
> `building-manager` / `file-finder`(8520) / `ai-ticket-counter`(8600) / `handwriting-ocr`。
> 根拠は直下 `SESSION_LOG.md` の 2026-09-03 の節（実測値つき）。要点は
> **building-manager の独自データはゼロで、AI側は同じExcelを毎晩自動で索引していた**こと。
>
> ### ★人にしかできないことが2つ残っている
>
> | # | やること | なぜ |
> |---|---|---|
> | 1 | **★社員への案内を出す**（オーナーのみ可） | 入口は**2026-09-03 に撤去済み**（Dropboxの `.url` 2本・`icons` 2個・Desktopの `.app` 2本）。**リンクが消えたことを社員はまだ知らない。** 伝える中身は「**ファイル探しと不具合報告は Chatwork の claude に頼んでください**」。※`全ファイル一覧.xlsx` 本体は共有フォルダに残してあり、週次更新も続いている |
> | 2 | **退避フォルダの実削除** `~/Desktop/削除予定_20260903/`（1.7GB） | `rm` は Claude の deny に入っているので人が Terminal で。コードはgit履歴に残っている |
>
> ### ★触ってはいけないもの（消しかけた）
>
> **`全ファイル一覧.xlsx` を毎週作る仕組みは廃止していない。** `file-finder` に同居していたが、
> **AIの知識索引・`find_files` の元データ・全社員が開く実物**の3役を兼ねていたため、
> `chatwork-ai-manager` へ移設して存続させた（`com.shinsei.chatwork-ai-manager-inventory`・日曜5:00）。
> **移設先で 25,372行の書き出しを確認済み。** 次の日曜に本番が流れるので、月曜に
> `tail ~/Library/Logs/com.shinsei.chatwork-ai-manager-inventory.log` を見ること。
>
> ---

> ## ⏰ 2026-09-03 の朝いちで確認すること（2026-09-02 メインPC）
>
> **外出先のスマホから繋ぐ Claude Code セッションを常駐させた**（`com.shinsei.claude-remote-control`）。
> オーナーの実機で「デバイスを追加」に出ることまで確認済み＝**もう使える**。
>
> **未確認が1つだけ**: 毎朝5:00 に `-restart` が落として立て直す（＝文脈を `/clear` する代わり）が、
> **セッションIDが変わったあと、スマホ側が自動で拾うのか／「デバイスを追加」をやり直す必要があるのか
> 分かっていない。** 9/3 の朝、スマホで開いて確かめること。
>
> - 毎朝追加し直しが必要だった → **自動リセットをやめる**（`-restart` を bootout）。
>   代わりにアプリ上で `/clear` を手で打つ運用にする。枠の面ではどちらでも同じ
> - 自動で拾った → このままでよい。この節を消す
>
> 生死は `./remote-control.sh --status`。経緯は直下 `SESSION_LOG.md` の 2026-09-02 の節。
> **未 push のコミットが1件ある**（`5a05f7ee`）。サブPCへ渡すなら push が要る。

---

> ## 🌱 引き継ぎ（2026-08-30 サブPC）— 新規アプリ2本を作った。**やることは `git pull` だけ**


>
> **機密ファイルの受け渡しはありません**（Dropbox の一時置き場も作っていない）。
> サブPCは常駐0本のまま。**LANに出ている待ち受けもゼロ**（`dev-doctor.py --sync` で確認済み）。
>
> ```bash
> cd ~ && git pull
> ```
>
> ### 作ったもの（3コミット・push 済み）
>
> | アプリ | 中身 | 状態 |
> |---|---|---|
> | **glow-runner**（グロウランナー・ゲーム7本目） | Three.js 1枚の3Dランゲーム。光るボールで動物6種をよける。**10%でドラゴンが出て、光って1.5秒後に前方だけへレーザー**（予告は橙の地面帯・本体は赤白・後ろは安全） | 動作確認済み。**音・石・コインは未実装**（構造だけ用意）。**公開はオーナー判断で未実施** |
> | **growth-tracker（GrowLog）**（ツール17本目・port 3005） | 子ども向け成長記録 PWA。Vite+React+TS+Tailwind v4+recharts、データは localStorage のみ。ホーム/グラフ/くらべる/カレンダー＋バッジ | 動作確認済み。**実機スマホ未確認**／**平均身長の表は目安値で照合未実施**／**公開は未判断** |
>
> ### 知っておくと得なこと（今回踏んだ分）
>
> - **直下 `.gitignore` は1行目 `*` で全無視**なので、新規アプリは `!<app>/` と `!<app>/**` を足さないと
>   `git add` しても黙って無視される。追跡されるかの判定は **`git add --dry-run`**
> - **`.git/index.lock` の残骸で git が止まる**（今回2回）。`pgrep` で git が動いていないことを
>   確かめてから消せばよい
> - **`git pull --rebase` のあとは開発サーバーの生死を見る**。作業ツリーが書き戻されると Vite が
>   再起動 → そのまま落ちることがある（今回「アプリが開かない」の原因はこれ）
> - **PWA の service worker は本番ビルドでだけ登録する**。開発中に登録するとキャッシュが残って
>   「直したのに変わらない」が起きる
>
> ### 次にやるなら
>
> - glow-runner: 人が遊んで難易度調整（`CONFIG.dragon` の `charge` と `rate`）／音／石・コイン
> - GrowLog: 実機スマホで操作感とホーム画面追加の確認／平均身長の表を公表値と突き合わせ
> - どちらも**公開するかはオーナー判断**（App Store は 4.3(a) で提出停止中のため、出すなら Web のみ）
>
> ---

> ## 🛑 App Store への提出を停止中（2026-08-29〜）


>
> **2026-08-29、3本まとめて Guideline 4.3(a)（スパム）でリジェクトされた。**
> KeyTag鍵管理 1.0 ／ スクラップメモpetapeta 1.0.5 ／ にゃんこのアイス屋さん 1.0。
> **3通とも同一の定型文**で、提出は約6時間のあいだに集中していた。
>
> **アプリ個別ではなく、アカウント全体の出し方を見られていると考えられる**:
> ・**スクラップメモ 1.0.5 は「配信中アプリのUI修正だけの更新」なのに 4.3(a)**（中身では説明が付かない）
> ・KeyTag は 1,153字の審査ノートで 4.3(a) に具体反論していたのに、同じ定型文で返ってきた
> ・6月末から2か月で**9本**提出。多くが Capacitor の同じ殻で、**ネイティブ実行ファイルはほぼ同一**
>   （実測: にゃんこ大脱出 105,472 バイト / サイボーグ防衛軍 105,488 バイト）
> ・ストア文言・サポート/プライバシーページも、アプリ間で置換して作った同じ型
>
> **★いまここ（2026-09-04 12:51）**: **1.0.5 が7日動かないので、一般の問い合わせフォーム
> （`developer.apple.com/contact/jp/` →「App Review ステータス」）から状況照会を出した。
> お問い合わせ番号 102953350193。** Resolution Center は6日間 無反応。
> 平均は10時間（2026年8月実測）なので、6日は明らかに外れ値。**取り下げ再提出はしない**
> （列の最後尾に回るだけ）。返事はメールアーカイブに入るので機械で拾える。
> **9/12 で動かなければコールバック依頼。** 経緯は `scrapmemo-petapeta/SESSION_LOG.md`
>
> **（2026-08-29 09:12）**: スクラップメモ 1.0.5 を返信つきで再提出＝審査待ち。
> **結果が出るまで KeyTag を含め何も出さない**（返信の中で「1件ずつ出す」と書いたため）。
> 確認: `python3 appstore_api.py --review com.shinsei99.scrapmemo`
>
> **★優先順位（2026-08-29 オーナー指示）**: ①**スクラップメモ 1.0.5 の更新** ②**KeyTag の提出**。
> **この2つが片付くまで App Store には一切触らない**（記録の作成・提出・アップロード・削除も含む）。
> その間、**手元の作業（ネオン化・統合）は進めてよい**。
>
> **やってよいこと**: ipa の作成・スクショ・文言の準備（外に出なければ問題ない）
> **やってはいけないこと**: **App 記録の新規作成／提出／アップロード／再提出**。
> 用意済みの4本（にゃんこ大脱出・カラー重力・サイボーグ防衛軍・ひよこ防衛軍）は**出さずに保留**。
>
> **いまの状況**: **KeyTag に返信済み（2026-08-29 07:32 JST）。いまは返事待ち。**
> 送った内容は ①4.3(a) への回答（アカウント全体の話として・出し方の非を先に認める）
> ②にゃんこアイスの記録削除の依頼（Rejected 状態では自分で消せないため）
> ③「どのアプリが重複と見なされているか教えてほしい」という質問。
> 返信文は `keyline/keytag/reply-4.3a-2026-08-29.md`（★審査ノートではなく**返信**で送ること。
> 8/28 は返信を付けずに出して、同じ定型文で返された）。
> **配信中の6本は無事**（2026-08-29 に API で実測）。
>
> 結果が出るまで、この節を消さないこと。
>
>> ## 🗓 次の一手（2026-08-29 オーナー決定）
>> **週明けに「スクラップメモ 1.0.5」だけを出す。ほかは何も出さない。**
>> | 順番 | やること | 誰が |
> |---|---|---|
> | 1 | にゃんこアイスの **App記録を削除**（ASC →「マイApp」→ App情報 → 下部「Appを削除」） | オーナー |
> | 2 | **KeyTag に返信**（`keyline/keytag/reply-4.3a-2026-08-29.md`・Resolution Center の「返信」で） | オーナー |
> | 3 | **週明け**: スクラップメモ 1.0.5 を**一言付きで**提出（`scrapmemo-petapeta/reply-4.3a-2026-08-29.md`） | オーナー |
> | 4 | 結果を見る `python3 appstore_api.py --review com.shinsei99.scrapmemo` | どちらでも |
>> **なぜ 1.0.5 を選んだか**: **失うものがゼロで、得られる情報が大きい**から。
> 1.0.4 が配信中なので弾かれても実害がなく（`1.0.5=REJECTED` と `1.0.4=READY_FOR_SALE` は
> **現に両立している**・2026-08-29 実測）、中身は**ボタン位置だけの修正**で説明が立つ。
> そして「配信中アプリの更新が通るか」で、**アカウントの札が外れたかどうかが分かる**。
>> **判定**: 通れば札が外れた公算 → 次は間隔を空けて1本だけ。
> また同じ定型文なら個別アプリの話ではないと確定 → **ゲーム5本を1本に統合**する案へ。
>> **守ること**: **焦って何度も出し直さない**（短期間の連続提出が今回の引き金）。


> ## ✅ Zenn の上限は解けた（2026-09-03 確認）— 以下は経緯の記録
>
> **2026-09-02 22:00 に `deploy-not-reflected` が公開された**（8/25以来8日ぶり・Zenn 12本）。
> 静止期間（9/2 22:00まで）が明けて `zenn-daily` が自動で再開し、push から10秒で公開。
> **Zennへの問い合わせ（`drafts/zenn-inquiry.md`）は送らなくてよくなった。**
> 効いたのは「詰まった記事を `articles/` に置きっぱなしにしない」＝
> **開発の push ごとに投稿を試みる状態をやめたこと**（下の 8/31 の記述が原因の本筋）。
>
> **note との本数も 9/3 の晩に揃える**（2026-09-03 オーナー判断・対応済み）:
> Zenn 12本 / note 13本 と note が1本先行していた。正体は `excel-calendar-theme-color`
> ＝ note だけ 8/28 に出て、Zenn版の原稿は待機にあるのに `zenn_order.txt` へ載せ忘れ、
> 最後尾（約1か月後）に回っていた。**そのあいだ note 記事末尾のZennリンクが切れていた。**
> → ①順番表の先頭へ移して**今夜のZennはこの1本**（`55dfe196`）
> ②note に期限つきの静止ファイルを新設して**今夜だけ見送る**（`39895f52`・9/4 12:00 で自動復帰）。
> **9/4 の朝に `curl -s "https://zenn.dev/api/articles?username=shinsei99&count=100"` が
> 13本、note も 13本になっていることを確認すること。** 以降は毎晩1本ずつ並走する。
>
> <details><summary>経緯（2026-08-30 時点の記述。原因究明の記録として残す）</summary>
>
> **8/26朝に在庫22本を一度に `published: true` で push してレート制限に当たり、
> 8/25を最後に1本も公開できていない**（Zenn 11本 / 待機26本）。
>
> **8/30 に条件を変えて3回試し、3回とも拒否された** ＝ **記事側でもコミットの形でもなく、
> アカウント全体に上限がかかっている**（① 既出スラッグを再push ② 移動して新規投稿
> ③ **Zenn未見のスラッグ**を、8/25の成功時と**同じ「新規追加(A)・単独コミット」**で push）。
> `CLAUDE.md` にあった「24時間空けて再push（空コミットで可）」は**この状態には効かない**。
>
> **★オーナーにしかできない作業（これが本筋）**:
> Zennの問い合わせフォームから**上限の解除を依頼する**。
> **文案は `ai-tools-base/drafts/zenn-inquiry.md` にある**（そのまま貼れる）。**自動化できない。**
> ※ 当初「大量に一度に公開したいので上限を緩和してほしい」と書こうとしたが、**それは誤り**。
>   こちらは**毎晩1本**の運用なので食い違う。**ミスを認めた上で、直したことを示し、解除を頼む**
>   のが実態に合う（2026-08-30 オーナー指摘）。
>
> **いま自動でやっていること**: `ai-tools-base/drafts/zenn_quiet_until.txt` に
> `2026-08-31T12:00` を書いて、**一度も投稿を試みない24時間**を作っている
> （試行そのものが「直近24時間の投稿数」を食い直している疑いがあるため）。
> **日時を過ぎれば勝手に復帰する**ので、放置してよい。前倒しするならファイルを消すだけ。
> → **8/31 22:00 の通常ローテーションで1本**（`deploy-not-reflected`）を出す。
> **9/1 朝に結果を見ること**: `curl -s "https://zenn.dev/api/articles?username=shinsei99&count=100" | grep -c slug`
> が 12 になっていれば解けた。11 のままならZennへの申請待ち。
>
> **やってはいけないこと**: `articles/` に未公開の `published: true` を**2本以上**置くこと。
> 溜まると**毎回のデプロイでその全部を投稿しようとする**（8/30朝に3本まで増えていた）。
> 関門（`zenn-daily.sh`）がこれを守るので、手で置かないこと。
>
> </details>
>
> **★この「置きっぱなしにしない」だけは今後も守ること。** 上限が解けたのはこれが効いたため。

> ## ⚠️ 全セッション共通（2026-08-28 夜）— **素の `git commit` を使わない。実際にコミットが消えた**
>
> **この作業ツリーは複数のセッションが同時に触るが、git のインデックスは1つしかない。**
> パス指定なしの `git commit` は、他人の作業を巻き込むか、**他人のコミットを消す**。
>
> **実際に起きたこと**: `.git/index.lock` が取り残されて（0バイト・約90分）通常の `git add` が
> 使えなくなり、一時インデックス＋`commit-tree` で迂回コミットした（`3a4d81af`）。その副作用で
> 本インデックスが古いHEADのままズレ、そこへ **日次の自動ジョブ `ec84f66e` が
> パス指定なしでコミットして、12ファイルを削除・巻き戻した**。ディスク上のファイルは
> 無事だったので `1cd3fe44` で復旧（内容が完全一致することを確認済み）。
> ついでにその復旧コミットへ `piyo-defense` の作業中ファイル8件が巻き込まれた（削除は無し）。
>
> **★守ること — 結論: 毎回「専用インデックス」でコミットする（2026-08-29 夜に確定）**
>
> 1日で**3回**同じ事故が起きた（①日次ジョブが31ファイルを消した ②その復旧コミットが
> piyo-defense の作業を巻き込んだ ③`git rm --cached` の削除を通そうとパス指定を外したら
> 91ファイルを巻き込んだ）。**原因は全部同じで「共有インデックスを触ったこと」。**
> パス指定だけでは足りない。`git commit -- <パス>` は **HEAD ＋ 指定パスの作業ツリー**で
> 木を作るので、**`git rm --cached` の削除が反映されない**。
>
> ```bash
> export GIT_INDEX_FILE=/tmp/my.index
> git read-tree HEAD            # 毎回ここから。他人のステージを引き継がない
> git add -A -- <自分のパス>     # 削除もここに入る
> git commit -m "..."           # 共有インデックスに一切触らない
> git push origin main
> ```
>
> **共有インデックスを汚さないので、3セッション並行でも事故らない。**
> 使い終わったら `unset GIT_INDEX_FILE`（付けっぱなしだと次の git が別indexを見る）。
>
> - `.git/index.lock` が残っていたら、まず `pgrep git` が0件であることを確認してから外す。
>   外したあと **`git read-tree HEAD` を1回**流して、本インデックスをHEADに合わせる
> - **`rm` は `~/.claude/settings.json` の deny に入っている**（`Bash(rm *)`）。
>   Claude からは外せないので、人が Terminal で叩くこと。退避（`mv`）は通る
>
> **★機械が起こした事故なので、運用として直す価値がある。** `ai-tools-base` の日次自動投稿
> （`com.shinsei.daily-write` 22:45 / `note-daily` 22:35）が同じことを毎晩やる可能性がある。
> **自動ジョブ側のコミットをパス指定に変える**か、専用インデックスで動かすかを検討すること。
>
> ---
>
> ## 🍎 引き継ぎ（2026-08-28 メインPC・朝）— **KeyTag が 4.3(a)スパム でリジェクト。作り直して build 4 まで上げた**
>
> **人にしかできないことが2つ残っています**（下の★）。サブPCは `git pull` のみ。
> 機密ファイルの受け渡しはありません。
>
> ### 何が起きたか
>
> 2.1（NFCのデモ動画）に回答して再提出 → 今度は **Guideline 4.3(a) — Spam**
> （他の開発者のアプリと似ている）でリジェクト。
>
> **★調べたら「似ている相手」は NFCツールではなく、`hao du` という開発者が量産した
> 台帳アプリ群だった**（`鍵台帳-貸出返却管理2026` / **`賃貸キー受渡し台帳2026`**＝大家・
> 不動産仲介向けで対象も機能もほぼ同じ / `工具貸出台帳…` ほか多数）。
> **それらは NFC を使わない**（説明文に NFC の語ゼロ・全部手入力）。
> つまり **NFC は捨てるものではなく差別化の中心**。
> ※調査前に「名前からNFCを外し台帳を前面に」と考えたのは**方向が逆だった**。
> 同種の判断をするときは**先に App Store を実測すること**。
>
> ### やったこと
>
> | | |
> |---|---|
> | 掲載名 | `KeyTagNFC` → **`KeyTag鍵管理`**（jp/us とも空きを実測）。ホーム画面の表示名 `KeyTag` は据え置き |
> | ストア文言 | `keyline/keytag/store-text.md` を全面改訂。説明文に**「ほかの貸出台帳アプリとの違い」**を新設。**副カテゴリ（ユーティリティ）を外す** |
> | 審査ノート | 4.3(a) への回答を明記（テンプレート不使用・相違点3つ・確認方法） |
> | **サーバー連携を公開仕様に** | 参照実装 `keytag/server-api/keytag-server.py`（標準ライブラリのみ・1ファイル）＋仕様 `API.md`。**gh-pages に公開して200を実測** → https://shinsei99.github.io/project/keytagnfc-support/api.html |
> | アプリ | 設定画面から仕様ページへのリンクを追加 |
> | ビルド | **build 4 を Archive → アップロード → `VALID` を API で確認** |
>
> ### ★人にしかできないこと → **2026-08-28 11:13 に完了。残り0件**
>
> **オーナーが画面から提出 → `WAITING_FOR_REVIEW` を API で確認済み**（build 4）。
> 提出前に API で全項目を点検してある（ビルドのひも付け・スクショ5枚 `COMPLETE`・説明文・
> 審査ノート・連絡先）。**次にやることは結果待ちだけ**:
>
> ```bash
> python3 appstore_api.py --review com.shinsei99.keytag
> ```
>
> なお **KeyLine 本体（8534）はこのMacの launchd に登録していない**（2026-08-28 オーナー判断で見送り）。
> 再起動で落ちたのではなく最初から入れていなかった。入れるなら `keyline/_launchd/install.sh`。
>
> <details><summary>提出時点で入っていたもの（記録）</summary>
>
> | 入れたもの | |
> |---|---|
> | 名前・サブタイトル・プロモ・キーワード・説明・審査ノート | ✅ API で流し込み済み（`keytag/push-metadata.py`） |
> | 副カテゴリ | ✅ `LIFESTYLE` を外した（主＝ビジネスのみ） |
> | スクリーンショット | ✅ **5枚を撮り直して入れ替え済み**（`keytag/screenshots/`） |
> | ビルド | ✅ build 4 をひも付け → **`PREPARE_FOR_SUBMISSION`** |
>
> スクショはシミュレータから**タップを使わずに**撮った（`keytag/screenshots/shoot.sh`）。
> **Simulator.app のウインドウが0枚で開かない**問題は未解決なので、`simtap.py` が要る
> 別のiOSアプリの作業では引っかかる可能性がある
>
> </details>
>
> ### 判断が要ること
>
> - **デモ動画は作り直さない**と判断した（NFCを見せている今の動画のほうが 4.3 に効くため）。
>   足すなら「サーバー連携で複数人が同じ台帳を見る」場面だが、実機とタグが要る
> - **新しい App 記録は作らない。** 4.3 の直後に似た中身を別記録で出すのは、Apple が
>   スパムとして探しているパターンそのもの（通知文に明記）。未配信なので今の記録のまま全部変えられる
>
> 詳細は `keyline/SESSION_LOG.md` の 2026-08-28 の節。
>
> ---
>
> ## 🌙 引き継ぎ（2026-08-27 メインPC・夜）— にゃんこアイス／スクラップメモ／OCR夜間自動化
>
> **後任が今すぐやることは1つだけ: 明朝、夜間OCRのログを見る。** あとは判断待ちが4件。
> サブPCは `git pull` のみ（機密ファイルの受け渡しは無し。Dropboxの一時置き場も作っていない）。
>
> ```bash
> cd ~ && git pull      # 引き継ぎ本文が TODO.md 先頭に見えていればOK
> ```
>
> ### やったこと（4コミット・すべて push 済み）
>
> | コミット | 内容 |
> |---|---|
> | `91bf5101` | **にゃんこアイス**: 見た目を「アイス屋の店内」に作り替え、書体（Zen Maru Gothic・SIL OFL）と音を追加 |
> | `928ffced` | **スクラップメモ**: 長文ペーストの調査（**原因はコピー側でアプリは無罪**）＋ 完了/キャンセルをヘッダーへ |
> | `c6c05f18` | **AI業務マネージャー**: スキャンPDFのOCRを**毎晩01:00の自動処理**に |
> | `6835ce4a` | 記録の訂正（本日の作業PC名を「サブ」→「メイン」へ） |
>
> ### ★明朝これだけ見てほしい
>
> ```bash
> tail -40 ~/Library/Logs/com.shinsei.chatwork-ai-manager-ocr.log
> ```
>
> **今夜 01:00 が初回。** 末尾に `OCR取込 N / 既取込スキップ N / 認識なし N / 失敗 N` が出る。
> 想定は **OCR 45件前後・1.5時間ほど**（OCRが要る比率が15%の場合）。3時間で打ち切り＝翌晩に続きから再開。
> 止まっていたら疑うのは2つだけ:
> `フォルダにアクセスできません` → Dropboxが読めていない（`/bin/bash` のFDA）／
> `連続失敗 5 件` → claude CLI の定額枠切れ（その晩は諦めて翌晩再開する設計）。
>
> **★この夜間ジョブを止めないこと。** 07:00 の日次リフレッシュは `ocr_fallback` を付けずに呼んでいる＝
> **スキャン書類を索引に入れられるのはこの01:00のジョブだけ**。止めると画像PDFは永久に検索に出ない。
>
> ### ★判断待ち（4件・どれも外部に出る操作なのでオーナー判断）
>
> | # | 内容 |
> |---|---|
> | 1 | **にゃんこアイスの音を一度聴く**。合成音で鳴ることは確認済みだが、**心地よいかは未確認**（こちらでは音を聴けない）。気に入らなければ `www/assets/audio/` に効果音ラボ等のファイルを置いて `USE_FILES=true` にするだけ |
> | 2 | ~~**にゃんこアイス Web版(gh-pages)への反映**~~ → **2026-08-28 に反映済み**（下記） |
> | 3 | ~~**スクラップメモ Web版(gh-pages)への反映**~~ → **2026-08-28 に反映済み**（下記） |
>
> **★GitHub Pages との付き合い方（2026-08-28 オーナー判断・全アプリ共通）**
>
> **公開しているWeb版は、main を push したときに一緒に更新する。**
> 仕組みは `.github/workflows/deploy.yml` の `DEPLOY_FOLDERS`。手で置きに行くと必ずズレる。
> 実際、**`nyanko-ice` と `realestate-calc` は一覧に無かったせいで数か月ぶん古いまま**だった
> （8/28 に公開版とリポジトリを突き合わせて発覚 → 同日、一覧に追加して解消。全6本の一致を実測）。
>
> **そして、フォルダは絶対に消さない。** 同じ場所に **Apple が必須にしているサポートURL・
> プライバシーポリシーURL** が同居している。スクラップメモ／ママカウンター／不動産電卓／
> ネオンブロックの4本は、登録済みの必須URLが **アプリのフォルダの中の `support.html` /
> `privacy.html`** を指している（API で実測）。消すと審査に出せなくなり、問い合わせ先も切れる。
> **デプロイは公開先を `rm -rf` で作り直す**ので、`DEPLOY_FOLDERS` へ足すフォルダには
> この2ページを同梱すること。**新規にApp Storeへ出すアプリは提出前に2ページを作る**
> （にゃんこアイスは両方とも未登録）。
>
> | 公開先 | 取り出し元 | 状態（2026-08-28 実測） |
> |---|---|---|
> | scrapmemo-petapeta / mom-counter / realestate-calc | 同名フォルダ | 一致（自動） |
> | nyanko-ice | **`nyanko-ice/www`**（直下に index.html が無いため） | 一致（自動） |
> | color-gravity / cyborg-defense | 同名フォルダ | 一致（自動ではない・変更が無いだけ） |
> | neon-blocks | — | **ゲーム本体は非公開**。support/privacy だけ置いてある |
> | 4 | ~~**スクラップメモ iOS**~~ → **2026-08-28 に完了。1.0.5 / build 9 を提出済み＝審査待ち** |
>
> ### 知っておくと得なこと（今日ハマって記録に残した分）
>
> - **`kill` や `launchctl bootout` ではOCRが止まらない**。`caffeinate -i python` の形なので親を倒しても
>   python が孤児として残る（実測）。`set -m` ＋ プロセスグループごと停止で解決済み。
>   手で止めるときは `pgrep -f ocr_ingest.py` が空になるまで確認すること
> - **`$rc` の直後に全角括弧を置くと bash が変数名に巻き込む**。`${rc}` と囲うこと
> - **`log()` の `tee` と plist の `StandardOutPath` が同じファイルだとログが二重化する**
> - **Google Fonts は `text=` でサーバ側サブセットできる**（fonttools 不要）。ただしソース全体を
>   走査すると**日本語コメントまで拾って倍近く膨らむ**（92KB→32KB。実測）
> - **`git check-ignore -v` は否定パターン(`!`)に一致しても終了コード0を返す**ことがある。
>   追跡されるかの判定は **`git add --dry-run`** で見ること
> - **ターミナルに「アクセシビリティ」権限が無い**ので、iOSシミュレータへタップや ⌘V を送れない
>   （`simtap.py` が使えない）。必要なら システム設定＞プライバシーとセキュリティ＞アクセシビリティ で許可を。
>   今回は「ページ内に一時計測を差し込んで `fetch('/diag?…')` でローカルHTTPサーバーのログへ送り返す」で迂回した
>
> ### 触っていないもの
>
> - **常駐は私（このセッション）からは1本も再起動していない**。OCRの1本だけ手で登録した
>   （`install-launchd.sh` を丸ごと流すと4本すべて unload→load されるため）。
>   ※ただし**開発エージェントが自分のコミットの後に worker/管理画面を自動で入れ替える**（`dev_restart.py`・設計どおり）
>   ので、PIDは変わっていることがある。実際この晩も dbca9e14 の直後に入れ替わった
> - **別セッションの未コミット作業が残っている**（`jyuusetsu-research` / `baikai-generator` /
>   `company_profile.py` / `registry_parser.py` ほか11件＋未追跡4件）。**私は一切触っていない**ので、
>   そちらの担当が続けること
>
> ---

> ## 💻 サブPCへ引き継ぎ（2026-08-27 メインPC・夜）— **AI重説アシスタントを完成にした**。やることは `git pull` だけ
>
> **機密ファイルの受け渡しはありません**（Dropboxの一時置き場も作っていない）。
> サブPCは常駐0本の役割分担どおりなので、再起動もポート確認も要りません。
>
> ```bash
> cd ~ && git pull
> ```
>
> | | |
> |---|---|
> | 何が変わったか | `jyuusetsu-research`（AI重説アシスタント）が**開発中 → 完成**。メインPCで **8536 常駐・社内LAN共有・Dropbox に `.url` 配布**まで済み |
> | 触ったアプリ | `jyuusetsu-research` ／ `baikai-generator`(8517・自社情報を共通マスタへ) ／ 直下 `registry_parser.py`・`company_profile.py`（共有モジュール） |
> | 検証 | `smoke_test.py` 合格（回帰テストを追加）／8536・8517 とも HTTP 200・画面を目視／**書式の紙面182ページを目視** |
>
> **サブPCで気をつけること**
>
> - **`jyuusetsu-research/run.sh` は `0.0.0.0` になりました。** サブPCでそのまま叩くと社内LANに
>   二重公開されます。動きを見るときは `--server.address 127.0.0.1` を明示して起動してください
> - **常駐に入れない**（`_launchd/com.shinsei.jyuusetsu-research.plist` はメインPC用）
> - サブPCで初めて動かすなら、git に入らない3つが要ります（`jyuusetsu-research/README.md` の表）。
>   `data/format_registry.json` は `scan_formats.py` で作れます（Dropboxの書類雛形を走査・約1〜3分）
>
> **今回いちばん効いた学び（他のアプリでも使える）**
>
> - **Excel の書類は AppleScript で PDF にできる**（`save wb in POSIX file "…" as PDF file format`）。
>   過去メモの「-50 で不可」は構文違いでした。**LibreOffice は入れなくてよい**。
>   画像化まで通す道具は `jyuusetsu-research/print_check.py`
> - **「どの項目がどのセルに入ったか」の突き合わせでは、書面が壊れていることは分からない。**
>   今回それで6件見落としていました（チェック欄の□を値が潰す、見出しセルへ印字、列が1つずれる等）。
>   帳票を出すアプリは**紙面を1ページずつ見る**工程を必ず持たせること
>
> ---
>
> ## 📱 引き継ぎ（2026-08-27 メインPC・夕）— デジタル書斎のアイコンを差し替え、1.0.1 を審査へ出した
>
> **後任がやることは無い。審査の結果を待つだけ。** サブPCは `git pull` のみ（配信用証明書が無いので
> このアプリの提出作業はできない＝メインPC担当のまま）。
>
> | | |
> |---|---|
> | 何をしたか | オーナー支給の画像へアイコンを差し替え → **1.0.1 / build 2** を Archive → アップロード → **審査へ提出**（提出はオーナーが ASC の画面で実施） |
> | いまの状態 | API で **`配信中 1.0 ／ 審査中のもの 1.0.1 … 審査待ち`**。build 2 は `VALID` |
> | 結果の見方 | `python3 appstore_api.py --review com.shinsei.shosai` |
> | 通ったら | `CLAUDE.md` と下の索引表の digital-shosai 行を「**1.0.1 が配信中**」へ書き換える |
> | 注意 | **アイコンが App Store と端末で新しくなるのは配信されてから。** 審査中は旧アイコンのままで正常 |
> | 出し直すとき | **必ず build 3 へ**（`./ios-build-guard.sh digital-shosai --bump`）。Archive〜アップロードは **GUI 不要**で、`digital-shosai/HANDOFF-APPSTORE.md` 冒頭の3コマンドで通る |
>
> **今回分かって記録に残したこと**（次のiOSアプリでも効く）
>
> - **支給されたアイコン画像は白背景に角丸アイコンが乗った形**のことがある。そのまま渡すと
>   iOS の角丸マスクで**角が白く欠ける**。`digital-shosai/icon-src/make_icon.py` が
>   「本体を切り出す → 1024へ → 角の外を地色で塗る」まで面倒を見る（他アプリでも流用可）
> - **Organizer を開かなくても提出用ビルドは上げられる**（`xcodebuild -exportArchive` →
>   `xcrun altool --validate-app` → `--upload-app`）。ただし **`altool` は `--apiKey` にパスを取らず**
>   `~/.appstoreconnect/private_keys/` などしか見ない。このMacの鍵は `~/.appstore/` にあるので
>   **同じものをそこへコピー済み**（新しい鍵は発行していない）
> - **Archive 内のアイコンPNGは PIL で開けない**（Xcode が CgBI 形式に最適化するため）。
>   確認は `xcrun -sdk iphoneos pngcrush -revert-iphone-optimizations` で戻してから
>
> **ついでに直した記載の誤り**: `CLAUDE.md` で「デジタル書斎＝審査待ち」「photo-remake 1.1.0＝審査待ち」
> となっていたが、API で見ると**どちらも既に審査を通っていた**（デジタル書斎 1.0／photo-remake 1.1.0 が配信中）。
>
> ---
>
> ## 💻 サブPCへ引き継ぎ（2026-08-27 メインPC）— 開発エージェントが**完了と同時に常駐を再起動**するようになった
>
> **やることは `git pull` だけです。** サブPCは常駐0本（役割分担どおり）なので、再起動もポート確認も要りません。
> **機密ファイルの受け渡しもなし**（Dropboxの一時置き場は作っていません）。
>
> ```bash
> cd ~ && git pull      # 1a154ae9 が入っていればOK
> ```
>
> ### 何が変わったか
>
> オーナー依頼「開発を実施すると反映に再起動が必要なので、**開発完了と同時に再起動もかけて**ほしい」。
> 開発エージェントは稼働中サービスを触らない設計なので、直しても launchd の常駐は古いコードを
> 抱えたままだった（psa-collection / brain-dump で実際に「pull したのに反映されていない」が起きていた）。
> 新規 `chatwork-ai-manager/services/dev_restart.py` が、完了報告と同時に**触ったアプリの常駐だけ**を
> 入れ替える。結果は完了報告の末尾に「🔄 反映（常駐の再起動）」として LINE/Chatwork に届く。
>
> | | |
> |---|---|
> | 対象の決め方 | `project_dir` ＋ **そのタスクのコミットが触ったトップレベルフォルダ**（着手時のHEADを `dev_task_events` に残して `git diff base..HEAD`） |
> | 触らないもの | 定時ジョブ（`kickstart` はその場でジョブを走らせる＝note の自動投稿が余計に出る）／未ロードのラベル／ngrok／Workspace直下 |
> | ビルド | Next.js(`next start`)・Vite(`vite preview`) は再起動前に `npm run build`（配信物がビルド済みのため） |
> | 再起動の仕方 | plist が変わっていれば `bootout`→`bootstrap`、そうでなければ `kickstart -k`。その後ポートへHTTPして起動を確認 |
> | worker 自身 | 完了報告を送ってから、切り離した子プロセスで再起動（先に落とすと報告が届かない） |
> | 設定 | `dev_restart_enabled` / `dev_restart_wait_sec` / `dev_restart_build_timeout_sec` / `dev_restart_exclude`（管理画面「システム設定」にもある） |
>
> 実測: psa-collection `kickstart` 2.4秒・`bootout→bootstrap` 3.1秒（8527 HTTP 200）／
> brain-dump は `npm run build` 込みで 29.5秒（3002 HTTP 200）。
> **メインPCの worker(40332→86160) と管理画面(8540) は再起動済み＝本番反映済み。**
>
> ### サブPCで知っておくとよいこと
>
> - **サブPCには launchd 常駐が無いので、開発が終わっても再起動対象がありません**（＝報告に「🔄 反映」は
>   出ない）。これは異常ではなく役割分担どおり。動作を見たいならメインPCで見ること
> - **`dev_tasks` にテスト用の行を作らないこと。** メインPCでは常駐 worker が即座に拾って
>   **本物の開発エージェント（`--dangerously-skip-permissions`）が起動します**（今回それをやって
>   30秒で `kill -9`。実害なし）。試すなら `dev_agent_enabled=0` にするか `dev_restart` の関数を直接呼ぶ
> - **はまり所は `chatwork-ai-manager/README.md` に書いてあります**（`CLAUDE.md` / `TODO.md` /
>   `SESSION_LOG.md` は識別子を含むため gitignore ＝ サブPCには渡りません）。要点だけ:
>   `kickstart -k` は plist を読み直さない／`ps -o command=,ppid=` は command を最後に置かないと切られる／
>   `com.shinsei.note-daily.plist` は XMLコメント内の `--login` のせいで `plistlib` が落ちる（`plutil` で読み直す）
> - **残っている確認は1件だけ（メイン担当）**: 本物の LINE/Chatwork 依頼で、完了報告の末尾に
>   「🔄 反映」が付き実際に入れ替わっているか。次に開発を頼んだときに見る
>
> ## 🖥 メインPCで受領・実施した（2026-08-27 メインPC）— サブPCへの返信：**①③③-b は完了**
>
> **サブPCの引き継ぎ（下のブロック）のうち、機械でできるものはすべて完了。** git は受領時点で同期済み
> （HEAD `22627b95`・未コミット0・stash0・push漏れ0）だったので pull は不要だった。
>
> | 依頼 | 結果 |
> |---|---|
> | ★① 本体サイトを本番へ | ✅ `./publish.sh site` → READY。**公開41本すべてが本番にある**ことを1本ずつHTTPで確認、`/novel` も200。※`pokecard-dex`/`psa-collection`/`swim-tracker` の404は **`visibility: "internal"` の意図的な非公開**でデプロイ漏れではない |
> | ★③ note 自動投稿の常駐 | ✅ `com.shinsei.note-daily`（毎日22:35）を設置。`--login` 実施 → `~/.note-profile` 作成 → **`--check` が「OK（エディタを開けている）」**。**今夜22:35に1本目** |
> | ★③-b 自動執筆の常駐 | ✅ **週次 → 日次に変更**（オーナー判断）。`com.shinsei.daily-write`（**毎日22:45**）。**初回は今夜** |
> | `.pii-blocklist.txt`（③-bの前提） | ✅ メインPCで作成。`parking-map/serve.py` と `gyomu-manual/業務マニュアル.html` から拾い直し **会社11・物件28・地番1・人29＝69語**。`guard` で**85本すべて通過＝誤検知0**。**ありふれた姓（佐藤・中村・鈴木・青木）は意図的に外した**（技術記事で誤爆すると記事が理由なく止まるため）。**サブPCの実物51件とは中身が一致していない**ので、厳密に揃えるならファイルを手で運ぶ |
> | ★④ note の下書き2本の削除 | ✅ オーナー削除済み（サブPCの SESSION_LOG に記載あり） |
>
> **★② note 22本の出し方は「③の自動投稿」で決着**（プレミアム加入はしない）。
> 22:35に1本ずつ、`drafts/zenn_order.txt` の順で **9/17まで**。
>
> ### ★3媒体を毎日1本ずつ、全部自動にした（2026-08-27 メインPC・**夜にまとめた**）
>
> ```
> 22:30  Zenn が自動公開（Zenn側＝GitHub連携。pushさえ済んでいればMacが寝ていても出る）
> 22:35  note-daily   … 同じ記事を note へ投稿
> 22:45  daily-write  … ①ネタを拾う ②1本書く（本体JSON＋Zenn原稿＋note原稿）
>                       ③guard ④予約＋zenn_order.txt へ追記 ④-b links ⑤本番反映
> ```
>
> **朝と夜の2回だったのを夜1回に寄せた。** Macが起きていないといけない窓が1つで済み、
> その晩に出た Zenn / note のURLをそのまま `links` に入れて本番へ出せる。
> **朝には寄せられない**（Zennの公開時刻22:30は一度きりで変更できず、noteはその後）。
>
> | 期間 | 中身 |
> |---|---|
> | 8/27〜9/20（25日） | サブPCが書いた25本の予約。**何があっても出る** |
> | 9/21〜 | **毎晩の daily-write が約25日先の予約を1本ずつ作り足す**。前もって存在しない |
>
> **★人の手で見てほしいこと（明朝これだけ）**
>
> ```bash
> # ★2026-08-28: ログを /tmp から ~/Library/Logs へ移した（再起動で消えていたため）
> tail -40 ~/Library/Logs/com.shinsei.note-daily.log    # 22:35 note が1本出たか
> tail -60 ~/Library/Logs/com.shinsei.daily-write.log   # 22:45 記事が1本増えて予約が入ったか
> tail -40 ~/Library/Logs/com.shinsei.zenn-daily.log    # 22:00 Zenn へ push できたか
> cd ~/ai-tools-base && ./publish.sh status
> ```
>
> - **両方ともMacがスリープ・ログアウト中は動かない**（noteは画面ありのChrome、執筆は `claude -p`）
> - `daily-write` が止まった日は**9/21以降の予約が1日ぶん抜ける**（既存の25本は無事）
> - ログに `★guard で止まった` が出ていたら、その晩は**わざと何も出していない**。原稿を見て直す
> - **`~/Library/LaunchAgents/com.shinsei.weekly-write.plist` を消してください。**
>   ロード解除済みで動かないが、紛らわしい（こちらでは `rm` が許可されず消せなかった）
>
> **★サブPCでは絶対に常駐させない。** `note-daily` の投稿済み記録は端末ごと（gitignore）なので
> 2台で動かすと同じ記事が2回出る。`daily-write` も2台だと同じ朝に2本書いてgitでぶつかる。
>
> 詳細は `ai-tools-base/SESSION_LOG.md` の 2026-08-27（メインPC）の節。
>
> ---
>
> ## 🖥 メインPCでやること（2026-08-27 サブPCより）— AIツールベース：在庫を全部書いた。あとは本番反映と note ※上のとおり実施済み
>
> **サブPCで、ネタ帳の在庫66本を26本の記事に束ねて全部書き上げた。**
> Zenn は予約投稿まで済んでいるので**メインPCですることは無い**。残っているのは次の2つ。
>
> | | 状態 |
> |---|---|
> | 本体サイト（AIツールベース） | 制作記録 14→**40本**（不動産33）。**★本番へ未デプロイ** |
> | Zenn | **22本の予約投稿を push 済み。2026-08-27〜09-17 の毎日22:30に1本ずつ自動公開**（触らなくてよい） |
> | note | 原稿33本（公開済み11・**未投稿22**）。1本目だけ下書きで入れてある（未公開） |
>
> ### ★① 本体サイトを本番へ出す（先にこれ）
>
> ```bash
> cd ~/ai-tools-base && ./publish.sh site      # npm run validate → npx vercel --prod --scope brain-dump
> ```
>
> **いま `https://ai-tools-base.vercel.app/works/kana-name-matching` も `/novel` も 404**（未デプロイ）。
> **Zenn の1本目が 8/27 22:30 に出る**ので、その前に出しておきたい。
> **サブPCからでもデプロイできる**（2026-08-22に実証済み）。`--scope brain-dump` が無いと `Not authorized`。
>
> ### ★② note 22本をどう出すか（判断が要る）
>
> **note の予約投稿は「プレミアム」（月500円・加入月無料）**だった。無料のままでは日時指定ができない。
>
> - **案A（推奨）**: プレミアムに加入する。**22本＝22日ぶんなので、加入月の無料お試しで終わる**。
>   加入すれば、Zenn と同じ日の **22:35**（Zennの5分後。note本文にZennのURLを埋めてあるため）で
>   22本すべてに日時を振れる
> - **案B**: 毎晩ひと言もらって1本ずつ投稿する（1本あたり数分）
> - **案C**: launchd で毎晩 Claude を起動して自動投稿 — **Chrome拡張がその起動から繋がるかは未検証**。
>   Macがスリープしていると動かない
> - **Claude のセッション内 cron は使えない**（7日で失効・セッションを閉じると消える。22日間はもたない）
>
> note を1本出したら、**URLを `content/works/<slug>.json` の `links` に追記 → `./publish.sh site`**。
> `npm run validate` の転載⚠️が消えれば完了。
>
> ### ★③ note の自動投稿を、今夜から動かす（**メインPCで常駐**・午前中の作業は5分）
>
> **`claude -p`（非対話）からは claude-in-chrome の拡張が使えない**ので（2026-08-27 実測）、
> Playwright で自前の投稿スクリプトを書いた。**headless では note のエディタが描画されない**
> （`editor.note.com/new` で止まり body が空）ため、**画面ありで動かし、ウィンドウを画面の外**
> （`--window-position=-3000,-3000`）に出す。作業中に画面が飛び出すことはない。
>
> ```bash
> cd ~ && git pull
> cd ~/ai-tools-base
> PY=$HOME/agent-platform/.venv/bin/python
>
> # 1. 初回ログイン（画面が出る。note にログインすると自動で検知して閉じる。最大10分待つ）
> $PY scripts/note_post.py --login
>
> # 2. 確認（画面外にウィンドウが出て数秒で終わる）
> $PY scripts/note_post.py --check          # → 「OK（エディタを開けている）」なら成立
>
> # 3. 常駐に入れる（毎晩22:35＝Zennの5分後）
> cp _launchd/com.shinsei.note-daily.plist ~/Library/LaunchAgents/
> launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.shinsei.note-daily.plist
> launchctl print gui/$(id -u)/com.shinsei.note-daily | grep -A3 StartCalendar
> ```
>
> **今夜22:35に1本目**（`deploy-not-reflected`＝「直したはずのものが、直っていなかった」）。
> 以降は `drafts/zenn_order.txt` の順で1日1本、**22本＝9/17まで**。
> ログは `~/Library/Logs/com.shinsei.note-daily.log`（※2026-08-28に `/tmp` から移した）、進み具合は `./publish.sh status`。
>
> **★守ること**
> - **サブPCの launchd には入れない。** 投稿済みの記録（`drafts/.note_posted.json`）は端末ごとなので、
>   2台で動かすと**同じ記事が2本出る**。常駐は**メインPCだけ**
> - **画面ありで動く**ので、Macがスリープ・ログアウトしていると動かない可能性がある。
>   **翌朝いちど `~/Library/Logs/com.shinsei.note-daily.log`（※2026-08-28に `/tmp` から移した） と `./publish.sh status` を見る**こと
> - ログインが切れたら `--check` が NG になり、その晩は投稿されない（`--login` をやり直す）
>
> ### ★③-b 記事を書くところまで自動にした（同じくメインPCで常駐）
>
> **週に一度、新しいネタから1本書いて、公開の予約まで入る。** 人が見なくても回るが、
> **外へ出る前に機械の関門を通す**（人の目の代わり）。
>
> ```
> 日曜 8:07（launchd）
>   ① ./publish.sh scan          … 各アプリの SESSION_LOG から、前回以降のネタを拾う
>   ② claude -p が1本書く         … 根拠のコミット/コードを開いて裏を取る。
>                                   **確認できない数値は書かない**（metric を空にする）
>   ③ ./publish.sh guard         … ★個人情報・固有名詞・寿命を縮める語を止める
>   ④ ./publish.sh zenn-schedule … 通ったものだけ予約に入る（埋まっている日は飛ばす）
> ```
>
> **③で落ちれば、その週は何も出ない。** ログは `/tmp/weekly-write.log`。
>
> **入れ方（メインPCで。note-daily と一緒に）**
>
> ```bash
> cp _launchd/com.shinsei.weekly-write.plist ~/Library/LaunchAgents/
> launchctl bootstrap gui/$(id -u) ~/Library/LaunchAgents/com.shinsei.weekly-write.plist
> ```
>
> **★先に `drafts/.pii-blocklist.txt` が要る。** サブPCで作ってあるが **gitignore なので
> メインPCには渡らない**。メインPCでは `drafts/.pii-blocklist.txt.example` を写して、
> 同じものを作ること（会社・物件・地番は example に無いので、サブPCから手で運ぶか、
> `parking-map/serve.py` と `gyomu-manual/generate.py` から拾い直す）。
>
> **試運転の結果（2026-08-27・サブPC）**: 1本書けた（`excel-calendar-theme-color`＝
> 休業日カレンダーのExcelをテーマ色から読む話）。実測値が確認できなかったので
> **metric は全部空**のまま出てきた＝指示どおりの挙動。**9/20 22:30 に予約済み**。
> あわせて2つの不具合を直した: 予約日が既存と重なっていた／`zenn_order.txt` に無い記事が
> note だけ永久に出ない状態だった。
>
> ### ★④ note の下書きを2本消してほしい（こちらでは消せない）
>
> 検証で **同じ内容の下書きが2本** できている（どちらも「直したはずのものが、直っていなかった」）。
> **データの削除はこちらでは行わない決まり**なので、note の記事一覧から消してください。
>
> - https://note.com/notes → 下書き2本（2026-08-27 06:52 と 2026-08-26 09:00）
>
> 消さなくても自動投稿には影響しない（毎回あたらしく作るため）。ただし今夜1本目が公開されると
> **同じ内容が下書きとして2本残る**ので、整理しておくほうがよい。
>
> ### 使い方が増えた道具（両PCで使える）
>
> - `./publish.sh queue` … ネタ帳の在庫と3媒体の進み具合、次に書く候補（在庫は `drafts/NETA.md` 一本のまま）
> - `./publish.sh zenn-schedule` … 未公開の記事に毎日22:30の `published_at` を振る（**既定はドライラン**）。
>   ★**Zenn の公開日時は一度きりで変更できない**ので、公開済み・予約済みには触らない作りにしてある
> - `./publish.sh status` … 予約中を「未反映」と誤表示しないよう直した（`scripts/zenn_status.py`）
> - `./novel_readthrough.py 20` … 小説続編の通し読みを作る（直下）
>
> ### ついでに入れたもの
>
> **小説（続編）への導線をサイト全体に置いた**。`/novel`（作品紹介＋どの記録が何話になったかの対応表）、
> 制作記録の詳細に「この記録は、小説の題材になっています」、フッターは全ページ。
> **続編はカクヨム未投稿なので、いまの行き先は `/novel`。投稿したら `content/novel.json` の `url` を
> 入れるだけ**で、フッターも詳細ページも作品ページへ直接飛ぶ（前作は本作と関連しないので載せていない）。
>
> 詳しくは `ai-tools-base/SESSION_LOG.md` の 2026-08-27 の節。
> ## 💻 サブPCへ引き継ぎ（2026-08-27 メインPC）— KeyTagNFC は**再提出まで完了**。やることは `git pull` だけ
>
> **KeyTagNFC の差し戻し対応は終わりました。以下は報告で、依頼はありません。**
> **機密ファイルの受け渡しもなし**（Dropboxの一時置き場は作っていない）。
>
> ```bash
> cd ~ && git pull
> ```
>
> | 結果 | |
> |---|---|
> | 審査 | **`WAITING_FOR_REVIEW`（build 3）**。`python3 appstore_api.py --review com.shinsei99.keytag` で追える |
> | デモ動画 | **公開済み** https://shinsei99.github.io/project/keytagnfc-support/ （1分59秒・720p/H.264・20MB・英語字幕＋日本語ナレーション） |
> | NFCの実機確認 | **済み**。動画の中でタグへの書き込みと読み取りが成功している＝8/26のNFC修正が実機で効いている証拠 |
>
> **★訂正: 「メインPCに ffmpeg が無い」は誤りでした。** `agent-platform/.venv` の
> **imageio-ffmpeg 同梱バイナリ（ffmpeg 7.1・libass/libfreetype/libx264 入り）**があり、
> 日本語字幕の焼き込みまでできます。**サブPCへ動画を渡す必要はありませんでした。**
> 今後、動画処理はどちらのPCでもできると考えてください。
>
> **サブPCで知っておくとよいこと**（詳細は `keyline/SESSION_LOG.md` の 2026-08-27）
>
> - 動画を作り直す手順は **`keyline/keytag/build-demo-video.py`**（git入り）。ただし
>   **元動画 `~/Pictures/GX010219.MP4`（628MB）はメインPCにしかない**（git管理外）
> - **GoPro HERO9 は MTP 接続なので `/Volumes` にマウントされない**。取り込みはイメージキャプチャ.app。
>   **`GX`＝本編 / `GL`＝低解像度プロキシ**で、番号が最大のものを選ぶ（カードの日付は壊れていて当てにならない）
> - **`.gitignore` は行末コメントに対応していない**（`#` は行頭のみ）。今回それでパターンが
>   無効になっていた。追加したら **`git check-ignore -v <path>` で必ず確かめること**
>
> ## 🖥 メインPCで受領・実施した（2026-08-26 メインPC）— サブPCへの返信
>
> **サブPCの44コミットを受領。機械でできるものはすべて完了。残りは人にしかできない1件（KeyTagの動画撮影）。**
> 手元にも未pushの3コミット（chatwork-ai-manager・8/25〜8/26 の開発エージェント分）があったので、
> **fast-forward ではなくマージ**（`77cd1b0f`）。競合は無し。
>
> | 依頼 | 結果 |
> |---|---|
> | ポケカ図鑑「🖨 並べる」の反映 | ✅ `git pull` ＋ `com.shinsei.psa-collection` を kickstart → **8527 HTTP 200 / 127.0.0.1 待受**。`Pillow 11.3.0` と `card_sheet` の import も実測（`ModuleNotFoundError: PIL` は起きない）。**PDFの実ダウンロードだけ未確認**（下記） |
> | ブレインダンプ（iOS録音の修正） | ✅ **申し送りと違い、メインPCでは launchd 常駐だった**（`next start -p 3002`＝ビルド済みを配信）。`npm run build` → kickstart → **3002 HTTP 200**、`.next` に `wakeLock` が入っていることまで確認。**pull だけでは反映されない**ので注意（詳細 `brain-dump/SESSION_LOG.md`） |
> | 8/25 23:47「✅ 完了しました。」の中身 | ✅ **`dev_tasks` id=38（TASK-20260825-009・日報検索/集計画面）**。JST 23:35着手→23:47完了。コミット `c4cdcdf9` としてメインPCにローカルで残っていた＝**設計どおり push されていなかった**ので、今回まとめて push した |
> | KeyTag の審査状態 | ✅ `appstore_api.py --review com.shinsei99.keytag` → **リジェクト（要対応）**。サブPCの報告どおり |
> | pull で変わった常駐アプリ | ✅ 洗い出し済み。**再起動が要るのは psa-collection と brain-dump の2本だけ**（他は ai-tools-base=Vercel / articles=Zenn / photo-remake=iOS / keyline=ログのみ） |
>
> **★フォトリメイク 1.1.0 / build 4 を App Store へ提出した（2026-08-26 メインPC・完了）**
>
> サブPCが用意した図形13種の版。`ios-build-guard.sh` で衝突なしを確認 → Archive → アップロード
> （build 4 が `VALID`）→ オーナーが画面で審査へ提出 → **`WAITING_FOR_REVIEW` を API で確認**。
> **配布証明書はこのMacのキーチェーンに無くクラウド署名**（＝異常ではない。CLIからの手順込みで
> `photo-remake/SESSION_LOG.md` に記録）。**実機確認は未消化のまま出したので、審査中に TestFlight で
> 触ること**（不具合が出たら build 5 で出し直し）。担当PC＝メイン。
>
> **★人の手が要る（残り）**
>
> 1. ~~**KeyTagNFC のデモ動画撮影と Resolution Center への返信**~~
>    → **2026-08-27 に完了。build 3 で再提出し `WAITING_FOR_REVIEW`。動画も公開済み。**
>    （※ここに書いていた「メインPCに ffmpeg が無い」は**誤り**。`agent-platform/.venv` の
>    imageio-ffmpeg 同梱バイナリ 7.1 が使え、字幕の焼き込みまでメインPCで完結した）
>    - **2026-08-26: 実機に入れる準備は完了。**TestFlight の内部テストグループ「社内テスト」を作り、
>      `s.washimi@icloud.com` を招待済み（`INVITED`）。**審査に出した build 2 そのもの**が入る。
>      iPhone で App Store から **TestFlight** を入れ → 招待メールの `View in TestFlight` →
>      **KeyTagNFC** をインストール。（詳細 `keyline/keytag/RELEASE.md`・`keyline/SESSION_LOG.md`）
> 2. ~~**ポケカ図鑑「🖨 並べる」で PDF が実際に落ちること**の目視~~ →
>    **2026-08-26 オーナーがメインPCで確認済み＝完了。この件はこれで終わり。**
>
> **★気づいたこと（オーナー判断待ち・急がない）**
>
> - **`dev_tasks` に `WAITING_USER` が2件たまっている**（id=39 空き待ち系TODOの除外／
>   id=41 管理物件台帳の担当者列。41は 8/26 03:44 UTC＝JST 12:44 でまだ返事待ち）
> - **3002（ブレインダンプ）が `*:3002`＝社内LANに出ている**。分類は「ツール（社内共有なし）」なので
>   規則上は `127.0.0.1` のはず。plist が `-H 0.0.0.0`。落とすなら**引数変更＝`bootout`→`bootstrap`**
>   （`kickstart -k` では反映されない）

> ## 🖥 メインPCでやること（2026-08-26 サブPCより）— KeyTagNFC の審査差し戻し（デモ動画の提出）
>
> **App Store から差し戻された。やることは「動画を撮って、リンクを添えて返信」だけ。**
> **新しいビルドは要らない**（build 2 のまま。`ios-build-guard.sh` も不要）。
>
> | 項目 | 値 |
> |---|---|
> | Submission ID | `a47e6a37-ee0d-48dc-8a35-2559cb1b976b` |
> | レビュー日 | 2026-08-25（**レビュー機 iPad Air 11-inch (M3)**） |
> | 対象 | 1.0 (2) |
> | 指摘 | **Guideline 2.1 - Information Needed**（NFC機能のデモ動画を出せ） |
> | ASC上のstate | **`REJECTED`**（2026-08-26 に `python3 appstore_api.py --review com.shinsei99.keytag` で確認） |
>
> **原因はアプリの不具合ではない（ほぼ確定）**: レビュー機の **iPad には NFC リーダーが無い**。
> KeyTag は **iPhone 専用で提出している**（`keytag/setup-ios.sh` に `TARGETED_DEVICE_FAMILY = 1`）が、
> iPhone専用アプリは iPad の**互換モード**で起動できるため、審査員はそれで確認して
> 「NFCが動かない＝動画で見せろ」となった。**設定ミスではないので直さない。**
> むしろ**返信文に「iPhone専用であり iPad は対応端末ではない」と書くほうが効く**。
>
> ### ★撮る前の1分チェック（ここで初めてNFCを実機で動かす）
>
> **アプリのNFC機能は一度も実機で動かしていない。NFCタグ（NTAG213）は手元にある**（8/26確認）。
>
> - アプリ起動 → 右上が「**NFC 利用可**」になるか
> - 「タグを読み取る」で iOS の読み取りシートが出るか
>
> ~~ここで失敗したら、原因は Developer Portal の App ID に「NFC Tag Reading」が無いこと~~
> → **2026-08-26 に API で確認したところ `NFC_TAG_READING` は有効だった**（`keyline/keytag/RELEASE.md`）。
> **ここが原因ではない**ので、失敗したら別の原因を疑うこと。
> この撮影がそのまま RELEASE.md の「実機が手に入ったら確認すること」の消化になる。
>
> ### 撮影（2台使う・一発撮り・2〜3分）
>
> - ①アプリを動かす **iPhone実機**、②それを撮る**カメラ役**（別端末）。**画面収録は不可**
> - **iPhoneの画面と、手に持ったタグが同時に1本の動画に映っていること**（＝今回の指摘の核心）
> - 720p で撮る（4K/1080pだと数百MBになり GitHub が拒否する）
>
> 1. **ホーム画面**を映す → KeyTag をタップして起動（＝実機で現行版が動く証明。必ず入れる）
> 2. 右上の「**NFC 利用可**」を2〜3秒はっきり映す
> 3. **まっさらな NTAG213 を手でカメラに見せる**（1〜2秒。"designated hardware" を映す）
> 4. **【初回ペアリング】「タグに書き込む」**→ 入力 → 実行 → 読み取りシート → タグを当てる → **成功表示**
> 5. **「タグを読み取る」**→ 同じタグをかざす → 内容が画面に出る
> 6. **鍵を登録**（ダミー。例「デモ鍵A」／貸出先「テスト太郎」）
> 7. **貸出**: タグをかざす → 貸出中になる
> 8. **返却**: もう一度かざす → 返却済みになる
> 9. **台帳（履歴）**を開き、いまの貸出・返却が残っているのを見せる
> 10. （任意・効く）**設定 > サーバー連携**を開き、**未設定のまま**であることを見せる
>     ＝「サーバー無しで端末内だけで全機能が動く」＝前回の審査ノートで主張した点の裏付け
>
> ### 個人情報の映り込み（★publicに出るので必ず守る）
>
> アプリ側は**ダミーだけ**なので問題ない（サーバー連携を繋がない限り KeyLine 本体の
> 社員名・鍵番号・免許証画像には一切つながらない）。危ないのは **iPhone 側**:
>
> 1. **撮影前に集中モード（おやすみモード）をON** … 通知バナーの本文・送信者名が動画に残る
> 2. **ホーム画面の映り込み** … 他アプリのアイコン・通知バッジ・壁紙（家族写真等）。
>    KeyTagだけのページを用意するか無地の壁紙にする
> 3. **サーバー連携は最後まで設定しない**（手順10の主張と個人情報対策を兼ねる）
>
> ### 動画の置き場（オーナー案: サポートページ）
>
> **既存のサポートページ `keytagnfc-support/`（gh-pages）に置く案**が第一候補。
> リンクが自分の管理下で、期限切れも共有設定ミスも起きない。
> URL: https://shinsei99.github.io/project/keytagnfc-support/
>
> - ただし **`shinsei99/project` は public**。動画は誰でも落とせるし、
>   **force pushしない方針（2026-08-18本人決定）なので履歴から実質消せない**。ダミーで撮る前提なら可
> - **50MB以下**に収める（GitHubは50MBで警告・100MBで拒否）。**mp4(H.264)** にする
>   （iPhone標準の `.mov`/HEVC はブラウザによって再生できない）
> - 代案: **YouTube 限定公開(Unlisted)**（"非公開(Private)"にしない）／Dropbox共有リンク
>   （「リンクを知っている全員が閲覧可」に変更する）。**審査が終わるまでリンクを消さない**
>
> ### サブPCでできること（渡してくれれば）
>
> 撮った `.mov` をサブPCへ渡せば、**フレームを切り出して条件を満たすか・映り込みが無いかを確認**し、
> **720p / H.264 / 音声なしの mp4 へ変換**して、サポートページへの掲載案まで作れる
> （`imageio-ffmpeg` を 2026-08-26 にサブPCへ導入済み。brew は汚していない）。
> メインPCで完結させるなら、変換だけ別途 ffmpeg が要る。
>
> ### 提出（★外部への操作なのでオーナーの手で）
>
> 1. 動画をアップし、ブラウザの**シークレットウィンドウで再生できるか確認**
> 2. App Store Connect → **App Review Information > Notes に動画URL**を書く
> 3. **Resolution Center で返信**（英文ドラフトは `keyline/SESSION_LOG.md` の 2026-08-26 の節。
>    「iPhone専用で提出しており iPad は対応端末ではない」を必ず入れる）
> 4. 返信後の状態は `python3 appstore_api.py --review com.shinsei99.keytag` で追える
>
> ### ついでに（別件・今日メインPCで見てほしい）
>
> - **8/25 23:47 に「✅ 完了しました。」の開発通知が出た分の中身**。開発エージェントは
>   設計上 **`git push` をしない**（`chatwork-ai-manager/services/dev_runner.py`）ので、
>   **メインPCにローカルコミットとして残っているはず**。`./dev-doctor.py --sync --fetch` と
>   `git log origin/main..main --oneline` で確認できる。通知本文は
>   `dev_tasks` から引ける（**created_at は UTC**。JST 23:47 = `14:47`）

> ## 🖥 メインPCでやること（2026-08-25 サブPCより）— ブレインダンプの音声入力
>
> **やることは `git pull` だけ。** 常駐していない（launchd 未登録・Vercel 運用）ので
> 再起動もポート確認も要らない。機密ファイルの受け渡しも**なし**。
>
> ```bash
> cd ~ && git pull
> ```
>
> **何が直ったか**: iPhone Safari で1分ほど録音して止めると「録音が空でした」になり
> 文字起こしされなかった不具合。**サブPCで直し、Vercel 本番へ反映済み・オーナーが実機で確認済み**
> （https://brain-dump-sable-one.vercel.app ）。**メインPC側の作業は残っていない。**
>
> **原因は iOS 固有の2点**（Mac の Chrome では再現しないので、次に触るときは実機で確かめること）
> 1. iOS Safari は `ondataavailable` が `onstop` より後に届くことがあり、
>    timeslice なしで `start()` すると停止時点の chunk が0個 → `blob.size === 0`
> 2. iPhone の自動ロックで画面が消えるとマイクのトラックが止まり、録音がそこで切れる
>
> 対処は `rec.start(1000)` の逐次回収／chunk を最大3秒待ってから Blob 化／Screen Wake Lock／
> トラック `onended` で自動停止。詳細と実測値は `brain-dump/README.md` にある。
> **サイズ超過でもタイムアウトでもなかった**（59秒=250KB・API 9秒）ことは実測で否定済み。

> ## 🖥 メインPCでやること（2026-08-25 サブPCより）— ポケカ図鑑の「🖨 並べる」
>
> **完了（2026-08-26 メインPC）。** 反映（pull＋psa-collection の kickstart）も、PDFが落ちることの目視も済み。
> 内容は 2026-08-25 の時点で push 済み・記述と実装の一致も確認済み
> （カード 72.45×101.20mm・カード間4mm・`app.py` への差し込み3か所・`Pillow>=10.0` 明記）。
>
> **やることは2つだけ。`git pull` と、PSAカード管理(8527)の入れ替え。**
> 機密ファイルの受け渡しは**なし**（コードだけ。Dropboxの一時置き場も作っていない）。
>
> ```bash
> cd ~ && git pull
> launchctl kickstart -k gui/$(id -u)/com.shinsei.psa-collection   # ← これを忘れると反映されない
> curl -s -o /dev/null -w "%{http_code}\n" http://127.0.0.1:8527   # 200 を確認
> ```
>
> **なぜ kickstart が要るか**: PSA管理は `pokecard-dex/app.py` を**モジュールとして読み込んで**
> 画面を出しているので、図鑑側を直しても**常駐プロセスは古いコードを持ったまま**。
> 今回は引数を変えていないので `kickstart -k` で足りる（plist を触ったときだけ bootout→bootstrap）。
> 8531 は常駐させない方針のままなので、単独で見たいときだけ `cd pokecard-dex && ./run.sh`。
>
> **入ったもの**: 図鑑の一覧・詳細から選んだ**4枚を A4 1ページのPDF**にする「🖨 並べる」タブ
> （`カードモデル.docx` を手で差し替えていた作業の置き換え）。
> カード1枚 **72.45×101.20mm**（実物 63×88mm の1.15倍・**4枚とも同一サイズ**）、**カード間4mm**。
> 実装は `pokecard-dex/card_sheet.py`（新規）＋ `app.py` に3か所差し込み。
>
> **依存**: `Pillow` を `pokecard-dex/requirements.txt` に**明記した**（今までは streamlit の
> 依存で入っていただけ）。PSA管理の venv には既に Pillow がある想定だが、**もし
> `ModuleNotFoundError: PIL` が出たら** `psa-collection/.venv/bin/pip install Pillow`。
>
> **メインPCで見ておいてほしいこと**（サブPCでは実機確認済み・`.see/0825-215839` / `0825-220142`）
> 1. 8527 →「📖 ポケモンカード図鑑」→ 適当に検索 → 「🖨 並べる」を4回押す
> 2. タブ「🖨 並べる」でプレビューが出て、**「📄 PDFをダウンロード」で実際にPDFが落ちること**
>    （サブPCは headless ブラウザで検証したため、**落ちたファイルの確認だけができていない**。
>    PDFの中身は同じ関数を直接呼んで生成し目視済み）
>
> **未実施（オーナーの指示待ち・急がない）**
> - ワンピースカード図鑑(8537)にも同じ「🖨 並べる」を付ける（`card_sheet.py` は図鑑非依存なので差し込みだけ）
> - PSA管理の**保有カード（PSAスラブ画像）**からも選べるようにする

> ## 🖥 メインPCで受領・実施した（2026-08-24 メインPC）— サブPCへの返信
>
> **サブPCの5日ぶん（111コミット）を受領。A-0〜A-3 のうち、機械でできるものはすべて完了。**
> 残っているのは**人にしかできない3件**（下の「★人の手が要る」）。
>
> | 依頼 | 結果 |
> |---|---|
> | `git pull` | ✅ 111コミットを fast-forward（未コミット0・stash0で開始） |
> | A-1 `secrets-sync.sh import` | ✅ **7件取り込み → 不足0件**（google-maps / japanpost / estat / appstore＋`.p8` / config/company_profile.json / mail-archiver）。実体のバイト数まで確認 |
> | A-1 置き場の削除 | ✅ **`apps-secrets-handoff/`(1.0M) を削除済み**。tar内27ファイルすべてが手元に実体としてあることを**1件ずつ確認してから**消した |
> | A-2 secrets.toml のSMTP5行 | ✅ 追記済み（`smtp_host/port/user/password_keychain/from`）。**パスワードはキーチェーン参照**にして平文を置いていない |
> | A-3 常駐の入れ替え | ✅ **14本を kickstart**（worker / line / 管理画面8540 / maisoku-converter / realestate-valuation / business-plan-generator / tokuyaku-generator / baikai-generator / kaitori-dm-maker / restoration-calculator / settlement-creator / memorandum-generator / shorui-cabinet / agent-platform）。全PIDが入れ替わり、**12本とも HTTP 200**・待ち受けも規定どおり（8526のみ127.0.0.1、他は0.0.0.0） |
> | 8/24追記の 8517 再起動 | ✅ 上に含めて実施。`*:8517` で待ち受け確認 |
>
> **★A-0 の順序を1つ間違えた（復旧済み）**: `soufu-generator/senders.json` の退避より先に pull し、
> 雛形（会社3件）で上書きした。**旧版は git 履歴（`13a3ab39`）に残っていたので
> `senders.local.json` へ復元済み**（4件・大京/新誠/京橋/個人）。アプリはこちらを優先して読むので
> 画面の見え方は元どおり。**サブPC側の対応は不要。**
>
> **★人の手が要る（メインPCに残っている3件・私からは実行できない）**
>
> 1. **SMTPパスワードをキーチェーンへ**（未登録を確認済み）
>    `security add-generic-password -s chatwork-ai-manager-smtp -a shin@daikyocorp.co.jp -w`
> 2. **`/bin/bash` にフルディスクアクセス**（システム設定＞プライバシーとセキュリティ）。
>    無いと業務日報の保管も休業日判定も失敗する
> 3. **LINE ライトプランへの変更 — まだ枠は0のまま。** 再起動直後の worker ログが実測を出した:
>    `[worker] line quota: 残り0通 / 200通（消費200）` → 管理者通知の push も 429 で失敗。
>    **プラン変更まで LINE は無反応のまま**（Chatworkは正常）
>
> **新コードが効いていることの確認**: 再起動した worker が起動直後に**残通数の日次見張り**を実行し、
> 上の quota ログを出した＝8/20 の LINE 改修が本番に入っている（8/19 09:00 起動の旧 worker には無い機能）。
>
> **★A-2 の「`/bin/bash` にフルディスクアクセス」は、既に付いていた**（2026-08-24 実測）。
> 一時的な LaunchAgent から `/bin/bash` を起動して確かめたところ、業務日報が使う
> **会社Dropbox『社内・総務』と GoogleDrive の両方を読めた**（確認用の plist は撤去済み）。
> `shorui-cabinet` のエラーログにあった `PermissionError` は **8/12 の古いもの**で、
> 8528 を実際に開き直しても新しいエラーは出ない。**サブPCの引き継ぎ書の「未」は解消済み。**
>
> **C-2 ワンピースカード図鑑（8537）は完了**（2026-08-24 メインPC）。`./setup.sh` を通しで実行し、
> **画像 4,962/4,962枚（100%）・失敗0・`check_dex.py` の8項目すべて ✅**・`data/` 1.1GB。
> `./run.sh` で **127.0.0.1:8537・HTTP 200** を確認し、**拡張パック23件の画像が出ることまで画面で見た**
> （`.see/0824-122032-onepiece-packs.png`）。**launchd には登録していない**（常駐させるかは人の判断）。
>
> **★休業日判定がメインPCで効いていなかった（2026-08-24に発見・修正済み）。**
> DBの `holiday_schedule_path` が **サブPCから見た Google Drive のパス**
> （`…/その他のパソコン/マイ Mac mini/Desktop/ルーティーン/…`）を指していて、
> メインPCにはそのパスが**存在しない**。`is_holiday()` は読めないとき False を返す
> （＝通常運転）ので、**エラーも出ないまま休業日にも催促が飛ぶ状態**だった。`holidays` テーブルも0件。
> → **このPCの実体 `/Users/apple/Desktop/ルーティーン/年間休暇スケジュール2026.xlsx` に付け替え、
> `refresh()` で106日をDBへ取り込んだ**（8/23(日)=休業日 / 8/24(月)=営業日 / 8/29(土)=休業日 を確認）。
> 設定は実行時に読むので worker の再起動は不要。**サブPC側の設定値は触っていない**（DBは各PC別）。
>
> **★LINEのライトプランは反映済み**（2026-08-24）。API実測で `limit 5000 / used 212 / remaining 4788`、
> 枠切れフラグも解除。worker のログでも `[line] push ok` に戻った。
>
> **★18:30の業務日報（初日 8/24）の見込み**: Chatworkへの自動アップは**通る**
> （`daily_report_enabled=1` / `time=18:30` / `upload=1`、今日は営業日、保管先へ
> **launchd の /bin/bash から書き込めることを実測済み**）。アップ先は `daily_report_room_id` も
> `manager_room_id` も空のため**監視中のgroupルーム＝「大京商事」(349546270)** にフォールバックする。
> **メールも通るようになった（2026-08-24 12:5x）**: `mailer.missing()` が **0件**・`is_configured()=True`。
> **SMTP認証を実測で確認**（`smtp.daikyocorp.co.jp:587` → EHLO 250 → STARTTLS → login 成功。**送信はしていない**）。
> **launchd 常駐からキーチェーンを読めることも probe で確認済み**（ここが読めないと18:30に静かに失敗する）。
> 設定は送信時に読むので worker の再起動は不要。
>
> **★キーチェーン登録の注意（はまった点）**: Claude Code の `!` から
> `security add-generic-password … -w` を叩くと、**対話的な入力待ちが効かず空パスワードで登録される**
> （`retype` まで素通りする。実際に文字数0で登録された）。**ターミナル.app を別に開いて叩くこと**。
> `osascript -e 'tell application "Terminal" to do script "…"'` で入力待ちのウインドウを出せる。
> なお macOS 26 では「キーチェーンアクセス」は Launchpad に出ず
> `/System/Library/CoreServices/Applications/Keychain Access.app` にある（新しい「パスワード」アプリには
> 今回のような generic 項目は表示されない）。
>
> **まだ手を付けていないもの**: C の目視4件
> （AI重説の印刷イメージ・デジタル書斎の実機・KeyTagのNFC実機・業務日報の初日18:30）。

> ## 🖥 メインPCでやること — 追記（2026-08-24 サブPCより／AI重説の謄本まわり）※上のとおり実施済み
>
> **`git pull` のあと、下の1コマンドだけ実行してください。** 下の 8/23 ぶん（A〜F）はそのまま有効です。
>
> ```bash
> launchctl kickstart -k gui/$(id -u)/com.shinsei.baikai-generator   # 媒介契約書ジェネレーター(8517)
> lsof -nP -iTCP:8517 -sTCP:LISTEN     # *:8517 で待ち受けていること
> ```
>
> 直下の共有モジュール **`registry_parser.py`（謄本パーサ）を直した**ため。**8517 も同じ1本を読む**が、
> Streamlit はスクリプトを再実行しても **import 済みモジュールを入れ替えない**ので、
> 再起動しないと常駐プロセスは古いパーサのまま動き続ける（8/23 の ③ と同じ理由）。
>
> **直した中身**: 区分所有で**本体（居宅）と車庫の謄本が別々**に出るとき、複数枚を後勝ちで
> 1件に潰していたため**車庫の 16.38 ㎡ が本体の 72.59 ㎡ を上書き**していた（渡す順で結果が変わる）。
> あわせて、AI解析が落ちたときの正規表現フォールバックが**謄本の罫線を値として拾っていた**のも修正。
> `baikai-generator/smoke_test.py` は合格＝**コードの互換性は確認済み。再起動だけが必要**。
>
> **AI重説（8536）は常駐していないので再起動不要。** 詳細は `HANDOFF.md` の 2026-08-24 の節。
> メインPCに残っている作業は従来どおり**4書式の印刷イメージの目視**（サブPCではセル一致まで確認済み）。

> ## 🖥 メインPCでやること — 引き継ぎまとめ（2026-08-23 サブPCより／8/19以降の4日ぶん）
>
> **メインPCの最終作業は 2026-08-19。以降サブPCで 8/20・8/21・8/22・8/23 の4日ぶんが進み、すべて push 済み**
> （HEAD `c00c3a9` 2026-08-23・8/20以降 **41コミット**。サブPC側は未コミット・未push・stash とも 0 件を
> `./dev-doctor.py --sync --fetch` で確認済み。**コードは `git pull` だけで全部そろう**）。
>
> **上から順にやる。A だけで本番は正しくなる（30〜40分）。**
> 各項目の詳細は下の日別ブロックと各アプリの README にある。
>
> ---
>
> ### A. 本番に効かせる（最優先・これをしないと直したものが1つも動いていない）
>
> **A-0. コードを取る**
>
> **★ `git pull` の前に1つだけ**（送付書ジェネレーターの差出人マスタを守るため）:
>
> ```bash
> cd ~/soufu-generator && cp senders.json senders.local.json    # 実データを退避（gitignore側）
> cd ~ && git pull
> ```
>
> 2026-08-23 に、`senders.json` から**自宅住所と携帯番号を外した**（公開リポジトリに入っていたため）。
> pull すると会社3件だけの雛形に上書きされるので、**先に控えを作る**。控えた側（`senders.local.json`）を
> アプリが優先して読むので、画面の見え方は今までと変わらない。
>
> **A-1. APIの資格情報を受け取る（先にやる。これが無いと新しいToolが動かない）**
>
> 8/19以降に取ったキーがメインPCに1件も渡っていない。**個人Dropboxに置いてある**:
>
> ```
> Dropbox-個人/apps-secrets-handoff/apps-secrets-appurunoMacBook-Air.tar （996K・34項目・2026-08-23 22:37）
> ```
>
> ```bash
> cd ~ && ./secrets-sync.sh check     # 何が無いかを見る
> ./secrets-sync.sh import            # 取り込む（既にある物は上書きしない）
> ./secrets-sync.sh check             # 「不足 0 件」を確認
> ```
>
> 新しく渡るもの: `.env.google-maps`（Web/Server/**Embed** の3キー）／`.env.japanpost`（本番）／
> `.env.estat`（**8/23からAI業務マネージャーの商圏統計Toolでも使う**）／`.env.appstore` ＋ `.appstore/AuthKey_*.p8`（**再発行不可**）／`mail-archiver/.env.mail-archiver`／
> **`config/company_profile.json`（8/23新規。自社の宅建業者・宅建士12欄。public リポジトリに値を置かない決まりなので、これが無いとAI重説の自社欄が空になる）**。
> ほかに既存19件も入っているが上書きされない。
>
> ※ 8/21 に置いた古い tar（`apps-secrets-MacBookAir.tar`・33項目）は、**中身が新しい tar に全部含まれることを
> 突き合わせて確認したうえで削除済み**。置き場に残っているのは上の1本だけ。
>
> **★不足0件を確認したら `apps-secrets-handoff/` を置き場ごと消す**（機密を同期フォルダに残さない決まり）。
>
> **A-2. 人の手が要る設定を2つ入れる（コードでは解決しない。A-3の再起動より前に）**
>
> 1. **日報メールのSMTP**（`chatwork-ai-manager` の機密は `secrets-sync` では運ばれない）
>    ```bash
>    security add-generic-password -s chatwork-ai-manager-smtp -a shin@daikyocorp.co.jp -w
>    ```
>    ＋ `chatwork-ai-manager/.streamlit/secrets.toml` に5行（`secrets.toml.example` からコピー）。
>    ホスト・ポート・暗号化は実測確定済み（`smtp.daikyocorp.co.jp` / **587のみ** / STARTTLS / 上限30MB）。
>    **サブPCで認証・実送信まで確認済み**（2026-08-21・自分宛）。入れるまでは送らず「設定が足りない」を
>    日報の結果に記録して管理者へ通知する（黙って止まらない）。
> 2. **`/bin/bash` にフルディスクアクセス**（システム設定＞プライバシーとセキュリティ）。
>    launchd 常駐は CloudStorage を読み書きできない。無いと**業務日報の保管も休業日判定も失敗する**:
>    保管先 Dropbox『社内・総務/業務日報』／休暇表 GoogleDrive『ルーティーン/年間休暇スケジュール2026.xlsx』。
>    （`shorui-cabinet` 8528 で同じ対処を実施済み [[reference_launchd_cloudstorage_fda]]）
>
> **A-3. 常駐を入れ替える**
>
> ```bash
> # ① AI業務マネージャー（LINE無反応の修正・業務日報・Tool追加・休業日判定）
> launchctl kickstart -k gui/$(id -u)/com.shinsei.chatwork-ai-manager-worker
> launchctl kickstart -k gui/$(id -u)/com.shinsei.chatwork-ai-manager-line
>
> # ② マイソクコンバーター（貼った画像が横1.2倍に伸びる不具合。社内LAN共有あり＝最優先）
> launchctl kickstart -k gui/$(id -u)/com.shinsei.maisoku-converter
> lsof -nP -iTCP:8505 -sTCP:LISTEN   # *:8505 で待ち受けていること
>
> # ③ 8/23の横断改修（http_compat・pdf_orient一本化・API追加）が入った常駐アプリ
> for l in realestate-valuation business-plan-generator tokuyaku-generator baikai-generator \
>          kaitori-dm-maker restoration-calculator settlement-creator memorandum-generator \
>          shorui-cabinet agent-platform; do
>   launchctl kickstart -k gui/$(id -u)/com.shinsei.$l
> done
> ```
>
> `services/line_client.py` は worker と line_webhook の**両方**が読むので、片方だけでは不足。
> ngrok（`-ngrok`）は触らなくてよい。
>
> **③ を忘れると、直下の共有モジュール（`http_compat.py` / `pdf_orient.py` / `japanpost_api.py` /
> `area_stats.py` / `law_citations.py`）を差し替えたのに、常駐プロセスは起動時に読み込んだ古いモジュールを
> 抱えたまま動き続ける**（Streamlit はスクリプトを再実行しても import 済みモジュールは入れ替えない）。
> 8/23 に追加した機能（住所⇄〒の照合・根拠条文の照合・商圏データ）は、再起動して初めて画面に出る。
> `business-plan-generator` と `agent-platform` は上の installer に無いラベルなので、
> `launchctl list | grep shinsei` で実際のラベルを確かめてから叩く。
>
> **worker は 8/19 09:00 起動のまま＝3日ぶんの修正が1つも本番に入っていない。** 溜まっているもの:
>
> 1. TASK-20260819-002（QAが未実行のTODO更新を「反映しました」と嘘をつく不具合）
> 2. TASK-20260819-003（QAのTODO一覧回答を定時確認と同じ担当者グループ化に統一）
> 3. **LINE無反応の再発防止**（失敗の握りつぶし修正・Chatworkへフォールバック通知・残通数の日次見張り）
> 4. **業務日報**（毎日18:30の自動処理。**サブPCに worker は常駐していないのでメインPCでしか動かない**）
> 5. **AIのToolを4系統追加**（法令の現行条文＝e-Gov／郵便番号と住所の照合＝日本郵便／
>    ストリートビューのリンク＝Google Maps／**商圏の政府統計＝e-Stat**（8/23追加。人口・世帯・
>    高齢化率・転入超過・将来推計／空き家率・借家率・共同住宅率・着工新設貸家。区どうしの比較も可））。
>    法令はキー不要、**郵便番号・ストリートビュー・商圏統計は A-1 が前提**（`.env.estat` も要る）。
>    ストリートビューは**リンクを返すだけ**（SV画像をAIに読ませない・印刷しないの線引きは README に記載）。
>    `{"property":"○○"}` の形は物件マスタDBがサブPCに無く試せていない → **メインPCで一度確認すること**
> 6. **休業日は定時確認を送らない**（8/22 の `314782e`。年間休暇スケジュール上の休業日 8/22（土）に
>    18:00の定時確認が飛んだ件。休業日判定が業務日報にしか入っていなかった）。
>    **★8/23（日）も休業日。再起動するまで日曜・休業日にも催促が飛び続ける**
>
> マイソクコンバーター(8505)は**社内LAN共有あり**＝入れ替えないと、貼った画像が横1.2倍に伸びる
> 修正前のコードが社員に出続ける。
>
> 手順・確認コマンド・本番に入る中身の一覧は `chatwork-ai-manager/README.md` 先頭
> 「★ メインPCで最初にやること」に一本化してある（SESSION_LOG/TODO は gitignore で届かないため）。
>
> ---
>
> ### B. 人にしかできない外部手続き
>
> - **LINE のライトプラン（月5,000通・¥5,000税別）への変更。** 無料枠200通は4日で枯れる
>   （実測 1日約50通）。**未実施なら枠は0のまま＝LINEは無反応のまま。Safari では支払い画面に
>   進めない**ので別ブラウザで（README に記録済み）
> - 法人番号Web-API は申請完了済み（**発行見込み 2026-09-04〜09-21**）。届いたら `API_STATUS.md` を更新
>
> ### C. メインPC限定の作業（配布証明書・実機・常駐がこちらにしかない）
>
> - ~~**App Store の審査結果を3本確認**~~ → **2026-08-24 に API で再確認済み（前回8/23から変化なし）**
>   （`python3 appstore_api.py --review`。**メインPCでなくても分かる**）。
>   結果: **デジタル書斎 1.0/build1（8/19提出・5日経過）と KeyTag 1.0/build2（8/17提出・7日経過）は
>   どちらも WAITING_FOR_REVIEW＝まだ審査に入っていない**（ビルドは両方 VALID で受理済み）／
>   **にゃんこのアイス屋さんは「申請中」ではなく未提出**（1.0が提出準備中・ビルド0件）。
>   配信中は スクラップメモ1.0.4 / ネオンブロック1.0.3 / フォトリメイク1.0.2 /
>   水泳記録1.0 / 電卓1.0 / ママカウンター1.0 の6本（**ママカウンターを「v1.0.1」と書いていたのは
>   誤りで、実際の配信は 1.0/build4**。CLAUDE.md を訂正した）。
>   デジタル書斎・KeyTag は結果が出るまで待ち。もう一度見るときは上のコマンドを叩く
> - **AI重説(8536)で4書式の印刷イメージを目で見る**（サブPCではセル一致まで確認済み・紙面は未了）。
>   起動は **`cd ~/jyuusetsu-research && ./run.sh`**（8/23新設。venv・書式レジストリ作成・
>   `127.0.0.1:8536` まで面倒を見る）。**A-1 の `config/company_profile.json` が入っていること**が前提
>   （無いと1枚目の宅建業者・宅建士12欄が空で出る）。書式200本の実体は Dropbox『契約・書類/書類雛形』
>   にあり、`scan_formats.py` が走査する。※README に `--server.port 8512` と書いてあったのは誤りで、
>   **8512 は THETAパノラマの常駐ポート**（叩くと衝突する）。8/23 に README ごと直した
> - **デジタル書斎を実機で1度通す**（手順 `digital-shosai/HANDOFF-APPSTORE.md`）
> - **KeyTag は NFCタグ到着後に実機検証**（NFC機能は一度も実機で動かしていない）
> - **業務日報の初日を見届ける**（**次の営業日 8/24（月）18:30**。8/23（日）は休業日で動かない）。
>   テストはサブPCで 8/21 に完了。18:30 は**承認を挟まず Excel が Chatwork へ上がる**
>   （オーナーの明示指示。止めるなら設定 `daily_report_upload=0`）。残る未確認は、サブPCのDBに
>   tasks が3件しか無く**「本日動いたTODO／未完了TODO」欄だけ実データで動かせていない**という1点
> - `mail-archiver`(8535) をメインPCで常駐させるなら A-2 の `/bin/bash` FDA が前提
>
> ### C-2. ワンピースカード図鑑（onepiece-dex・8537）を使えるようにする ← 2026-08-23 追加
>
> **サブPCで新規に作って完成させた。メインPCにはまだ実体（データ）が無い。**
> コードは git で渡るが、**`data/`（画像4,962枚＝約1.2GB）は git 管理外**なので作り直す。
>
> ```bash
> cd ~/onepiece-dex && ./setup.sh     # ← これ1本。40〜70分（ほぼ画像取得の時間）
> ./run.sh                            # http://127.0.0.1:8537
> ```
>
> 途中で止めても叩き直せば続きから走る（冪等）。常駐させたい場合だけ、そのあと
> `bash ~/_launchd/install-launchd.sh`（APPS に登録済み）→ `lsof -nP -iTCP:8537 -sTCP:LISTEN`
> で **`127.0.0.1:8537`** であることを確認する。**社内には配らない**（ポケカ図鑑と同じ扱い）。
>
> 詳しくは `HANDOFF.md` の先頭と `onepiece-dex/README.md`。
>
> ### D. 余力があれば
>
> - **小説の続編『不動産屋、つくってます。』は 2026-08-22 に脱稿**（全32話）。**カクヨムには未投稿で、
>   作品ページもまだ作っていない**。原稿は GoogleDrive『新誠プロパティ/新誠不動産/続編_カクヨム用/』
>   （**git管理外**なので両PCとも Drive から読む）。
>   **2026-08-23（サブPC）: 外部評論をもとに第1話の見直しを完了**。あわせて全32話の表記を統一し、
>   **管理料を一戸四千円→二千円に変更**（実務の是正。連動して月八千円・時給176円・話タイトル2本を改題）。
>   **バックアップは作らない**（`_backup/` は削除済み。戻すなら Drive のゴミ箱＝30日）。
>   **2026-08-24（サブPC）: 外部評論をもとに第2話の見直しを完了**（3,738字→4,330字）。直した中身は
>   ①市役所で登記事項→道路台帳へ（登記は前夜に登記情報提供サービス・一通332円）②「前の入居者が
>   住んでいる」→「まだ入居中」③一〇二も一〇一も一階なので階下の天井に染みは出ない→床下伝いで
>   隣室の押し入れ下が濡れる形に④排水漏れなのに給水メーターのパイロットが回っていた→止まっている
>   （漏水の切り分けを描写）⑤内見が土曜午後なのに後段で「土曜の午前」と衝突→午前に統一
>   ⑥立ち会い40分＋徒歩12分で「十時に事務所」は無理→十時十分⑦時給176円を最低賃金比較ではなく
>   「これでは、人を雇えない」という経営者の内面へ⑧一か月を数行のモンタージュで見せて45時間20分に
>   裏づけ⑨コラムの締めを実用記事調から小説（丸と四角）へ返す。
>   **2026-08-24（サブPC）: 第3話も見直した**（2,941字→3,206字）。①「五分の一」が何と何の比か
>   曖昧だった→打ち直し90分に対し、機械20分＋突き合わせ15分＝35分＝**半分以下**に訂正（枚数も
>   二十七枚に統一）②「四十二戸ぶんに増えれば四十二倍」がもう42戸持っていると読める→「いま手元に
>   あるのは四戸」を明示③（人がやることじゃない）→（……人が、時間を使うことじゃない）＝作品の
>   主題に寄せた④20分で27枚できた手応えを静かに一段追加（時計・暖簾・机の上の紙）⑤コラムに
>   **徒歩分数の起点の改正**（2022年9月からマンション・アパートは建物の出入口が起点。それ以前の
>   資料は敷地の出入口で測っている）を追記。80m＝1分・端数切り上げは現行で正しいことを公的解説で確認済み。
>   あわせて `00_作品情報.txt` の「一戸あたりの時間は三分の一を切った」を**十分の一以下**に訂正
>   （26話の実数は 11時間20分→55分）。タグはDX・AIを末尾へ回し、経営小説として置いた。
>   **管理戸数の時系列は本文どうしで矛盾なし**（42戸のうち丸: 2話4→5話5→9話9→16・17話14→22話21→
>   25・26話30→終章40。別枠の総管理戸数は 開業4→半年19→一年33→16話62→22話70超）。
>   **2026-08-24（サブPC・3回目）: 評論の総評を受けて1〜3話を通しで補強**。①**開業資金と月々の固定費を1話に入れた**
>   （手元二百五十万／毎月三十二万＝事務所家賃9・リースと回線2・国保と年金5・協会ほか1＋生活費／
>   **八か月には少し足りない**）。**家族構成は伏せる方針**なので「それに、生活がある」とだけ書き、
>   内訳は出さない（15万は伏せた差額）。2話の末尾で「出32万・入8千・残218万・八か月→七か月」と受ける
>   ②**摩擦を1話早めた**。1話の誠一に「そら、無理やと思うで」（送り出しながら失敗を見込む本音）を足し、
>   3話の末尾に同業からの「お一人でされてるんですか。専任の宅建士さんは」を5行入れて4話の赤西へ渡す
>   ③**1話の前半を圧縮**（城山を3段落→1段落、経歴の重複、コラムと逐語で重なる説明を削除）。
>   鍵の束の登場位置は本文の63%→58%。**さらに前へ出すには構成の入れ替えが要るので未着手**
>   ④**温度の起伏を2箇所だけ**（2話＝176円のあとに声を出して笑い電卓を伏せる／3話＝初めて通ったときの「あ」）。
>   字数は 1話5,504→5,647・2話4,264→4,360・3話3,206→3,376。
>   ⑤**3話に「なぜ打ち直すのか」が無かったので追記**（オーナーの指摘）。売る部屋がない（管理4戸・
>   募集に出せるのは二〇一だけ）／当座の食いぶちは客付けの手数料／**届いた紙には元付けの社名・電話・
>   担当者名が刷ってあるので、そのままでは客に渡せない＝自社の名前で出すために打ち直す**、を本文3段落と
>   コラム1段落で立てた（3,376→3,821字）。**マイソクを打ち直す本当の理由はここ**なので、以降の話でも外さない。
>   **2026-08-24（サブPC・4回目）: 第4話「同業という客」を見直した**（3,126→3,868字）。
>   ①**赤西が来た理由が舞台の外にあった**（「宮岡んとこから聞いた」だけ）→3話で立てた「元付けの社名を
>   消して自社名で出す」の当然の帰結として、**赤西が自分の流した紙のヒラク様式版を机に出す**場面を追加。
>   久美は「**広告可の紙だけです。不可と書いてあるものは打ち直していません**」と答える（実務の裏づけ。
>   紙が赤西に届いた経路も、客が営業に見せた、と本文で示した）②**3話末に置いた「専任の宅建士さんは」の
>   橋が回収されていなかった**→赤西に「専任の宅建士も、あんたか」と言わせ、「うちは一人です」の論拠に繋げた
>   ③**渡すものの実体がなかった**→Ａ４一枚の手順書＋機械への言いつけを写した紙、**機械代は赤西が直接契約**
>   （「うちが止まったときに、そちらまで止まる」＝作品の主題と同じ理由）④「知らん」と言われた直後に
>   久美が「一枚三百円で月三万」と単価を出していた→「指で弾くと、百枚は下らない」を挟んだ
>   ⑤冒頭の赤西不動産の経歴を6文→4文に圧縮（1話と同じ「経歴書」対策）⑥コラムに「渡すときは何を渡すのか
>   （手順書・設定・連絡先まで含めて渡したことになる）」を1段落追加＝31話の多田の独立に繋がる。
>   **3話の実務ミスを1点訂正（オーナー指摘）**: 「徒歩八分」と「約六五〇メートル」を並べていたが、
>   80m＝1分・端数切り上げなら**徒歩八分は561〜640m**で、650mは切り上げて九分になる→**六三〇メートル**に修正。
>   （8話の「五分なのに九百メートル」は、わざと食い違わせた例なので正しい）
>   あわせて3話の「作った図面を、二度、読み返した」を「それを、二度、読み返した」へ（直前が「募集図面を、
>   二十分で作った」で重複していた。**「資料」には置き換えない**――この話は「資料＝他社から来た元の紙／
>   図面＝自社が作るもの」で語を使い分けており、同じ文の後半が「元の資料と突き合わせて」なので意味が濁る）。
>   **2026-08-24（サブPC・5回目）: 4話の外部評論を受けて1点だけ直した**。「半日で、彼女は覚えて帰った」→
>   **「一通りの操作を覚えた／帰る前にマイソクを十枚、自分で処理した／十枚とも久美の突き合わせを通った」**
>   （教育→実務適用まで見せる。評論§11）。**§12（赤西に「家まで持って帰らせとるとは思わなんだ」を足す）は
>   評論自身が「今の『三年、知らんかった』のほうが強い」と結論しているので入れない。**
>   **評論の伏線2件を全32話で検証した**:
>   ①**§8「三パーセント」は23話「無料という土俵」で完全に回収済み**（大手が三％＋アプリ＋24時間コールセンターを
>   すべて無料で提示→久美が「五→三％で月8.2万足りない。埋めるには戸数か手間。増やすと谷中の九戸が載らない」と
>   詰める。コラムは「値下げ競争の帰結として、いちばん助けが要る物件が最初に切られる」）。**触らない**
>   ②**§6「仕組みを安く売っている」は本当に抜けている**。全32話を検索したが、久美が安売りを指摘される場面も
>   値付けを考え直す場面も**一度もない**（16話の銀行「処分できないものは担保になりません」が唯一、仕組みの
>   値打ちを外から測る場面だが値付けには繋がらない）。このままだと「善人だから安い」で終わる。
>   **入れる場所は 23話（本命・大手が同じものを無料で配る＝安く売るの意味が反転）→16話（半日三万円で売った
>   ものが一千二百万円の理由になっている）→31話（多田への対価が金額でなく「直せるようになること」）の順。
>   5話から順に見直していく途中で当該話に着いたときに入れる**（先に手を入れると前後の温度が合わない）
>   ③評論§9の**「道具を入れるかどうかの判断基準＝それによって誰の時間が戻るのか」は作品の核**として、
>   通し推敲のときに各話が外していないか点検する。§10の**コラムの型（本編で事件→コラムで一般化）は維持**、
>   本文の言い換えだけになった回は書き直す。
>   **2026-08-24（サブPC・6回目）: 第5話「電線の向こう」を外部評論で見直した**（3,022→3,112字）。
>   ①**§6 久美の基準を法的基準に見せない**——「見に来れば分かることは消してもいい」のあとに
>   **「この線が法に照らして正しいかどうかまでは断言できなかった。宅建業の免許は持っていても広告の専門家では
>   ない。だから会社の決まりとしては、もう一段きつくした。迷ったものは、消さない」**を追加
>   ②**§8 四十枚に意味を持たせた**（「多すぎるくらい撮った。あとになって、撮っていない、と気づくほうが、
>   よほど怖い」）③**§9 パノラマの出自**を一行——「新誠にいた最後の年、遠方の買主のために一度だけ試して、
>   それきりになっていたやり方」＝**十七年の経験の上に道具を載せている**という人物像の補強
>   ④**§12 コラムを法務・実務の補足に絞った**。本文と逐語で重なる2段落（電線は三秒で分かる／ひび・傾きは
>   隠蔽）と最後の思想の段落を削り、**景表法＋「不動産の表示に関する公正競争規約」／加工は一律禁止ではなく
>   問われるのは現況を誤認させたかどうか／同じ電線でも判断が変わるので一律の答えはない／社内で決めておく三点
>   （範囲・迷ったら消さない・加工した旨を書くか）**に置き換えた。
>   **§7（電線は建物にかかったぶんだけ消す・電柱と道路上の電線は残す）は既に本文にあるので触っていない。
>   §10（村瀬の「子どもにも」→「どうぞ」）も評論の推奨どおり無改変。**
>   ⑤**既知の齟齬を解消**: 13話が5話の訪問を「五月の午後」と回想していたのを**「七月の午後」**へ。
>   （2話＝六月、5話は「六月には十一時間二十分かかっていた」と前月を振り返るので七月、7話が八月の終わり）
>   **2026-08-24（サブPC・7回目）: 第6話「同じ箱の中に」を外部評論で見直した**（2,674→2,876字）。
>   ①**§6 技術的な言い過ぎを訂正**——「機械は、どれが免許証で、どれが外壁の写真かを、区別しない」は事実に
>   反する（画像認識は区別できる）→**「必ずしも区別してくれるわけではない。区別できる道具もある。ただ、
>   どの道具も、それを外に出してはいけない、という責任のほうは負わない」**へ。締めの「負っているのは、その紙を
>   渡してきた人の顔を、知っている側だけである」は残した（作品の思想）。本文中の「機械は、それが免許証だとは
>   判断しない」も**その場の道具に限定**（「その道具は、それが免許証かどうかを見ていない」）
>   ②**§11 フォルダを作り直す場面を膨らませた**（今回が「自分だけが分かっていればいい仕事」→「誰がやっても
>   事故らない仕事」への移行点）——「一人でやっているうちは、覚えておけば済む。……覚えている人間の頭の中に
>   しかない境目は、その人が休んだ日に消える」＋**（境目は、人ではなく、箱に置く）**。後の組織化の伏線
>   ③**§9・§10 コラムを圧縮し、法律解説から実務設計へ寄せた**。「不動産業は、個人情報の塊を扱う仕事である」の
>   教科書的な入りを削り、**個人情報保護法の求めを3点（利用目的を具体的に決めて知らせる／目的外に使わない／
>   安全管理）＋家賃保証会社への提供は第三者提供にあたり申込書に同意欄がある**、と精度を上げたうえで、
>   実務設計を**箱を分ける／道具から見える範囲を狭める／いつ捨てるかを決める**の3点に整理した
>   （「いつ捨てるか」は新規追加。決めていないものは永久に残る）。
>   **§8（「下がっているが、丸は、まだ六つである」）は評論が高く評価しているので無改変。**
>   **2026-08-24（サブPC）: 数字の齟齬を2件解消（オーナー指摘）**。
>   ①**6話の二〇一号室の家賃 五万八千円 → 四万円**。1話が「四戸とも、家賃は四万円ほど。その五パーセント」＝
>   管理料二千円と決めているので、五万八千円では二千九百円になる（**管理料を四千円→二千円に下げたときの
>   直し漏れ**）。**吉岡の四戸の家賃は四万円**が正。19話の一〇一号室 五八、〇〇〇円は藤木の桜宮なので別物
>   ②**部屋番号の衝突**——2話の二〇二号室（吉岡・滞納）と 21・22・32話の二〇二号室（谷中・七十九歳の女性）が
>   別の家主の別建物なのに同番だった → **谷中側を「二階の角（の部屋）」に改めた**。
>   26話の二〇二（給湯器の排気口・物置を三十センチ動かす）は吉岡の物件なので、そのままで正しい。
>   **2026-08-24（サブPC・8回目）: 第7話「調べるという仕事」を外部評論で見直した**（2,908→3,212字）。
>   ①**§16 用語の混同を訂正**——「重要事項説明の調査」→**「重要事項説明のための調査」**（久美がやっているのは
>   重説書を作るための調査。コラム冒頭は元から正しかった）②**§7 冒頭の調査項目を圧縮**（権利関係・法令・
>   インフラ・管理・災害を4ブロックの箇条書き調から2段落へ。詳細はコラム側にあるので本文で重ねない）
>   ③**§8 聞く先の断定を汎用化**——「役所の窓口が三つ、水道局が一つ、ガス会社が一つ」は物件・地域で変わる→
>   「役所、上下水道、ガス、管理会社、売主。地図を開けば済むものも、窓口まで行かないと出てこないものも、
>   電話で人に聞くしかないものもある」④**§10「出典があるから正しい、ではない」の一場面を追加**——機械が
>   用途地域を出典つきで返したが**いつ時点かが書かれていない**（頼んだのに落ちた＝道具は指示どおりとは限らない）→
>   久美が市のページを自分で開いて日付を確かめ、出典欄に書き足す。**6話の「境目」と7話の「出典」が繋がる**
>   ⑤**§9 コラムの断定を修正**——「一は、渡せる。二は、渡せない」→**「一つめは、機械に渡せる。二つめは、
>   機械だけには渡せない。聞いた答えを整理させることはできるが、聞くことと、返ってきた答えを疑うことは、
>   人が引き受けるしかない」**。あわせて見出しが段落末尾に貼り付いていた体裁を直し、公開情報には
>   「いつ時点か」の但し書きが付くことを追記。
>   **§14（「書いてないことは、持ってこない」「速くなったのは、探すほうだけ」）と§5（その月、丸は一つも
>   増えなかった）は評論が核と評価しているので無改変。**
>   **§13 の注意（今後は「ＡＩにやらせたら失敗した」ではなく「ＡＩにやらせる範囲を間違えた」を描く。例外の
>   扱いまで設計して初めて道具になる）は、第二部の各話を見直すときの観点として持っておく。**
>   **2026-08-24（サブPC・9回目）: 第8話「鵜呑みという事故」を外部評論で見直した**（3,043→3,198字）。
>   評論はほぼ全面肯定で、直しは**§8の2点だけ**。
>   ①**プロパンの金額の断定をやめた**——「冬の光熱費が、月に三千円から五千円変わる」→
>   **「プロパンは、供給する会社が自分で単価を決めるの。同じ使い方でも、都市ガスより高くつくことが多い。
>   冬の給湯だと、月に数千円違ってくることもある」**（＝自由料金であることが根拠。金額は建物ごとに違う）。
>   コラムにも**設備費を料金に上乗せしている場合があること／どれだけ違うかは供給会社による**を追記し、
>   あわせて壁芯・内法を「マンションの登記は内法、広告は壁芯」と具体化
>   ②**徒歩分数の言い方を規約に合わせた**——「駅からの分数は、八十メートルにつき一分で表示する決まり」→
>   **「駅からの表示は、徒歩の所要時間を、道路距離八十メートルにつき一分として算出する決まり（端数は切り上げ）」**。
>   規約が定めているのは分数そのものではなく所要時間の算出方法なので。
>   **§10（「またか。うちの子ばっかり」→「教えるほうです。今度は、私が教わります」）は無改変**
>   （4話で渡した相手から学ぶ＝知識が一方向に流れなくなった場面。評論のいちばんの推し）。
>   **★公開前にまとめてファクトチェックする箇所（評論の指摘。最終稿の必須作業）**:
>   3話コラム（80m=1分・端数切り上げ／2022年9月からの起点＝建物の出入口）／5話コラム（景品表示法と
>   不動産の表示に関する公正競争規約・加工は一律禁止ではない）／6話コラム（個人情報保護法の三点＋保証会社への
>   提供は第三者提供）／7話コラム（重説は宅建士が記名した書面または電磁的方法で説明）／8話コラム（宅建業法の
>   不告知・不実告知の禁止／プロパンは自由料金／壁芯と内法）。
>   **次は第9話「紙の在り処」から。**
>   **2026-08-24: 実話ネタ対応表を作った**（`続編_カクヨム用/00_実話ネタ対応表.md`）。
>   `ai-tools-base/drafts/NETA.md` の実話66本と各話を突き合わせ、**どの話にどの実話を足せるか**を表にした
>   （縛りも同じ表に明記＝技術の話にしない／ＡＩの語を増やさない／数字は移し替える／1話に多くて1つ）。
>   **★10話と17話が構造的に同型**（黙って止まり、記録が無く、後から気づく）で、**10話が山の予告に
>   なってしまう**ため、**10話の事件を差し替える案を3つ用意した。オーナーの判断待ち**
>   （推奨は案A＝通知は止まっておらず一戸だけ別オーナーへ届いていた／原因は名寄せ＝
>   人には同じに見える文字が機械には別物だった。27話への伏線になり、題はそのまま生きる）。
>   **さらに（同日・オーナー指示「同じ系統は違う話へ、まだ出ていないアプリで」）**: 系統の重なりを
>   数え直し、**構成書にあった「使わなかった実在アプリ11本」**から充てる案に作り替えた。
>   **10話→駐車場配置図ビューア**（配置図は毎日出ているのに、共有フォルダのファイル名に ★要更新★ が
>   付いて古い表を読み続けていた＝**止まっていない。動いていて、古い**。空車表示の区画へ客を案内したら
>   埋まっていた）／**29話→マンション・ビル管理**（久美が作りかけた万能の管理画面を**自分で全部外す**。
>   二重管理をやめる＝32話中に無い「作りすぎない」軸）。
>   **→ 2026-08-24 に両方とも実施済み**（10話 2,745→3,199字／29話 3,036→4,068字）。
>   29話は**誠一が手順書を持ってくる場面を無改変で残し**、久美側の事件とコラムだけ差し替えた。
>   29話の中で10話の駐車場を参照して対にしてある。**総字数 95,119→107,705字**。
>   `00_目次.md`・`続編_全話構成.md`・`00_実話ネタ対応表.md` も実態に合わせた。
>   **未使用アプリの残りは9本**（手書き検針記録・顧客追客マネージャー・決済案内書などは、
>   材料として既存話に足す候補）。
>   **次は第5話から**。**未解決**: 13話が5話の訪問を「五月の午後」と回想しているが、5話は2話（六月）より
>   後なので月が合わない。5話を見直すときに直す。
>   **2026-08-25（サブPC）: 前編最終話との齟齬を解消した。**（オーナー判断＝B案）
>   前編はカクヨムで**17話まで公開済み／18〜31話は未公開の予約投稿**なので、**公開済み本文は一切触らず**、
>   未公開の終章と続編側だけを直した。
>   **①谷中＝九戸を二段階に分けた**（8/25夜・オーナー判断で最終形）。断る場面は作らず、**両方で受ける**。
>   ・10年前: 新誠が谷中の**九戸のうち七戸を返却**。**二戸（ワンルームと戸建）は誠一の一存で残す**
>   　→ **前編16話（公開済み）の「谷中の二部屋だけは、誠一の一存で、残された」がそのまま生きる**
>   ・続編21・22話（2年目）: 久美が訪ね、一度断られ、**七戸**を引き受ける。
>   　**二戸には手を出さない**（「あれは、新誠さんが預かっています。私からは、言えません」＝誠一への義理立て）
>   ・**前編終章（3年目・老女の売却のあと）**: 谷中から電話。「情けで残されるんは、性に合わんのや」で
>   　**二戸も久美へ**。誠一が十六年ぶんの書類を持参し、門扉の板きれを付け替える＝**元の場面をほぼ復元**
>   ・続編31話の手前に「前の年の秋、二戸が移って**鍵は九本になった**」を一段落追加
>   　→ 31話「谷中さんの、九戸です」・32話がそのまま生きる
>   **数字の連動**: 22話は九戸→**七戸**（管理料2,500円×7＝17,500円／2,000円提案で月1万5千の赤字／
>   鍵七本／七十九戸→**七十七戸**）、21話の採算説明も七戸ぶんへ。谷中の内訳は文化住宅5戸＋戸建2軒＝7戸、
>   新誠に残る2戸がワンルーム＋戸建1軒。
>   **②老女の売却**: 前編終章の「新会社の、**初めての**売却」を外した（続編13話＝村瀬の査定が初回のため）。
>   **③年表（確定）**: 2008秋 創業／**2009 谷中のワンルーム→戸建て**（前編4話）／**2015 新誠が42戸から撤退**
>   （谷中の七戸を含む・二戸は残す）／**2025 久美が独立・開業**（続編序章）／**2026 続編21・22話＝七戸を受託**
>   （撤退から**十一年前**。25話が「一年半」、26話が「二年」なので21話は1年〜1年半目）／
>   **2027 前編終章の谷中の電話＝二戸が移る**（七戸を預かって一年・2009から**十八年前**）＋老女の売却＋
>   踏切の掲示／**2028 続編31話**（「前の年の秋」＝2027）／**2029 続編終章**。
>   → **整合性優先の指示（8/25）で、21〜23話の「十年前」を「十一年前」に統一**（計11か所）。
>   終章の「十六年」系も電話が2027になったため**「十八年」へ**（誠一が預かった年数・板きれの雨風・
>   「安さで集めた客は…」と思った年＝前編4話も十八年前）。
>   **前編終章は続編の「4年後のあと」ではなく2〜3年目と同時期**なので、老女の売却は「何年か経った頃」に置き、
>   **谷中の電話はその後ろ**（新人→老女→谷中の順で時間が前へ進む）。
>   谷中が戸建てを任せたのは2009年なので、終章の「十七年前」→**十六年前**に修正（誠一が十七年ぶんの
>   書類を持ってくる段落は、受託ごと削除）。
>   **④踏切**: 前編終章にある「京橋駅周辺 連続立体交差事業」が続編に一度も出てこなかったので、
>   続編32話（同じ新喜多の踏切）に掲示の描写を1段落追加した。
>   **触ったファイル**: `カクヨム用/31_終章_久美の旅立ち.txt` ＋ `不動産屋、はじめました。（完成稿）.docx`
>   （両方に同じ本文を反映済み・段落単位で一致を確認）／`続編_カクヨム用/` の 21・22・32話 と `00_目次.md`。
>   **続編の総字数 107,705→108,429字**（21話3,642／22話3,713／32話3,590）。
>   バックアップは `scratchpad/novel-backup-0825/` に4ファイル。
>   **⑤踏切の事実関係を読売2025-09-19の記事で裏取りし、数字を直した**
>   （https://www.yomiuri.co.jp/local/kansai/news/20250919-OYO1T50048/）。
>   **新喜多踏切のピーク時遮断は「1時間に39分」で、45分は別の踏切**（JR西2019年調査）。
>   両作品とも新喜多を「四十五分以上」と書いていたので、**「四十分近く」**へ修正
>   （実測39分。地の文なので数字を丸めた。続編32話の回想だけは「この四十分を」）。
>   あわせて事業の記述を記事準拠にした＝ＪＲ片町線・東西線の京橋駅を地下化／地上線路**約1.3km**を
>   地下へ／**踏切3か所を撤去**／総事業費**1,031億円**（うちJR西14億円・国庫補助が付けば市負担約450億円）／
>   **2031年度に用地取得と工事着手・2053年度完了**／2000年度に検討着手し2015年3月休止、2025年に再始動。
>   **★ただし前編第2話「種を蒔く」は7/19に公開済み**で、そこにも同じ「四十五分以上」があった。
>   **ローカルは直したので、カクヨム側の第2話も同じ一文を差し替える必要がある**（オーナー作業・1か所。
>   「一時間のうち四十五分以上も、遮断機が下りたままになる。」→「一時間のうち四十分近く、遮断機が
>   下りたままになる。近くの踏切には、四十五分というところもある。」）。
>   **→ 上記の「十年前」問題は 8/25 に解消済み**（十一年前へ統一）。
>   なお TODO にあった「13話が5話の訪問を五月と回想」は、**現状どちらも七月・六月で矛盾なし**（解消済み）。
>   **2026-08-25: 9話「紙の在り処」に評論を反映**（オーナー経由の指摘）。
>   ①1,800ページの分類のリアリティ＝**「一枚だけを見せるのをやめ、前後三枚を並べて中央を判定させる」**を
>   本文とコラムの両方に追加（一枚では頭か途中か決めようがない、という理由まで書いた）。
>   ②「正しく分かれていたのは、二十六件」→**「三十件のうち、二十六件は、そのままで問題なかった」**
>   （26/30＝86.7%の精度と読まれないように。この回の主題は精度ではなく「自信がないものを人へ戻す」）。
>   ③コラムの一・二・三の見出しが本文に埋もれていたので改行して整形。
>   ④コラムの締めを**「記録を残すこと」と「経験を引き継ぐこと」は別**に書き直した
>   （紙をデータに変える仕事の次に聞き取りがある／前者は数日で終わる、後者は終わらない）。
>   **評論にあった「コラムが途中で切れている」は、貼り付けが切れていただけで原稿は完結していた。**
>   9話 3,188→3,517字・総字数108,445字。
>   **「丸」の意味の変化は、すでに一部あることを確認**（13話＝取り壊しで**丸ではなく横線**／
>   17話＝十四戸まで増えた時点で**止まる事故**／26話＝丸ではなく一戸あたりの時間を見る）。
>   ただし「丸を増やすこと＝成功ではない」を**正面から書いた回は無い**ので、検討の余地あり。
>   **2026-08-25: 10話「動いているつもり」の評論は、指摘1件だけで、それは現状維持と判断。**
>   「――この図は、十一月二十四日の表から作りました。」を「十一月二十四日時点の情報から作成しています」に
>   変える案。**採らない**。理由: ①**2025-11-24は月曜、25日が火曜**で、冒頭「十一月の、火曜の昼」＝11/25。
>   つまり二十四日の表は**前日の最新版**で、日付として最も自然（カレンダーで確認済み）。
>   ②直後に地の文「日付である。」があり、何の数字か迷わない。③三つめの「表を読めませんでした。現地で
>   確かめてください。」と同じ、機械が素朴に喋る調子で揃っている（仕様書口調にすると、そこだけ浮く）。
>   評論側も「好みです」と留保している。
>   **★この確認の過程で、全話に渡る書式の不具合を発見して直した。**
>   コラムや作中の箇条書きで、**「次の三つである。一、失敗を記録する」のように見出しが前の段落の末尾に
>   くっついていた**（改行されず、カクヨムでは箇条書きに見えない）。**16ファイル・31か所を行頭へ出した**
>   （12・14・15・16・17・18・19・20・23・26・27・28・29・30話＋先に直した9話）。
>   本文の文字は一字も変えていない（改行を入れただけ）。総字数 108,445→108,507字。
>   **2026-08-25: 1〜10話の通し評論を受けて、構造の穴5点を全部埋めた。**
>   評論の指摘のうち**2点は「10話までしか読んでいない」ゆえのもの**だった（④大きな傷＝17・18話で
>   窓口が四日止まる／③梅田大手の三パーセント＝23話「無料という土俵」で本回収）。
>   **理念が変わったという指摘は事実誤認**で、「儲からないままでも、成り立つ会社は、作れる」は
>   **前作30話の久美の台詞そのまま**（続編序章が引き継ぎ、続編終章が回収）。
>   残っていた穴を、次のとおり埋めた。
>   **①引き受けられなかった一戸を作った（18話・32話）**。窓口が黙って止まった四日のうち三日目に、
>   鴫野の文化住宅の家主・**川端**から問い合わせが来ていて、返事が届かなかった。二週間後に電話すると
>   「お忙しいんやろな思て」と、もう他社に頼んでいた。**責める声ではなかった**。赤西の
>   「気ぃ遣うて帰ったんや」がそのまま戻る。→ **最終の帳尻を 40戸→39戸**に変更
>   （42＝丸39／取り壊し2／取り落とし1）。25話の残りの内訳にも川端の一戸を足した。
>   これで**誠一が全面的に間違っていたことにならない**（＝1話の賭けが賭けのまま残る）。
>   **②通帳を10話で開かせた**。丸が九つのまま増えない回に、**残高102万円**（開業時250万）を置いた。
>   図を直すのに三日使い、その三日、募集は一件も動いていない。
>   **③誠一が賭けを締め直す場面を16話に追加**。融資1,200万の直後に電話で
>   「一千二百万は、管理料で返す金やないぞ」「無理やと思うで」**「四十二戸ぜんぶ埋めて、月十万かそこらやろ」**。
>   久美はノートに「――四十二戸ぜんぶで、月十万。」と書いて、**その下を空けたままにする**。
>   → **26話「二千円の答え」で回収**（一戸55分×42戸＝月38時間、十万円÷38時間＝**二千六百円**）。
>   **④第1話の前書きを新設**（`00_作品情報.txt` に「カクヨムの前書き欄に入れる」として記載）。
>   前作の結末を先に見ている読者への断りを、紹介欄だけでなく第1話でも出す。
>   **⑤「知ってます」を6回→4回に間引いた**（19話・31話を別の返しへ）。22話「……重いで」「知ってます」と
>   23話「知ってます」「……知ってるんだ」は機能しているので残した。**「褒めてへんで」は3回だが、
>   13話が作中で「京橋の年寄りは、どうしてこう、同じ言い回しをするのだろう」と自分でツッコむので意図的**。
>   総字数 108,507→**109,586字**。`通し読み_01-10話.txt`（37,012字）も作り直した。
>   **2026-08-25（続き）: 型の反復に着手した。**数え直したら、3〜16話の**14本中9本が
>   「ノートに――の一行」で締めて**いて、しかも**10→11→12 と 5→6 と 15→16 が連鎖**していた。
>   **5本に減らし、連鎖をすべて切った**（現在は 03・05・08・10・15・16 のうち締めが「――」なのは5本、
>   隣り合いなし）。やり方は**格言を削るのではなく、物の動作に置き換える**:
>   ・**6話** ノート「便利は、境目のないところから来る」→削除。**免許証の写しを封筒に入れ、封をして、
>   　鍵のかかる引き出しにしまう**で締める（引き出しの鍵は事務所に一つしかない）
>   ・**11話** 踏切で手帳に一行→削除。**遮断機の前で、藤木に返された図面を出して見る**。
>   　切れているのは「駐輪場あり（月三百円）」の「月三百円」。三か月気づかなかった、まで書いた
>   ・**12話** ノート二行→削除。**九本目の鍵が、封をした封筒で戻ってくる**。封に店名と日付と名前。
>   　「怒られると思って」。久美はその封筒を捨てずに引き出しへ
>   ・**16話** 誠一の一撃を書く場所を**ノート→一覧表の余白**に変更（26話の回収側も合わせた）。
>   　さらに末尾に**一覧表を引き出しに戻す動作**を足して、「――」で終わらないようにした
>   **15話の「――道具が減らしたのは、私の手間ではない」は残した**。あれは格言で終わらず、
>   そのまま久美の慢心になって17話の事故へ落ちる作りなので、型ではなく仕掛けとして機能している。
>   総字数 109,586→**109,931字**。
>   **2026-08-25（続き）: 14話「なんか動かない」を華の視点に書き直した**（案A＝視点を渡す）。
>   久美をほとんど出さず、**「なんか動かない」としか言えない側の一日**として書き直し。
>   ・華が自分の言い方のまずさに気づくところから始める／久美が八分で来て一分で直す恥ずかしさ
>   ・**華が久美のノートを覗いてしまう**（五つの質問がそこにある）＝道具の正体を、使う側が見つける形に
>   ・質問画面を自分で埋めていくうちに**「手書きの紙だけ空白になる」に自分で行き着く**
>   ・最後は**ヒラクの事務所で一覧表を見る**。丸を数えて十三。余白の「一戸あたり五十五分」の隣に
>   　**自分の名前が書いてある**（久美が書いた覚え書きを、書かれた側が見つける）
>   ・締めは、帰りの踏切で**華が自分のノートの一行目を書く**。「――入居者さんに、いつも聞いていること。」
>   　**その下は、まだ空いている**（16話の久美の空白行と対。31話で華がヒラクへ移る伏線）
>   **★書き直しの過程で前作との食い違いを発見して修正**: 続編14話は華を**「宮岡華・二十九歳・
>   二〇二一年採用」**としていたが、**前作21話では「黒崎華・二十四歳・前職は地方銀行の事務」**。
>   **姓が誠一のものになっていた。** 前作に合わせて**黒崎華**に統一し、31話の
>   「三十一歳・新誠不動産で十年」も**「三十六歳・十二年」**へ修正（前作の採用時24歳から逆算）。
>   総字数 109,931→**110,029字**。
>   **2026-08-25（続き）: B・C・D も実施。第二部前半の型崩しは、これで4本になった。**
>   **B＝一度で直らない回（11話「切れた文字」）**: 行の高さを計算で直したあと、**翌月また藤木から
>   電話が来る**構成に変更。今度は**摘要欄の右端**が切れている。画面では横にずらせば読めるので
>   気づかない（「私の手元にあるのは、紙です」）。**行の高さは計算で直せたが、列の幅は計算では直らない**
>   → 幅ではなく高さで逃がし、**送る前に必ず印刷して指で一行ずつ追う**手順を足す。
>   道具では閉じず、人の手順で閉じる形にした。**同話の締め（募集図面の右端が切れていた発見）が効く**。
>   **C＝時間を飛ばす回（12話「善意の台帳」）**: 「三か月後。」の一行で飛ばしていたところを、
>   **台帳の抜き書き6行**（日付・部屋・相手・遅れ日数）に置き換え、その三か月に何があったかを
>   数行で送る形にした（年末に一〇二が決まり、一月に給湯器、二月に水道が二度漏れた）。
>   **D＝逆順の回（8話「鵜呑みという事故」）**: **起きなかった未来から始める**。
>   「その年の十二月、……最初のガスの請求書を開く。一万二千円である。……**そういう電話が、
>   赤西不動産にかかってくることは、なかった**」→ 九月の電話へ遡る。
>   末尾も frame を閉じた（「十二月、野江のアパートの二階に、人が入った。プロパンと書いてある紙を見て、
>   決めた人である。**最初の請求書がいくらだったかを、久美は知らない**」）。
>   **26話の「何も起きなかったことは、数字に出ない」と対**になる。
>   総字数 110,029→**110,919字**。`通し読み_01-10話.txt`（37,300字）も更新。
>   最後は 通し推敲 → 作品ページ作成 → 投稿（**直近24時間の投稿数に非公開の上限**があるので
>   一度に大量投稿せず、弾かれていないか毎回確認する）
>   **2026-08-25（続き・サブPC）: 11〜15話の個別評論と、1〜15話の通し整合の指摘を反映した。**
>   個別評論ぶん（14か所）: **11話**=全角半角は「高さ」でなく「横幅」に効くと直し、折り返しのズレ→
>   高さのズレの順に並べ替え／「十四＋四」の内訳を明示。**12話**=NFCシールの値段（文房具屋で二十枚千円）を
>   削除し手近な物を組み合わせた印象へ／コラム末に「台帳は、人を疑うために作るものではない」を追加。
>   **13話**=鷹尾に自分の後悔（「わしも昔、一軒だけ、数字だけ見て売った」「あの家、ええ家やったなって」）を
>   持たせ、名台詞「金額は答えの、しるしや」が説教でなく後悔から出る形に／コラムを「AIにできる・できない」から
>   **「何を入力し、何を判断させ、最後に誰が説明するか」**へ。**14話**=コラム末に「良い聞き取りは、相手から
>   情報を取り出すことではない」を追加。**15話**=佐々木のフルネームを外す（**普通の登場人物は苗字のみ**が
>   本作のルール。全32話を調べ、フルネームは武本久美・宮岡誠一・黒崎華の3人のみ。**菊原和彦は父と息子の
>   区別に必要なので意図的**）／窓口を「携帯にあらかじめ入っているもの」から**スマートフォンで短い文字**へ
>   具体化／自動応答を**診断から情報収集へ**（エラー番号の意味を言わせず、メーカーと型番を聞く形。実際の
>   給湯器のエラーコードは機種で意味が違うため）。
>   **★通し整合の指摘12点は、すべて実物で裏が取れた（誤指摘ゼロ）。** うち判断不要の7か所を修正:
>   2話の登記情報提供サービス **332円→331円**（2024年4月改定・作中は2025年）／4話の鷹尾の発話を地の文から
>   括弧へ／11話の式を「行の数に十四を掛けて余白の四を足す」に直し**一行=18(合う)・二行=32(実際36で四足りない)**と
>   算数を通した（「二行なら高さを二倍」は36になってしまい式と矛盾していた）／11話「藤木に返してもらった図面」は
>   返却の場面が無いので「桜宮の募集図面」へ／12話の紛失鍵は**空室の一〇二**なので「入居者がいる部屋」を
>   二〇一の若い夫婦へ伸ばす形に。
>   **オーナー判断3件を反映**: ①**丸の欠落4か所**（6・12・14話ぶん・15話）を目立たない一句で埋めた
>   （6話=蒲生／12話=鴫野／**13話=蒲生のもう一戸で丸を13にし、12→13の飛びを解消**／15話=野江。すべて
>   1話に出る四十二戸の地名から取り、「四十二に丸がついたとき証明は終わる」と矛盾しない）。
>   ②**誠一は標準語に統一**（該当は1話に2か所・16話に1か所だけ。**16話の「無理やと思うで」は1話の台詞の
>   反復で「一年ぶりに聞く言い方だった」と受けている**ので、揃えないと呼応が壊れる箇所だった）。
>   ③**佐々木の契約は新誠時代のもの**とした（管理は十年前に切ったが**募集の仲介は新誠が受けていた**、
>   契約書類を作ったのは久美、という一文を追加）。
>   **`00_整合表_01-15話.md` を新設**（話ごとの丸・一戸あたり時間・増分の帰属・未解決の一覧）。
>   **`通し読み_01-10話.txt` と `通し読み_01-15話.txt` を作り直した**（本文が変わり古くなったため）。
>   ★通し読みの生成で**「（第一部）（第二部）」を二重に出す不具合**を出した（01・07話の .txt に元から
>   入っているのに生成側でも足していた）。**修正済み**。
>   総字数 110,919→**111,648字**（全32話）。
>   **残る未解決9点**（整合表に一覧あり）: ①4話の赤西が「いつ知ったか」（家か事務所かがずれる）
>   ②9話の菊原に「店舗部分は継続・住居2室だけ切った」の一行 ③11話の原因が三つ重なっている（絞るか）
>   ④13話の鷹尾の問い（十五分でどう好きになるか）を第二部の終わりまでに引き受ける ⑤「褒めてへんで／
>   知ってます」が4回（鷹尾以降は封印するか）⑥コラムが本文と同じ結論を再説明する回（2・7・12話）
>   ⑦14話の時制（冒頭だけ現在形）⑧13話の時期（12話が十一〜二月なのに13話が十二月）⑨久美の内面の揺れ。
>   **原稿は git 管理外**（`.gitignore` 2行目で CloudStorage ごと除外）。戻すなら **Drive の版履歴**で、
>   git では戻せない。
>   **2026-08-26（サブPC）: 16話（第十二章「無形という担保」）の個別評論を反映した**（3,221→**3,763字**・+542。
>   総字数は 111,648→**112,190字**）。指摘のうち本文に効く4点を直した。
>   ①**冒頭の「三日かかった」の中身を具体で出した** — 書く欄は多くない（会社名・代表者名・資金使途・
>   希望金額）のに二日は金額の欄で止まり、八百万と書いて消し、一千万と書いて消し、三日目の朝に
>   一千二百万円と書いて**そのままにした**。心理の説明は足していない（**加筆は具体物だけ**の方針どおり）。
>   ②**一千二百万円の根拠を北村に問わせた** — 「金額そのものは無理だとは思いません。問題は、金額ではなく、
>   何にお使いになるか」に対し、久美が**人件費七百二十万（一人ぶんの二年分・一年目は教える時間で売上に
>   ならない）／机とパソコン四十万／残りは二十八戸を引き受けてから管理料が入るまでの時間差の運転資金**と
>   答える形に。創業一年半・利益三百八十万で一千二百万という金額への引っかかりを、使途で受けた。
>   ③**「人を雇う」を軽く見せない** — 「事業計画書に、人件費という行を作った。一年半、その行は、無かった」を
>   一段追加（**17話＝上西の初出勤へつながる**）。④**コラムの解約率の言い切りをゆるめた** — 「来年もその収入が
>   ある」→「少なくとも現時点で続いている」＋相続・売却・取り壊しで終わることはある、と断ったうえで
>   「こちらの落ち度で切られた件数がゼロ」を見積もりの根拠に。あわせてコラムに**資金使途の段**を足した
>   （担保がない以上、金額の根拠は使途で示すしかない／金額が先で理由が後の計画書は通らない）。
>   ★**前回の「誠一は標準語」の統一漏れが16話に3か所あった**ので直した（「借りたんやてな」→「借りたそうだな」、
>   「保証協会は狭いんや」→「保証協会は、狭い世界だ」、「返す金やないぞ」→「返す金じゃないぞ」。
>   前回は1話と、反復台詞の「無理だと思うぞ」だけを揃えていた）。
>   **評論の残りの提案は反映していない**（すでに後続話で回収済みのため）: 「最初の一人を雇う話が見たい」＝
>   **17〜18話が上西の回**／「私にしか分からない仕組みは、仕組みじゃない」＝**28話「頭の中の手順」**が
>   まさにその回（誠一の事務員が三人続けて辞める→手順書）。**16話の末尾（四十二戸ぜんぶで、月十万）は
>   評論も高く評価しているので触っていない。**
>   **2026-08-26（続き・サブPC）: 17話（第十三章「黙って、止まる」）の評論も反映した**（3,673→**3,985字**・+312。
>   総字数 112,190→**112,502字**）。指摘は3点で、いずれも「技術の筋が通っていない」系だった。
>   ①**なぜ本番の窓口を無料枠二百通で動かしていたのか** — 「この窓口を作ったのは、半年前だった。あのころ
>   預かっていたのは、今の半分ほどである。月に二百通も使わなかった。だから、無料のまま、そのままにしていた」を
>   追加。**作ったときの規模のまま、仕組みだけ昔のまま**という形にして、久美の油断として成立させた
>   （半年前＝開業一年ごろ＝33戸／今は62戸なので「半分ほど」で整合する）。
>   ②**「受信は、生きていた」が送信上限と噛み合っていなかった** — 「入居者が送った文字は、サービスの側には、
>   届いている。止まっていたのは、それに返事を返すほうだけだった。**送った人の画面には、送れたように
>   見えている**」に。窓口が死んだのではなく**返事だけが死んだ**＝佐々木が催促しなかった理由とも噛み合う。
>   ③**コラムに四つめを足した** — 「四、『何も起きていない』を疑う」（三日問い合わせゼロ／いつも二十件の週に
>   四件／処理が毎回ゼロ件。**異常はエラーの形ではなく無風の形で現れる。エラーがゼロであることは、正常の
>   証明ではない**）。本文の入り口（一覧に四件しかない）と直結する。
>   末尾の（……私が、いちばん急いだ）は評論も残せと言っているので触っていない。
>   **2026-08-26（続き・サブPC）: 18話（第十三章「言えなかった三日間」）も反映**（3,317→**3,442字**・+125。
>   総字数 112,502→**112,627字**）。指摘は「惜しい」3点だけだったので、そこだけ触った。
>   ①**五千円の場面に一拍**入れた — 「月に五千円ほどで、五千通まで使えます」→**上西は、黙った。／久美も、
>   少し笑った。**→「**佐々木さんの四日間は、五千円で防げたかもしれない、ということです**」。説明で流していた
>   ところを、数字と四日間を並べる形に（元の「五千円払っていれば、そもそも止まらなかった」は重複するので削除）。
>   ②**川端の電話のあとに余韻を一文** — 「電話は、もう切れていた。久美は、受話器を、しばらく戻せなかった。
>   戻してからも、その上に手を置いたままでいた」。**丸ではなく点を打つ**場面への繋ぎ。
>   ③**コラムの表題を本文の主題へ寄せた** — 「代わりの連絡経路を、必ずもう一本持つ」→**「催促しない人ほど、
>   仕組みで守る」**。表題だけ替えると入りとズレるので、冒頭に「返事が来なかったとき、二度めを送る人は、
>   多くない。だから、返事を出せなくなったことには、こちら側が気づくしかない」を足して繋いだ
>   （三本立ての中身と末尾の「連絡をためらう人」はそのまま）。
>   **2026-08-26（続き・サブPC）: 19話（第十四章「出したつもり」）は指摘1点のみ**（3,135→**3,211字**・+76。
>   総字数 112,627→**112,703字**）。本文への指摘はゼロで、**コラム末の法律的な断定だけを弱めた**。
>   「**掲載していた条件で契約するのが原則であり、差額をどう扱うかは、業者とオーナーの間の問題になる**」→
>   「まず優先すべきは、**申込者に不利益を負わせないこと**である。そのうえで、誤表示による損失を誰が負担するかは、
>   **貸主と業者の間で整理する**。どちらが負うかは、**誤りの原因と、契約がどこまで進んでいたかによる**。
>   少なくとも、申込者に押し付けて済ませる話ではない」。募集広告の表示・申込・承諾・錯誤・仲介業者と貸主の
>   責任が絡む論点を一刀両断していたため（本文の運び＝藤木が差額を負い申込者は五万四千円で契約、とも矛盾しない）。
>   **2026-08-26（続き・サブPC）: 20話（第十五章「敷金という戦場」）は最小限だけ触った**（3,352→**3,425字**・+73。
>   総字数 112,703→**112,776字**）。**評論の指摘2件のうち、採ったのは1件**。
>   ①**採用**: コラムが教科書寄りに見え、耐用年数の数字（壁紙は六年で残存価値一円）が本編の余韻を切る、という
>   指摘。**冒頭に一段置いて主題を先に示した** — 「争いの多くは、金額そのものより、**その金額がどこから出て
>   きたのか分からない**ところから始まる。計算の基準は、**先に押さえておく材料にすぎない**」。以降の数字が
>   結論ではなく材料として読める。**表題（負担割合の根拠を、相手の言葉で説明する）と後半（理由を先に、
>   相手の言葉で）はすでに「どう説明するか」中心なので、構成は変えていない。**
>   ②**見送り**: 退去者の「最初からこう説明してくれたら、こんなに怖くなかった」を足す案。**評論自身が
>   「入れすぎると説明臭くなる／今のままで十分」「むしろ水漏れのくだり（＝男性が最後に評価したのが計算では
>   なく久美の過去の行動）を評価する」**と書いており、足すと男性に主題を代弁させることになるため入れなかった。
>   **本文は無修正**（「正しさは、納得ではない」「二枚の紙を裏返す」「計算は一分、説明は四十分」はいずれも
>   評論が核と認めた箇所）。
>   **2026-08-26（続き・サブPC）: 20話までの通し読みを作った** — `続編_カクヨム用/通し読み_01-20話.txt`
>   （**72,983字**）。あわせて `通し読み_01-10話.txt`（37,311字）と `通し読み_01-15話.txt`（**54,156字**＝
>   前回と完全一致。15話までは今回触っていないので当然）も作り直した。
>   **生成の道具を直下に置いた: `./novel_readthrough.py`**（前回はその場で生成して残していなかった）。
>   `./novel_readthrough.py 20` で 01-20話、引数なしで既存の通し読みを全部作り直す。**話の先頭の
>   「（第一部　…）」は原稿側に入っているので生成側では足さない**（足すと二重になる。前回の不具合）。
>   `.gitignore` に許可行 `!novel_readthrough.py` を入れてある（1行目の `*` 方式なので入れないと消える）。
>   **2026-08-26（続き・サブPC）: 1〜20話の通し評（A修正必須6点・B推奨・C校閲）を反映した。**
>   **A・B・Cの指摘はすべて実物で裏が取れた（誤指摘ゼロ）。** 全32話 112,814→**113,480字**。
>   ★オーナー判断2件は本人に確認して決めた（どちらも推奨案）:
>   ①**20話の退去者を一〇二号室→一〇一号室へ**。一〇二は6話で夏に退去・12話で空室→年末に新規決定なので
>   「六年住んだ」人は住めない。**一〇一は2話の水漏れの朝、在宅で押し入れの下を久美に見せた部屋**なので、
>   ラストの「朝早くに来てくれた」がむしろ強くなる。②**17話の「吉岡の通知が二か月止まっていた」は
>   未描写の過去として自足**させた（1〜20話のどこにも無い事件だった）。中身を3行で書き、吉岡に
>   「ああ、あれ、二か月ほど来てへんな。まあ、入っとるんやろ思て」と言わせた（＝18話の「催促しない人」と呼応）。
>   **20話の敷金計算を内訳と一致**: 見積14.9→14.5万（畳4.8→4.4万＝借主負担と同額で紛らわしかった）／
>   借主負担3.8→**4.8万**（引きずり傷1.2＋下地0.6＋クリーニング3.0）／家賃**4.2万**（1話「吉岡の四戸は四万円ほど」）／
>   敷金11.6→**8.4万**／返還7.8→**3.6万**。
>   **その他の修正**: 16話の戸数推移「半年後、十九戸」→**九戸**（10話「九戸で一万八千円」と衝突）／
>   18話「四十二戸ぶんの入居者の名前」→**六十二戸ぶん**／17話コラム「三つ」→**四つ**（同日の直し漏れ）／
>   17話で耳の聞こえない佐々木の家を**ドアを叩いて**訪ねていた→携帯から文字＋手のひらで二度（振動）／
>   17話 上西 二十四歳→**二十六歳**／17話 画面の一覧が何を映すのか一行／16話 売上二千百万の**内訳**を
>   北村に答えさせた（仲介1,500・管理料240・査定調査360＝**仲介で食っている描写が一度も無かった**）／
>   16話 融資の残り440万を「28戸ぶんの管理料の時間差」→**募集にかける金**（28戸×2千円＝月5.6万で桁が
>   合わなかった。**これは前段で私が入れた説明の穴**）／16話「亡くなったオーナー二件」→「今のところ、ありません」
>   （菊原の父は久美の受託**前**に他界）／11話 桜宮を預かった時期／5話 村瀬の実家が一覧表に載る理由／
>   12話 三か月の貸出114→**68件**／8話 多田の呼称「久美さん」→「武本さん」／7話 華の初出に半行の紹介／
>   1話「抽斗」→「引き出し」。
>   **`00_整合表_01-15話.md` を `00_整合表_01-20話.md` に差し替えた**（16〜20話の行と、今回の反映済み一覧、
>   **残る宿題7点**を収録）。宿題は構成に関わるので手を付けていない: ①同じ教訓を三度言う（本文→ノート→コラム）
>   ②一話の型が固定（14話だけ例外）③「褒めてへんで／知ってます」4回 ④久美が常に事後に正しい（本気で
>   反論するのは20話の退去者だけ）⑤道具の描写が抽象寄り ⑥上西の告白に伏線が無い ⑦登記情報331円は投稿時に再確認。
>   通し読み3本も再生成した（**01-20話は 73,649字**）。
>   ★**字数の数え方を統一した**: 各話 .txt の**改行を含む全文字数**（第1話＝5,655字。通し読みの目次と同じ
>   数え方）。これで数えると**全32話は 112,814字**。TODO.md にこれまで書いてきた「110,919→111,648→…」は
>   別の数え方が混ざっていたので、以後は**112,814字を起点**にする（16〜20話の反映ぶんは +1,128字）。
> - **ポケモンカード図鑑の相場は週1回**更新するとよい（`.venv/bin/python ingest_tcgdex_price.py`）
> - ~~note `nanka-ugokanai` はメインPCから~~ → **完了済み。メインPCの出番は無い**
>   （Zenn `ai-intake-hearing` は 8/20 23:40 に反映、note は 8/21 07:53 に公開）。
>   **2026-08-23 に実データで数え直した結果、AIツールベースの3点セットは
>   本体9 / Zenn9 / note9 で揃っており、公開待ちは0本**（本体の制作記録15本のうち
>   転載対象は不動産カテゴリの9本。ツール5＋メディア1は方針どおり本体のみ）
>
> ### E. アプリの増減（`git pull` で手元の構成が変わる）
>
> **52本 → 53本**（メインPCが最後に触った 8/19 は 52本）。増減は3件:
> ツールが2本増え（**メールアーカイバ**・**ワンピースカード図鑑**）、不動産が1本減った
> （legal-crosscheck を吸収）。内訳は 不動産**31** / ツール**16** / ゲーム6。CLAUDE.md の一覧は更新済み。
>
> - **新規: ワンピースカード図鑑 `onepiece-dex`（8537・ツール）** → セットアップは **C-2** を見る。
>   `data/`（約1.2GB）は git 管理外なので `./setup.sh` で作り直す
>
> - **新規: メールアーカイバ `mail-archiver`（8535・ツール）**。IMAP容量対策で `.eml` を
>   ローカル保管＋FTS5全文検索、取込から14日たったぶんだけサーバーから削除する。
>   **launchd 未登録・`127.0.0.1` 固定**（メール本文＝個人情報なので社内LANには出さない）。
>   残作業は ①iCloudのApp用パスワード発行→IMAP取込 ②Tailscale導入（人の作業）③`restore.py`
> - **削除: `legal-crosscheck`**。`jyuusetsu-research` の④タブへ完全に吸収した。
>   **`git pull` するとメインPCからもフォルダごと消える**（launchd 登録・`.url` は元から無し）。
>   `tokuyaku-generator`（8513）は**畳んでいない。恒久的に残す**
> - **直下に共有モジュールが増えた。コピーを作らないこと**:
>   `registry_parser.py`（謄本解析）／`tokuyaku_clauses.py`・`tokuyaku_core.py`（特約170項目）／
>   `doc2docx.py`／API関係4本（`appstore_api.py` `egov_law_api.py` `google_maps_api.py`
>   `japanpost_api.py`）と `API_STATUS.md` `GOOGLE_MAPS_API.md`
> - **`jyuusetsu-research` は port 8536 を予約したが、まだ開発中で launchd 未割当**（配布もしない）
>
> ### F. この4日でサブPCが終わらせたこと（メインPC側の作業は不要）
>
> - **重説アプリの大改修**（8/21）: `legal-crosscheck` を吸収してアプリ削除。全宅連の公式書式200本を
>   取得・分類。共有モジュール3本を直下に新設＝**コピーを作らないこと**。
>   残りは「4書式を出力して Excel を目で見る」の1つ（サブPCで続ける）
> - **Intel Mac で AI解析が黙って無効になるバグを修正**（`CLAUDE_BIN` が `/opt/homebrew/bin/claude`
>   固定だった。`baikai-generator` と `tokuyaku-generator` の両方）
> - **マイソクコンバーター**: A4縦対応＋貼った画像の横1.2倍伸びを修正（Excel実測確認済み）→ A-3で反映
> - **ポケモンカード図鑑に相場**（8,346件・25.9%で表示・円換算つき）
> - **APIの棚卸しで「未確認」6件を解消**＋**法人番号Web-API を申請完了**
> - **AIツールベース 9本目を3媒体とも公開し本番反映まで完了**（8/22）:
>   Zenn `openpyxl-row-height-autofit` ／ note `moji-ga-kireteru` ／ 本体 `/works/excel-row-height`
>   （`npx vercel --prod` 済み・リンク確認済み）。**次は10本目の題材選び**
> - **休業日は定時確認を送らないようにした**（8/22）→ A-3 の再起動で本番に入る
>
> **8/23（この日だけで17コミット）**
>
> - **AI重説アシスタント（8536・開発中）を大きく進めた**: 災害3項目（洪水・土砂・津波＋高潮）を実装／
>   書式のチェック欄が□なのにテキストを流し込んで壊す不具合を2件修正／抵当権を土地・建物に分離／
>   **自社の宅建業者・宅建士12欄の自動入力**（実物の重説と一字一句一致を確認・宅建士は一覧から選ぶ）／
>   区域指定7種・公示地価・追加資料5種。**他社の実案件が残っていた同梱テンプレ4本を削除**。
>   **メインPC側の作業は「4書式の印刷イメージを目で見る」だけ**（C を見る）
> - **取ったAPIを既存アプリへ広げた**（日本郵便＝買取DM8526・媒介8517の住所⇄〒照合／e-Gov＝特約8513の
>   根拠条文照合／e-Stat＝査定8509・事業計画8533の商圏データ）→ **A-3 の再起動で本番に入る**
> - **全53本を機械点検して3件直した**: ①requests依存の地雷（`http_compat.py` で解消。本番の
>   `/usr/bin/python3` で ImportError になりうる状態だった）②`pdf_orient.py` の8アプリ重複を直下1本へ
>   ③smoke_test を3本追加（原状回復24項目・決済25項目・覚書12書式）
> - **送付書ジェネレーター(8518)から個人情報を外した**（自宅住所・携帯番号が public リポジトリに
>   入っていた。差出人マスタを `senders.local.json`＝gitignore に分離）→ **A-0 の退避を必ず先にやる**
> - **ワンピースカード図鑑を新規に作って完成**（4,962枚・画像100%）→ C-2
> - **App Store の審査状況を API で照会できるようにした**（`python3 appstore_api.py --review`）



## ★App Store はこの順番で進める（2026-08-30 オーナー指示）

**1件ずつ。前の結果が出るまで次に進まない**（同時に3件出したことが 4.3(a) の引き金だった）。

| 順 | やること | 現状（2026-08-30 に API で実測） |
|---|---|---|
| **1** | **スクラップメモ 1.0.5 の更新** | **審査待ち**（配信中は 1.0.4）。8/29 に返信つきで再提出済み。**待つだけ** |
| **2** | **KeyTag の再申請** | **リジェクトのまま**（配信なし）。★1が通ってから出す |
| **3** | **ネオンブロックスの更新**（ネオンシリーズ**7本**入り） | 配信中 1.0.3。★2の結果が出てから |

**3の中身**: ブロックスの中に7本（ブロックス／サイボーグ／タワー／グラビティ／アイス／
エスケープ／ランナー）が入り、下の帯で行き来する形。見た目も7本で揃えてある
（決まりは `neon-blocks/NEON_STYLE.md`）。**出すときに必要なこと**:

- `ios-build-guard.sh neon-blocks` でビルド番号を +1（1.0.3 → 1.0.4 など）
- **スクショの撮り直し**（`python3 tools/shoot-store.py --only blocks`。旧デザインのものが残っている）
- 掲載文の見直し（「100面のパズル」から「7本入り」へ変わるなら、4.3(a) を踏まえて
  **前回と同じ型の文章を使い回さない**）
- ★他6本は**単体では出さない**（アプリを増やすと 4.3(a) の指摘に逆戻りする）

確認は `python3 appstore_api.py --review`。

## ★ネオン6本の見た目・音・入口を1つの型に揃えた（2026-08-29 メインPC）

オーナー指示「全部ネオン系にできた？」→ 実測したら**揃っていたのは3点だけ**（暗い地・名前・切り替え帯）
だったので、**共通の型を `neon-blocks/NEON_STYLE.md` に定めて6本に適用した**。

- 書体（全6本を角ゴシック＋Orbitron）／タイトルの型（アイスは新設）／音ボタンを右上に統一／
  決定ボタンをマゼンタのネオン管に／**調をゲームごとに変えた**（A・D・E・G・C・F）／
  **グラビティを縦画面に**（物理は無傷＝全20面が正解到達）／favicon 2本追加
- 検証: 6本とも Console エラー0・崩れ0（`va.sh check`）・6画面を目視
- **残り: 音の試聴（人）**／ブロックスは配信中なので**アプリ版は次の提出に乗る**（App Store は凍結中）
- 経緯と実測値は `neon-blocks/SESSION_LOG.md` の同日分

# TODO — 全アプリの索引

> ## 🖥 2026-08-21（サブPC・午後）— 重説アプリの大改修。次はここから
>
> **`jyuusetsu-research` を「調査 → 書類作成 → 特約 → 検算」の一本道にした。** 完了条件は
> **10/11 達成**。残るのは「4書式それぞれを出力して Excel を目で見る」の1つだけ。
>
> **① 全宅連の公式書式200本を Dropbox に取得・分類**（`契約・書類/書類雛形/`）。
> `.doc` 14本も `doc2docx.py` で .docx 化（うち6本に入力表あり）。アプリに同梱していた
> **他社の実案件入りテンプレート4本は使うのをやめた**（前案件の貸主名が残ったまま出る状態だった）。
>
> **② 共有モジュールを3本作った（直下）。コピーを作らないこと。**
> - `registry_parser.py` … 謄本解析。実体は `baikai-generator` にあった完成品。8517は薄い入口
> - `tokuyaku_clauses.py` / `tokuyaku_core.py` … 特約カタログと本文生成。8513は薄い入口
>
> **③ `legal-crosscheck` を吸収してアプリを削除**（53本→52本／不動産32→31）。
>
> **④ 見つけて直したバグ（他アプリにも影響）**
> - `CLAUDE_BIN` が `/opt/homebrew/bin/claude` 固定で、**Intel Mac では AI解析が黙って無効**
>   （`baikai-generator` と `tokuyaku-generator` の両方。エラーも出ない）→ 実体を探すようにした
> - `legal-crosscheck` 由来の**偽の🟢一致**と、建ぺい率で**必ず落ちる正規表現**
> - Web法令調査の `--tools` の渡し方（300秒タイムアウト → 71秒で完了）
>
> **⑤ ストリートビューの403を解消**（Maps Embed 専用キー `maps-embed-internal` を作成）。
>
> **★次にやること**: 4書式を出力して `./see.sh file <出力.xlsx>` で目視。
> そのあとは UI の作り込み（いまサイドバーを触るたび調査からやり直しになる）。
> 詳細は `jyuusetsu-research/SESSION_LOG.md` の 2026-08-21「その2」。
>
> **★注意（解消済み）**: `maisoku-converter/app.py` の未コミット変更（別セッションの縦マイソク対応）は
> **2026-08-21 に検証のうえコミット・push 済み**（`46ce8c7`）。作業ツリーは空。

> ## 🖥 2026-08-21（サブPC）で進んだこと — 次はここから
>
> **① AI業務マネージャーのLINE無反応を解決した（原因確定・コードはpush済み）**
> 原因は **LINEプッシュの無料枠切れ**（200/200。LINE自身が `You have reached your monthly limit.`
> を返して確定）。Chatworkは最初から正常だった。**★メインPCで反映が必要**:
> ```bash
> cd ~/chatwork-ai-manager && git pull
> launchctl kickstart -k gui/$(id -u)/com.shinsei.chatwork-ai-manager-worker
> launchctl kickstart -k gui/$(id -u)/com.shinsei.chatwork-ai-manager-line
> ```
> `services/line_client.py` は worker と line_webhook の**両方**が読むので片方だけでは不足。
> 調べた事実は `chatwork-ai-manager/README.md` の「LINEに送っても回答が返らない」節にある
> （SESSION_LOG/TODOはgitignoreでメインPCに届かないため、READMEに書いた）。
> **LINEのプラン変更（ライトプラン月5,000通）はオーナーが実施中。** 未完了なら枠は0のまま。
>
> **② ポケモンカード図鑑に相場を追加した**（`ingest_tcgdex_price.py` 新設・`app.py` に表示）。
> 8,346件取り込み、**8,175枚（25.9%）で相場が出る**。為替連動の円換算つき。
> 相場は日々動くので**週1回 `.venv/bin/python ingest_tcgdex_price.py` を回す**とよい
> （サブPCなのでlaunchd登録はしていない）。
>
> **③ APIの棚卸しで「未確認」を6件潰した**（詳細は `API_STATUS.md` の E-2）。
> RESAS=提供終了／Pokémon TCG API=実質停止でTCGdexへ代替／登記所備付地図=API不要・DL／
> PSA公開API=承認制で403・代替ルートで解決済み（見送り）／銀行API=要情報2つ／
> Document AI=月1,000ページ無料だが精度比較は未測定。
>
> **④ 法人番号Web-APIを申請完了**（橙＝法人番号のみ。フォーム 2026-08-20 ＋ メール 2026-08-21）。
> **発行見込みは 2026-09-04〜09-21。** 橙のため**インボイスWeb-APIは含まれない**（★3は別申請のまま）。
>
> **⑤ 解決済（2026-08-22）: サブPCから note へ自動投稿できる。**
> できなかったのは `~/.mcp.json` の **Playwright（`--isolated --headless`＝ログインが残らない）**の話で、
> **Claude in Chrome 拡張（普段のChromeのセッション）なら投稿できる**（8/22 に実証・ボット検知にも掛からず）。
> ただし `computer` の `cmd+v` は**合成キーにOSのクリップボードが載らない**ので貼れない。
> `javascript_tool` で `text/html` を入れた `ClipboardEvent('paste')` を `.ProseMirror` に投げる。
> 詳細は `ai-tools-base/SESSION_LOG.md` の 2026-08-22 の節。
>
> **人にお願いする残り**: 上記①の反映／★2 Dropbox APIのアプリ作成／★3 Google Drive・
> Search Console・Analytics（Console操作）／銀行APIは**取引銀行と会計ソフト**を教えてもらえれば再開。

> ## 🖥 サブPCの作業を受領した（2026-08-18 メインPCで実施）
>
> **合流は完了。** サブPCの22コミット と メインPCの未pushだった27コミット（KeyLine/KeyTag・
> chatwork開発エージェント等）を合流させ、push済み。両PCとも `git pull` すれば同じ状態になる。
>
> | サブPCからの依頼 | 結果 |
> |---|---|
> | ① `.dev-role` を main に | ✅ 済 |
> | ② `ai-tools-lab` → `ai-tools-base` の実体移動 | ✅ 済（node_modules/.next/.vercel/.env.local を移し、空フォルダを削除） |
> | ③ `dev-doctor.py --sync --fetch` | ✅ 実行。Python 3.9.6 / Node 26.3.1 は基準どおり。常駐36本＝メインPCなので正常 |
> | ⑤ MCP設定の受け渡し | ✅ **不要になった**。`.mcp.json` は git に入っており、今回のpushでサブPCへ渡る |
> | ⑥ メモリ差分の受け取り | ✅ 済（本文2本を取込・重複1本を統合・索引を更新。Dropboxの置き場は削除済み） |
> | ④ Zenn 残り1本 → note | ✅ **Zennは 2026-08-18 夜にサブPCで実施**（下の返信ブロックを見る）。noteは未 |
> | ⑦ `--verify` の試用 | ⬜ 未（触ったアプリが出たときに使う） |
> | ⑧ デジタル書斎の App Store 提出 | ✅ **済（2026-08-19 メインPCで審査提出・1.0.0/build1）**。次は審査結果の確認と実機で1度通すこと。手順は `digital-shosai/HANDOFF-APPSTORE.md` |
>
> **メインPCからサブPCへ返すこと（次にサブPCで `git pull` したら読む）**
>
> - **共通Visual Agent を統合した**（下の横断作業を見る）。`git pull` → `./visual-agent-check.sh` だけでよい
> - **メモリの実体20本がメインPCに無い**（索引にはあるのに本文が来ていない）。次の受け渡しで
>   Dropbox に置いてほしい: `project_chatwork_ai_manager` / `project_color_gravity` /
>   `project_cyborg_defense` / `project_fudosan_novel` / `project_kaitori_dm_maker` /
>   `project_kato_kyakuzuke` / `project_mansion_kanri` / `project_memorandum_generator` /
>   `project_pasha_calo` / `project_pokecard_profit` / `project_publish_setup` /
>   `project_shared_folder_reorg` / `project_shorui_cabinet` / `project_shorui_sender` /
>   `project_soufu_maker` / `reference_dropbox_url_icons` / `reference_pdf_orient` /
>   `reference_streamlit_bind` / `reference_this_pc` / `reference_xls_images`（すべて `.md`）
> - **KeyTag（iOSアプリ）を渡す上での注意（2026-08-18に判明）**
>   - `ios/` は gitignore。サブPCでは `cd keyline/keytag && ./setup-ios.sh` で作り直す
>     （NFCのentitlement・ATS例外・署名・**版数**まで一発で当たる）
>   - **版数は `keytag/version.json` が正**にした。build番号を上げたら pbxproj と version.json の
>     両方を揃える（片方だけだと、作り直したときに build 1 に戻って古いビルドが配信される）
>   - **署名（配布証明書と秘密鍵）はメインPCのキーチェーンにしか無い → App Store提出はメインPC限定**
>   - `keyline/data/` は gitignore（社員名・鍵番号）。サブPCのサーバーは空DBから始まる
>   - サブPCに Xcode が入っているかは**未確認**。無ければビルド自体ができない
> - サブPCに残っている `stash@{0} pre-origin-sync` とローカルブランチ2本は、**中身を見てから**消す
>
> **メインPCの現状**: 常駐36本・社内LAN共有あり（＝正常）。chatwork-ai-manager の4サービス
> （画面8540 / worker / LINE 8530 / ngrok）は 2026-08-18 11:33 に再起動し、稼働を確認済み。

> ## 🖥 サブPCで実行済み（2026-08-18 夜）— メインPCへの返信
>
> 依頼①〜④は**すべて完了**。⑤KeyTag は触っていない（提出はメインPC限定のため）。
>
> | 依頼 | 結果 |
> |---|---|
> | `git pull` | ✅ 48コミットを fast-forward で取り込み |
> | ① `dev-doctor --sync --fetch` / `dev-setup.sh --all` | ✅ 対象0本。**`dev-setup.sh` の空配列バグを修正**（bash 3.2 + `set -u`） |
> | ② `visual-agent-check.sh` | ✅ 入口A（MCP）・入口B（`./va.sh`）とも全項目 ✅ |
> | ③ `secrets-sync.sh import` | ✅ **`keyline/data` を受領**（DB 180KB＋免許証画像）。機密の不足は**0件**に |
> | ④ `secrets-sync.sh export` | ✅ 18件・980K → `Dropbox-個人/apps-secrets-handoff/apps-secrets-appurunoMacBook-Air.tar` |
> | ④ メモリ実体20本 | ✅ **20/20・168K** → `Dropbox-個人/handoff-20260818-sub-to-main-2/memory/` |
>
> **★メインPCでやること: 上の2つを受け取ったら、`./secrets-sync.sh import` と
> メモリ20本のコピーを実行し、`handoff-20260818-sub-to-main-2/` を置き場ごと削除する**
> （受け取りを確認した人が消す。件数20・168K を `ls`/`du` で見てから）。
>
> **回答: サブPCに Xcode 16.1 (16B40) が入っている**（メインPCで「未確認」だった点）。
> KeyTag のビルド自体は可能。ただし配布証明書はメインPCのキーチェーンにしか無いので、
> **App Store 提出はメインPC限定**という結論は変わらない。
>
> **後始末（2026-08-18 夜・サブPCで実行済み）**
>
> - `stash@{0} pre-origin-sync` と ローカルブランチ `pre-sync-backup-20260626` を **破棄した**
>   （中身は未追跡ファイルのみ＝作業ツリーのほうが新しい／固有コミットは PR #1 で main に既出、と
>   確認したうえで実行。復元用SHA: stash `9812065` / branch `b507e7c`。reflog にも残る）
> - **メインPC発の機密 tar（`apps-secrets-usernoMac-mini.tar`）を削除した。**
>   中身13件すべてが手元に実体としてあることを1件ずつ確認してから消している
>
> **★メインPCで受領・実施した（2026-08-19）**
>
> | 依頼 | 結果 |
> |---|---|
> | 1. `./secrets-sync.sh import` | ✅ **機密5件を取り込み**（brain-dump/.env.local, pasha-calo/.env.local, ai-ticket-counter/.env, theta-viewer/server/ftp-config.json, kaitori-dm-maker/senders.json）。既存13件は上書きせず据え置き |
> | 2. メモリ実体20本 | ✅ **20/20 取り込み・540K**。索引にあって本文が無いものは**0件**になった（中身が空でないことも1本ずつ確認） |
> | 3. 確認プロンプトを減らす設定 | ✅ 控えを取って合流したが、**6件とも既にメインPCに入っていた**（追加0件・既存51件は1件も消していない） |
> | 3-b. `theta-viewer` のドキュメント | ✅ **README を実体のある内容に書き直し、SESSION_LOG.md / TODO.md を新規作成**（コミット `1c4b4d6`）。実物のコードを読んで書いた |
> | 4. 置き場の削除 | ✅ **削除済み**（2026-08-19）。`handoff-20260818-sub-to-main-2`(180K) と `apps-secrets-handoff`(988K) の2つ。**消す前に、メモリ20/20・機密5件・tar内20件すべてが手元に実体としてあることを1件ずつ確認**した |
>
> **★サブPCへ返す・要判断（2026-08-19 メインPCより）**
>
> - **`Bash(python3 *)` と `Bash(curl *)` はメインPCでは許可のまま残す**（2026-08-19 オーナー判断・決着）。
>   サブPCは「メインPCには渡さない」と判断していたが、メインPCには**既に両方入っていた**。
>   外すと確認プロンプトが増えるため、**現状維持**とした（「いまより確認は増やしたくない」）。
>   **この件は決着済み。サブPCは再提案しないこと。**
>   なお渡さない理由（python3から削除・上書き・外部送信ができ、`git`/`rm`/`launchctl` を
>   許可していなくても迂回できる）自体は有効なので、**メインPCで破壊的な操作をするときは
>   許可されているからといって自動で進めず、これまでどおり人に確認する**（運用でカバーする）。
> - `/fewer-permission-prompts` はまだ回していない（`launchctl` `lsof` `xcodebuild` `sips` などを拾う想定）

> ## 🖥 サブPCへの引き継ぎ その2（2026-08-19 夜・メインPCより）— PSAカード管理／ポケモンカード図鑑
>
> **`git pull` だけで揃う。機密ファイルの受け渡しは不要**（変更したのはコードと文書だけ）。
>
> | もの | 場所 | 備考 |
> |---|---|---|
> | ポケモンカード図鑑を PSA管理の中から開く仕組み | `psa-collection/app.py` ＋ `pokecard-dex/app.py` | 画面のコードは**図鑑側の1本を共有**（写していない） |
> | アルバムの「⭐ 欲しいカード」 | `psa-collection/app.py` | `albums.json` に `dex:<図鑑キー>` で入る |
> | launchd の登録漏れ2件を追記 | `_launchd/install-launchd.sh` | psa-collection(8527) と pokecard-dex(8531) |
>
> **サブPCで気をつけること**
>
> - **図鑑データ（`pokecard-dex/data` 4.3GB）が無いPCでは、図鑑の選択肢は出ない**。
>   PSAカード管理はそれで普通に動く（オプション扱い）。慌てて用意しなくてよい
> - **`./install-launchd.sh` をサブPCで実行しないこと**。常駐はメインPCだけの決まり
>   （サブPCの launchd 常駐は0本のまま）。図鑑を見たいときは `cd pokecard-dex && ./run.sh`
> - **PSAカード管理のデータ（`psa-collection/data`）は gitignore**。サブPCに無ければ
>   CSVが無い旨の画面が出るだけ。持ち込みは README「別PCへの引き継ぎ」のとおり
> - 図鑑の画像パスは**DBに相対で入っている**。触るときは `_p()` を必ず通す
>   （通さないと単独起動では動くのに PSA管理からは1枚も出ない、という形で壊れる）
>
> **決着した判断（再提案しないこと）**
>
> - **図鑑の実体は `pokecard-dex/` のまま**。いったん `psa-collection/dex/` へ移す方針で
>   着手したが、本人判断で撤回した（移動は元に戻してある）
> - **保有カードと図鑑カードの自動突き合わせはしない。** PSA側にはポケモン以外の
>   カードもあり、CSVは英語表記・図鑑は日本語で名前が繋がらない。連携はアルバムだけ
> - **バインダーでは保有カードと欲しいカードを混ぜない**（上下2枠・枠をまたぐ並べ替えなし）

> ## 🖥 サブPCへの引き継ぎ（2026-08-19 メインPCより）
>
> **`git pull` だけで揃う。** 機密ファイルの受け渡しは**不要**（デジタル書斎は完全オンデバイスで
> `.env` もキーも無い）。この日にメインPCで入ったもの:
>
> | もの | 場所 | 備考 |
> |---|---|---|
> | デジタル書斎の iOSアプリ化一式 | `digital-shosai/` | Capacitor設定・同梱PDF4冊(5.7MB)・スクショ3サイズ(8.8MB) |
> | サポート／プライバシーページ | **gh-pages** `digital-shosai-support/` | `git fetch && git log origin/gh-pages` で確認 |
> | シミュレータ操作の道具 | 直下 `simtap.py` | 初回に `~/.sim-venv` を自動で作る |
>
> **サブPCで気をつけること**
>
> - **`digital-shosai/ios/` は gitignore**。触るなら `cd digital-shosai && npm run build && npx cap add ios`
>   で作り直す。**やり直すと build番号が 1 に戻る**ので、再提出のときは必ず
>   `./ios-build-guard.sh digital-shosai` で確認してから（いま提出済みなのは **1.0.0 / build 1**）
> - **App Store への提出はメインPC限定**（配布証明書がこちらのキーチェーンにしかない）。
>   サブPCでできるのはシミュレータでの確認まで
> - `simtap.py` は**端末を変えたら `./simtap.py calib` を先に叩く**（ウインドウの倍率を実測している）
> - サブPCの Xcode は 16.1。メインPCは 26.5 なので、**iOS 26 のシミュレータは無い**はず。
>   手元にあるシミュレータで確認すればよい（アプリはiOS 15+で動く）
>
> **決着した判断（再提案しないこと）**
>
> - **App Store の著作権欄は `SHINSEI PROPERTY MANAGEMENT.K.K.`** が既定（2026-08-19 オーナー判断）。
>   メモリに「新誠プロパティマネジメント」と書いてあったのは**誤りだったので訂正済み**。
>   ただし**サポート／プライバシーページのフッターの日本語表記は別物**なのでそのままでよい
> - デジタル書斎は **iPadも対象**（`TARGETED_DEVICE_FAMILY = 1,2` のまま）

**この表だけで「いま何が進行中か」が分かるようにする。** 詳細は書かない。
詳細は各アプリの `<アプリ>/TODO.md` と `<アプリ>/SESSION_LOG.md` にある。

書き方: 1アプリ1行。終わったら行を消す（記録はアプリ側のログに残るので消してよい）。

| アプリ | 担当PC | いまの状態 / 次にやること | 最終更新 |
|---|---|---|---|
| dropbox-backup | **メイン** | **2026-08-31 新規: 社内共有フォルダを個人Dropboxへ週1回ミラーする**（元＝大京商事Dropbox `共有フォルダ/（★必読★）新共有フォルダ` 22,575件・約11GB／先＝個人Dropbox `社内バックアップ/（★必読★）新共有フォルダ`。日曜6:00・`com.shinsei.dropbox-backup`）。**保存先の直下にある2026-07-04の手動コピーには触らない**（別サブフォルダへ入れている）。安全弁は ①元が15,000件未満なら何もせず中止 ②**削除が50件を超える回は空実行の時点で中断して通知**（`MAX_DELETE=99999 bash backup.sh` で承認して流す）。踏んだ罠2つ＝**openrsync は `--backup` を付けると `--delete` が効かない**／**launchd の C ロケールでは `"$VAR）"` が未定義変数になる**（どちらも `dropbox-backup/README.md`）。**次にやること: 初回ミラーの完走確認と launchd 登録（`bash dropbox-backup/_launchd/install-launchd.sh`）** | 2026-08-31 |
| ★夜間ジョブ全般 | **メイン** | **2026-09-04 朝の点検: 6本すべて正常、落ちたのは daily-write だけ**（`API Error: 529 Overloaded` で記事を書かずに終了 → 朝に手で流し直して完了＝`small-n-rate-false-alarm` を待機へ・push・本番デプロイまで確認。待機29本）。進み具合の実測: **共有フォルダOCR 2,157/7,957件目（27%・実質の残り約3,300件）／英語メール翻訳 941/1,395通（67.5%）／添付OCR 個人97.8%・社内96.4%**。**同日、共有フォルダOCRの1晩あたり上限を 300→1,000 件にした**（300件に7.7分＝持ち時間120分の6%しか使っていなかったため。3〜4晩で終わる見込み。**終わったら300へ戻す**）。**速くなった理由は8/31の二段構え化**（macOS Vision→取れなければclaude。無料・2.3秒/ページ）＝`README`にある「186件/晩」はclaude単独時代の数字で当てにしない。ただし**Visionで読めない手書き帳票の束に当たる晩はclaudeへ回って枠を食う**（9/2は新規24件が全部claude行き）。**次にやること: 9/5 朝に `com.shinsei.chatwork-ai-manager-ocr.log` で1,000件が何分で終わったかを見る**（120分で時間切れなら上限を戻すか並列を上げる判断）。 ▼過去の教訓: **2026-08-28 夜間ジョブ全滅の真因は PATH だった**（定額枠切れではない）。**launchd 起動は PATH が空**で、claude CLI の終了フック（Vercelプラグインの `session-end-cleanup.mjs`）が呼ぶ `node`（/usr/local/bin/node）が見つからず、**実際には応答していても claude が非ゼロ終了**する。★手で流すと PATH があるので再現しない。launchd から呼ぶ wrapper 5本（run_ocr_nightly.sh／sync-daily.sh／daily-write.sh／note-daily.sh／zenn-daily.sh）の先頭で `export PATH=...` を明示して解決。launchd経由でOCR2件成功を実測（昨夜は0件）。 | 2026-09-04 |
| photo-remake | **メイン** | **2026-08-27: 1.1.0 / build 4 は審査を通って配信中**（`appstore_api.py --review` で実測。下の「提出待ち」は解消済み。残るのは実機で触ること）。**フォトリメイク（iOS）1.1.0 / build 4 の提出待ち。実装はサブPCで完了・push済み。** 図形注釈を新設し矢印もその中へ統合（全13種＝矢印/四角/角丸/丸/三角/ひし形/五角形/六角形/星/ハート/吹き出し/十字/バツ）。塗りの色と枠線の色は別々に指定でき、それぞれ「なし」にできる。「種類」タブで矢印⇄図形を入れ替え可（中点・長さ・向きを引き継ぎ Undo 可）。上部「写真」ボタンは編集内容があるときだけ確認ダイアログ。Debug/Release ともビルド通過、シミュレータで画面と書き出しPNGを目視確認済み（**実機は未確認**）。配信中は 1.0.2/build3・審査中なし（2026-08-25 API 確認）。**メインPCでやること＝実機で触る→Archive→提出の3つだけ。手順は `photo-remake/RELEASE.md`**（新機能の文案も入れてある）。※`xcodegen generate` は実行しない（Signing の Team 選択が消える） | 2026-08-25 |
| onepiece-dex | サブ | **ワンピースカード図鑑（新規・8537・127.0.0.1）**。**2026-08-23: 立ち上げから動作確認まで完了。** 公式サイト（onepiece-cardgame.com）1本で カード4,962枚／62シリーズ／画像100%（1.2GB）。`check_dex.py` の点検は全項目0件。画面は **拡張パック23件／構築デッキ37件／その他2件**に分けた4タブ（さがす・拡張パック・構築デッキ/その他・リーダー）。パッケージ画像・発売日・価格は公式の商品ラインナップ156件から。`./va.sh` で目視・Consoleエラー0件まで確認済み。**スーパーパラレル系のレアリティ51枚を追加済み**（スーパーパラレル44/レッド3/海賊団2/ゴールド1/リーダー1）。公式が区分を持たないので tier-one-onepiece.jp の一覧＋画像照合で補完（`data/super_parallel.json`）。OP-18の2枚は公式カードリスト未掲載のため未反映。**相場は未着手**（`dex.price` 列は空で用意済み。マイカのIDはゲーム横断で全ID巡回が要る）。**launchd未登録**（サブPCは都度起動）。画像の「SAMPLE」透かしは公式・マイカとも入っており消せない。**2026-08-24(メイン): PSAカード管理(8527)の中からも開けるようにした**（実体は移していない・8537 単独起動もそのまま。`album_ui` 差し込み口＋`card_no_label()`＋session_stateを `op_` で名前空間化）。**同日: メインPCでスーパーパラレル51枚が丸ごと抜けていたのを復旧**（原因は `setup.sh` が画像取得より前に `fill_super_parallel.py` を置いていて照合が全外れ→0件で正常終了。工程順を直し件数チェックを追加。加えて `@st.cache_data` の鍵が `_stamp` で効かず再起動まで古い結果が出ていたのも修正）。**常駐はしない**（常設は PSA管理だけ） | 2026-08-24 |
| pokecard-dex | サブ | **2026-08-25: 「🖨 並べる」を新設＝選んだ4枚をA4 1ページのPDFにする**（`カードモデル.docx` の手作業を置き換え。カード 72.45×101.20mm＝実物の1.15倍・4枚とも同一・カード間4mm。実装は `card_sheet.py`。**PSA管理(8527)の中からも動く**ことを実機で確認）。**ワンピ図鑑(8537)にも同じものを付けられるが未実施**。画像100%（31,520枚）。内訳に推定14枚・参考画像4枚・透かし2枚あり。次はそれらの実物差し替え。**2026-08-19: PSAカード管理(8527)の中からも開けるようにした（実体は移していない）**。**2026-08-24: 常駐(8531)を外した**（オーナー判断＝常設は PSA管理だけ。`bootout`＋`disable`済み・8531が止まっていてもPSAの中から使える）。あわせて `@st.cache_data` の鍵が効いていなかったのを修正（引数名 `_stamp`→`stamp`。アンダースコア始まりはハッシュから外れる） | 2026-08-25 |
| psa-collection | メイン | **PSAカード管理**（旧「PSA保有カード管理」・8527）。**2026-08-19: ポケモンカード図鑑をオプションとして中から開けるようにし、アルバムに「⭐ 欲しいカード」を入れられるようにした**（バインダーは保有カードと欲しいカードを上下2枠に分離）。あわせてグレード絞り込みを削除・セット/年を「さらに絞り込む」へ格納・タイトルの絵文字を削除。**2026-08-24: ワンピースカード図鑑も中から開けるようにし、図鑑を1本決め打ちから登録制（`DEXES`）に変えた**（目印はポケカ `dex:` のまま・ワンピ `opdex:`。実機で検証し albums.json は検証前と同一に復元）。**同日: 常設はこのアプリ(8527)だけにし、図鑑2本の常駐は外した**（8531 は `bootout`＋`disable`。図鑑は止まっていても中から使える）。**2026-09-02: データ更新した新カードの画像が出ない問題を直した**（オーナー報告）。原因は2つで、どちらも無言で外れる型。①`fetch_new_images.sh` の画像URL書き出しが空を返していた（`osascript` に渡した JS の `"\n"` を AppleScript が本物の改行に展開 → JS が SyntaxError → `do JavaScript` は空文字・rc=0 で返す → アプリは「画像0枚を取得しました」と**成功表示**）。②収集が `ACTIVE` 固定で売却済を1件も見ていなかった。さらに PSA のCSVで **`Item Status` が `Sold` → `Previously Owned`** に変わり、`app.py` の決め打ちが外れて**売却済ビューが0枚**になっていた。→ 区切りを `String.fromCharCode(10)` に・0件なら失敗・待ち150秒・ステータス判定を `is_active()`/`is_sold()` に集約。**実測: 画像 873→918枚＝CSV全件／売却済 0→561枚**（`./va.sh` で目視・新規cert 168518237 の画像も確認）。次は「欲しいカード」の集計/書き出しが要るかの判断 | 2026-09-02 |
| growth-tracker | サブ | **GrowLog（成長記録アプリ・新規・ツール17本目・2026-08-30）**。Vite+React+TS+Tailwind v4+recharts、データは localStorage のみ、PWA対応（3005）。ホーム/グラフ/くらべる/カレンダーの4画面＋バッジまで実装し、Console 0件・3幅で崩れ0件・本番ビルドで sw 登録まで確認済み。**次: 実機スマホ確認／平均身長の表を公表値と突き合わせ／公開の判断** | 2026-08-30 |
| glow-runner | サブ | **グロウランナー（新規・ゲーム7本目・2026-08-29）**。Three.js の3Dランゲーム（`index.html` 1枚）。光るボールが自動前進、左右移動だけで動物をよける。ステージ制・ゲームオーバー/クリア画面・PC/スマホ両対応まで動作確認済み（Console 0件・`va.sh check` 0件・1440×900 と 390×844 で目視）。**次: 音／石・コイン（構造は用意済み）／実機のスマホ確認**。公開（GitHub Pages）はオーナー判断で未実施。**同日追記: 動物を6種類（速さ・当たり判定・形が種類ごとに違う）にし、10%の確率で出るドラゴン＝光って1.5秒後に前方だけへレーザーを撃つ敵を追加**（予告は橙の地面帯・本体は赤白。後ろは安全。`?dragon=1` で確認できる）。**難易度は人が遊んで未確認** | 2026-08-29 |
| flyer-creator | サブ | チラシクリエーター。型10種はagent-platform共通（直すのはagent-platform/core）。下帯ロゴ＋メイン写真の切取位置(上下)スライダー追加。次は物件データの未決3点 | 2026-08-16 |
| agent-platform | サブ | **完成扱いへ移行（2026-08-17）**: launchd 登録・0.0.0.0・社内LAN共有（8532）。残るのは作り込み（出来た .pptx 11枚の見栄え目視確認／字幕焼き込み／投稿API）で、通し実行はできる | 2026-08-17 |
| ai-tools-base | **メイン** | **AIツールベース**（https://ai-tools-base.vercel.app）。**2026-08-28: Zenn/note/本体の足並みを揃える自動化を入れた**。毎晩 22:00 zenn-daily が待機（drafts/zenn_pending・24本）から1本だけ出す → 22:35 note-daily が「Zennで公開済み」を確認して同じ記事を note へ → 22:45 daily-write が新しい1本を書いて待機へ足す。**前に出した1本がZennで公開されるまで次を出さない**（未公開を積み増さない）。詰まっている晩は空コミットで再デプロイを促す（Zennは自動再試行しない）。**いま `deploy-not-reflected` がレート制限で未公開。8/28朝に24本を一斉に試したのが「直近24時間の投稿数」に効いている。明日22:00（約38時間後）で通る見込み＝待てばよい**。articles 12本 / 待機24本 | 2026-08-28 |
| scrapmemo-petapeta | メイン | **2026-08-19: 保存容量の問題を根本解決し、1.0.4/build8 を審査へ提出済み。** 画像だけ IndexedDB へ移した（localStorage 5,100KB は WebKit固定／IndexedDB quota 9,830MB）。旧データは起動時に自動移行。実測で**写真30枚・77.3MB でも保持**（以前は1枚で上限）。`save()` の握りつぶしも解消し、孤児画像の掃除を追加。すべてXcodeシミュレータで実測確認済み。**審査は通過し 1.0.4/build8 が配信中**（2026-08-23 に API で確認、2026-08-24 に再確認。CLAUDE.md 反映済み）。**このアプリの残タスクは無し。** リリースノート文案は `RELEASE_NOTES.md`。**2026-08-27（メインPC）: 「長文をペーストすると途中までしか入らない」を調査 → スクラップメモ側は無罪だった**（iOS実機エンジンで 7,091字が無傷。切れた文章は 2,454字。同じ資料を2回貼って1回目だけ切れた＝**コピー/選択側の問題**）。ただし調査中に**実在の欠陥**を発見し修正: 長文だとテキスト欄が数千pxに伸び（7,091字＝9,620px）、末尾に置いていた「完了」に指が届かず、枠外タップ＝保存せず閉じる、を踏みやすかった。**完了／キャンセルをヘッダー（タイトルの右）へ移した**（縦の表示領域を1pxも使わない。字数の独立行も畳んで編集エリアを拡大）。iOSシミュレータ402ptで1行に収まることを画面で確認・Consoleエラー0件。**2026-08-28（メインPC）: iOSアプリへ載せて提出まで完了。1.0.5 / build 9 を Archive→アップロード（`UPLOAD SUCCEEDED`）→ ASC に 1.0.5 を作成・最新情報201字・build 9 をひも付け → オーナーが審査へ提出＝`審査待ち`（API で確認）。残りは結果待ちのみ**（説明・キーワード・スクショは前版から自動で引き継がれた）。**★Web版(GitHub Pages)は今も未反映**（外部公開＝要判断）。手順は `RELEASE_NOTES.md` 末尾（`.xcworkspace` は無い＝`-project` を使う） | 2026-08-28 |
| digital-shosai | **メイン** | **2026-08-27: 1.0 は審査を通って配信中**（API で実測。8/24時点の「審査待ち」から変わっていた）。**同日、アイコンをオーナー支給の画像に差し替え、1.0.1 / build 2 を Archive まで済ませた**（`~/Library/Developer/Xcode/Archives/2026-08-27/デジタル書斎 2026-08-27 1.0.1-build2.xcarchive`・22MB。中身が 1.0.1/build2 で iPhone/iPad とも新アイコンになっていることを確認済み）。支給画像は白背景に角丸アイコンが乗った形で**そのまま使うとホーム画面で角が白く欠ける**ため、`icon-src/make_icon.py` を「切り出す→1024へ→角の外を地色で塗る」処理に書き換えた（原本 `icon-src/source_2026-08-27.png` も git に入れた）。**同日、アップロード → 審査提出まで完了**（Organizer を使わず `xcodebuild -exportArchive` → `xcrun altool --validate-app` → `--upload-app` の3コマンドで通し、`build 2 … VALID` を確認。提出はオーナーが ASC の画面で実施。API で **`配信中 1.0 ／ 審査中のもの 1.0.1 … 審査待ち`** を確認済み）。手順とつまずき所は `digital-shosai/HANDOFF-APPSTORE.md` 冒頭（※`altool` は `--apiKey` にパスを取らず `~/.appstoreconnect/private_keys/` しか見ないので鍵をそこへ置いた）。**次は審査結果を待つだけ＝このアプリで今やることは無い**。通ったら `CLAUDE.md` と この行を「1.0.1 が配信中」へ書き換える。**アイコンが新しくなるのは配信後**（審査中は旧アイコンのままで正常）。残る積み残しは**実機（iPhone）で1度通すこと**。手順は `digital-shosai/HANDOFF-APPSTORE.md` 冒頭。**2026-08-28: 崩れたOCRを端末内で読み取り直す機能を実装**（`plugins/document-ocr`＝Apple Vision の `RecognizeDocumentsRequest`。**iOS 26以降のアプリ版のみ**・通信なし・課金なし・AIサービスではない）。`npm run build` / `cap sync` / iOSビルド（error 0・シンボル確認）／ Web版でボタンが出ないことまで確認済み。**未了は実機で実際に読み取り直す確認だけ**（`simtap.py` が Simulator のウインドウを取れず画面を押せないため）。学び: **ひらがな率だけでは「読み取り直すべき本」を判定できない**（縦書きを1文字ずつ拾った壊れたテキスト層は、読めないのにひらがな率が高く出る。実例 23.2%→22.8% でほぼ動かないのに文章として読める形になった）。詳細は `digital-shosai/SESSION_LOG.md` 冒頭 | 2026-08-28 |
| keyline | メイン | **2026-09-04（夕）: build 5 をアップロードし、提出の一歩手前で止めてある**。版 1.0 は `PREPARE_FOR_SUBMISSION`・**build 5 ひも付け済み(VALID)**・**スクショ5枚をネイティブ版で撮り直して COMPLETE**・説明1,760字（Excel入出力を追記）・審査ノート1,308字（**1項目めを「ネイティブに書き直した」＝4.3(a)への直接の回答**に差し替え）。あとは**［審査へ提出］を人が押すだけ**。踏んだ罠2つ: **entitlement の `NDEF` は SDK 26.5 で禁止**（altool 90778）→ `TAG` だけにした（`NFCTagReaderSession` しか使っていないので機能は減らない）／**`TARGETED_DEVICE_FAMILY` が iPad込みになっていた**→ iPhone専用に戻した。**2026-09-04 夜: 実機検証が完了**（オーナー確認・**新品の未フォーマットタグも通った**＝build2→3で踏んだ2件と同じ場所）。`NDEF` を entitlement から外しても実機の読み書きは成立する。**残るのは提出だけ**（スクラップメモ1.0.5の結果が出てから・押すのはオーナー）。※**KeyLine本体(8534)の「タグURLをSafariが開くか」は別件で未確認**。 **2026-09-04: KeyTag を SwiftUI+CoreNFC のネイティブに作り替えた**（`keytag-native/`・バンドルIDは同じ）。4.3(a)の「①バイナリの類似」を消すため＝旧版は Capacitor の殻でゲーム各種と実行ファイルがほぼ同一だった。**台帳のExcel書き出し・読み込みも新設**（外部ライブラリなし）。詳細は keyline/SESSION_LOG.md ／ **KeyLine（NFC鍵・備品貸出管理）＋ KeyTag（iOSアプリ）。** サーバーは 8534・社内LAN限定・テスト99件成功。**2026-08-18: KeyTag を App Store へ提出**（1.0.0/build2・掲載名 KeyTagNFC・サポートページ公開済み。**API上の提出日は 2026-08-17**）。**2026-08-25 レビュー → Guideline 2.1 - Information Needed で差し戻し**（レビュー機は **iPad Air 11-inch M3＝NFC非搭載**。**NFC機能のデモ動画**を App Review Information に載せて返信せよ、という要求。**リジェクトではないので build の上げ直しは不要**。Submission ID `a47e6a37-ee0d-48dc-8a35-2559cb1b976b`）。**次は ①NFCタグ（NTAG213）を入手 ②iPhone実機で撮影＝そのまま RELEASE.md の実機チェックリスト消化になる（NFC機能は一度も実機で動かしていない） ③YouTube限定公開にアップ→Notesにリンク→ASCで返信（★外部への操作なのでオーナー判断）**。撮影台本と詳細は keyline/SESSION_LOG.md 冒頭、配信手順は keyline/keytag/RELEASE.md | 2026-08-26 |
| spacemarket-monitor | メイン | **SpaceMarket 5掲載（うち稼働3件）の現状モニタ＝読み取り専用。** **★2026-09-04: レセプル福島の掲載・料金・カレンダーを全面的に作り直し、公開ページで反映を確認済み**（見出し／スペース名／説明文／プラン2本＝平日￥790・1時間〜＋**8時間パック￥380**・1日貸切は廃止／維持管理費なし／カレンダーは月〜木のみ開放／ネット環境を登録）。**カード表示は ￥380〜￥790**＝大阪の下位25%（￥346）に迫る水準。**残りは①説明文の「5時間以上」→「4時間以上」②備品（人感ライト・延長コード・ハンガー）を整えてから写真撮影＋VR枠**。**2026-09-03: 加東の落ち込みの原因を特定＝新規客の流入が半減**（申込43→20・新規25→11・リピート7→7・歩留まりは74%→90%と改善）＝**入口＝露出の問題**。診断は `./run.sh fetch` ＋ **`./run.sh trend`**（ログイン不要）。**加東・秋津は未着手**（原稿は `reports/copy-2026-09-03.md`。定員6→8名・雨天OK・日帰りグランピング・Weber電気グリル・会場タイプを「イベントスペース」へ）。**★実測で分かった重要な事実: SpaceMarketの並べ替えに「価格が安い順」は無い**（おすすめ順/評価が高い順/評価が多い順/駅から近い順/広い順/狭い順）＝**値下げで順位は上がらない**。福島の平日予約は**週末を手放す前に80件→13件（▲84%）**落ちており、オーナーの見立て（競合増・表示低下）と整合。詳細は spacemarket-monitor/TODO.md | 2026-09-04 |
| chatwork-ai-manager | メイン | Chatwork/LINE常駐AIエージェント（社内RAG・TODO/案件・Web/国交省API）。**常駐4サービスはメインPCで稼働中**（サブPCは常駐0本。worker・ngrokは1台のみ・同時起動禁止）。**2026-08-18: 定時TODO確認(13時/18時/翌10時等)を担当者ごとにグループ化＋TO付与する形式に修正（TASK-20260818-002・worker再起動済みで本番反映済み）。同一担当者でも一部TODOのaccount_id未解決だと別グループ・TO欠落になる不具合を修正＋名前解決の全ルーム横断フォールバックを追加（TASK-20260818-003・コミット済み・オーナー承認を得てworker再起動済み＝本番反映済み。ただし本日18時分は再起動前に実行済みのため旧仕様のまま。次回13時/18時/翌10時から新仕様）。添付Excel等の読込＋LINEからの常駐再起動も追加（TASK-20260818系）**。**2026-08-19: LINEに「処理中にエラーが発生しました: ClaudeError」が3回返った障害を調査し、原因を確定（claude CLIのOAuthトークン更新が約50分ハング＝アプリのバグではない。Keychainのmdat 09:43:03と復旧時刻が一致・実作業は18秒で残り159秒はセッション開始前に消えていた）。自然復旧済み・コードは未変更。切り分け手順は README「処理中にエラーが発生しました…」節に記載。障害中の依頼は黙って消えるので取りこぼし確認が要る。同日 TASK-20260819-001（定時TODO/週次を10:00→10:30。DB設定を実行時に読むので再起動不要・反映済み）と TASK-20260819-002（QAが未実行のTODO更新を「反映しました」と嘘をつく不具合の修正）が完了・コミット済み**  **2026-09-02: 写真の連投を1件にまとめた**（Chatworkは8枚同時でも8通に分け、本文は1通目にしか載らないため ack・返信が8回飛び、2枚目以降はキャプション無しで別案件扱いになっていた。実測で修正確認・worker反映済み・コミット `592f5533`）。**残: 新誠の所有物件が `properties` に1件も無く、写真に物件名が付かない**（会社の壁があるので大京のマスターへ素で足せない＝設計判断が要る）  **同日夜: 新誠プロパティの所有物件マスター23件を別テーブル `shinsei_properties` で作った**（`properties` へ会社列を足す案はオーナー指摘で取りやめ＝読む場所が6ファイル14か所に散り、絞り忘れが気づけない形の壁破りになるため。接点は名寄せ2関数だけ）。契約者27・空き7、呼び名（秋津2→秋津戸建て２）も機械的に導出。写真8枚は物件名が付いた。**残: SBP岸辺中の送金明細書が毎月届くが自動取込が無くベタ書き＝届き先を聞くこと** | 2026-09-02 |
| ↑ chatwork-ai-manager LINE障害 | **メイン** | **2026-08-20（サブPCで対応）: 「LINEに送っても反応がない」の原因は LINEプッシュの無料枠切れ**（コミュニケーションプラン200通/月を200/200消費。LINE自身が `You have reached your monthly limit.` を返すことを実測確認）。**Chatwork側・webhook・ngrok・各トークンはすべて正常**。実測ペースは1日約50通・月約1,000通で無料枠では構造的に不足（4日で枯渇）。オーナー判断で**ライトプラン（¥5,000税別/月・5,000通）へ有料化する方針**（プラン変更は人が実行・**未実施**）。再発防止として、`line_client._post()` の失敗握りつぶしを修正・`services/line_alert.py` でChatworkへフォールバック通知・残通数の日次見張り・push呼び出し元のlabel記録 を実装しコミット済み。**★メインPCで `git pull` → worker と line_webhook の両方を再起動しないと本番に効かない（未実施）**。**メインPCでの反映手順**: `cd ~/chatwork-ai-manager && git pull` → `launchctl kickstart -k gui/$(id -u)/com.shinsei.chatwork-ai-manager-worker` → `launchctl kickstart -k gui/$(id -u)/com.shinsei.chatwork-ai-manager-line`（`services/line_client.py` は worker と line_webhook の**両方**が読むので片方だけでは不足）。調べた事実・確認コマンドは **`chatwork-ai-manager/README.md` の「LINEに送っても回答が返らない」節**にある（SESSION_LOG.md と TODO.md は識別子を含むため gitignore＝**メインPCには届かない**） | 2026-08-20 |
| ↑ chatwork-ai-manager 要承認 | メイン | **worker再起動の承認待ち。** TASK-20260819-002（analyzer.py/qa.py/tasks.py）に加え、TASK-20260819-003（QAのTODO一覧回答を定時確認と同じ担当者グループ化＋アイコン整形に統一。scheduler.py/qa.py/services/agent_tools/*）も稼働中workerがまだ旧コードを保持している（PID 96042・09:00:11起動 < 両タスクのコミットより前）ため未反映 | 2026-08-19 |
| ↑ chatwork-ai-manager 業務日報 | **メイン** | **2026-08-21（サブPCで実装・テスト完了）: 業務日報＋18:30の自動処理**。作成→Dropbox保管→**Chatworkへ承認なしでExcelをアップ**→**`info@daikyocorp.co.jp` へ添付メール**（オーナー依頼で追加）。承認が要るのは本文のメッセージ投稿だけ（`outbox.NEVER_AUTO_KINDS`）。**来週から本番運用**。メールは SMTP直（`smtp.daikyocorp.co.jp`・**587のみ**・STARTTLS・上限30MB。実測で確定）で、パスワードは**キーチェーン** `chatwork-ai-manager-smtp`。サブPCで認証・実送信まで確認済み（自分宛。**info@ 宛はまだ1通も送っていない**）。**★以降はメインPCで作業する。手順は `chatwork-ai-manager/README.md` 先頭の「★ メインPCで最初にやること」全8段** | 2026-08-21 |
| ↑ chatwork-ai-manager 休業日 | **メイン** | **2026-08-22（サブPCで実装・検証済み）: 会社の休業日は定時確認を送らないようにした**。8/22（土）は年間休暇スケジュールで休業日なのに 18:00 の定時確認が投稿された（休業日判定が業務日報にしか入っていなかった）。`scheduler._is_company_holiday()` を追加し、carryover_1000(10:30)／closing_1800(18:00)／due_reminder(09:00)／週次棚卸し（月・金）の claim 直後にスキップを入れた。ナレッジ増分リフレッシュ(07:00)は投稿しないので継続。休み中に期限を過ぎたTODOは翌営業日の carryover_1000（期限超過）で拾われる。**★メインPCで `git pull` → worker 再起動しないと効かない（8/23（日）も休業日なので、反映すれば投稿が止まることでそのまま検証できる）**。あわせて「朝(10:30)の定時確認が無く18:00だけ出た」理由を調べたが**サブPCからは本番DBを見られず未特定**。設計上、朝は「期限超過」か「期限未設定かつ未確認」だけが対象で、18:00 が毎晩 期限未設定を全件確認して `last_check_at` を刻むため**対象0件で無言終了するのが既定の挙動**（メインPCで `scheduled_runs` の run_date=2026-08-22 を見れば `candidates:0` かエラーかが分かる。確認コマンドは `chatwork-ai-manager/SESSION_LOG.md`） | 2026-08-22 |
| ↑ chatwork-ai-manager 商圏統計 | **メイン** | **2026-08-23（サブPCで実装・実データ検証済み）: 取得済みAPIの棚卸しから、AIが1度も呼べていなかった e-Stat（政府統計）をTool化した**。直下に共通クライアント `estat_api.py`（urllib のみ）を作り、`stats_tools.py` で4Tool追加（`estat_area_profile` / `estat_housing_profile` / `estat_indicator_search` / `estat_indicator_value`）。qa.py の system prompt にも「印象で答えず統計を引く・調査年を書く・統計の空き家は募集中の空室も含むので自社空室率とは別物」を明記。`jyuusetsu-research` の population_service も同じクライアントへ付け替え（二重実装をやめた・戻り値は不変）。**あわせて `egov_law_api.py` を requests 無しでも動くようにした**（本番の `/usr/bin/python3` に requests が無いと 8/21 の法令Toolが ImportError で落ちるため）。**★メインPCで `git pull` → worker 再起動＋`.env.estat` の受け取りが要る。`{"property":"…"}` 指定は物件マスタDBがサブPCに無く未検証なので、メインPCで1度通すこと** | 2026-08-23 |
| ↑ chatwork-ai-manager 自動反映 | **メイン** | **2026-08-27（メインPCで実装・実機検証・本番反映済み）: 開発エージェントの完了と同時に、触ったアプリの launchd 常駐を自動で入れ替えるようにした**（オーナー依頼「開発完了と同時に再起動もかけて」）。従来は開発エージェントが稼働中サービスを触らない設計のため、直しても再起動するまで反映されず、psa-collection / brain-dump で実際に「pull したのに反映されていない」が起きていた。新規 `services/dev_restart.py` が `project_dir` ＋**そのタスクのコミットが触ったフォルダ**から対象を決め、Next.js/Vite なら `npm run build` を回し、plist が変わっていれば `bootout→bootstrap`・そうでなければ `kickstart -k`、その後ポートへHTTPして起きたことまで確認する。結果は完了報告の末尾に「🔄 反映（常駐の再起動）」として LINE/Chatwork に届く。**定時ジョブ（note投稿など）・未ロードのラベル・ngrok・Workspace直下は触らない**。worker 自身は報告を送ってから切り離した子プロセスで再起動する。実測: psa-collection 2.4秒/3.1秒・HTTP 200、brain-dump は build 込み 29.5秒・HTTP 200。**worker(40332→86160) と管理画面(8540)を再起動済み＝本番反映済み**。残るのは本物の依頼での確認1件（詳細は `chatwork-ai-manager/SESSION_LOG.md` 2026-08-27） | 2026-08-27 |
| ↑ chatwork-ai-manager OCR夜間 | **メイン** | **2026-08-27（メインPC）: 共有フォルダのスキャンPDFのOCRを毎晩の自動処理にした**（オーナー依頼「夜間に毎日300枚程度ずつ」）。着手前の実測は PDF 7,827件 / 取込済み 2,904（OCR 424・テキスト層 2,480）/ **未処理 4,923**。新規 `run_ocr_nightly.sh` ＋ plist `com.shinsei.chatwork-ai-manager-ocr`（**毎日 01:00 開始・300件 or 3時間の早い方で終了＝04:00には終わる**）。`ocr_ingest.py` に `--max-new`（**実際に処理できた件数**で止める。既存の `--limit` は先頭N件を切り出すだけで毎晩同じ所を舐めるため夜間バッチにならない）・`--max-minutes`・`logs/ocr_skiplist.json`（OCRしても文字が取れないファイルの再挑戦抑制）・連続5失敗で中断 を追加。**実測: OCRが要る1件あたり約2分**／テキスト層は1〜2秒。02:00 の mail-archiver-sync（16分）とは取り合う資源が無い。**launchd 経由で Dropbox が読めることを実測**（`/bin/bash` のFDA付与済み）。はまり所2件を修正: ①`kill`/`bootout` では `caffeinate → python` が孤児として残る→`set -m`＋プロセスグループごと停止 ②`log()` の `tee` と plist の `StandardOutPath` が同じファイルでログが二重化→`[ -t 1 ]` で分岐。**★新規PDFも継続して拾う**（毎晩フォルダ全体を再走査）。ただし **07:00 の日次リフレッシュは OCR しない**ので、スキャン書類を拾えるのはこの夜間ジョブだけ＝止めないこと | 2026-08-27 |
| 買取DM／媒介契約書（住所照合） | サブ | **2026-08-23: 日本郵便APIで宛先住所を照合できるようにした**。`kaitori-dm-maker`(8526) は台帳の全宛先を一括照合し、一致／不一致／〒の補完／不明に仕分けて**問題のある宛先だけ**を出す（DMは1通ずつ郵送費がかかるため）。`baikai-generator`(8517) は依頼者（甲）の住所⇄〒をその場で照合。判定は直下の共有クライアント `japanpost_api.verify()` に集約。**実データ（実台帳）での照合はまだで、架空4件で確認しただけ**。詳細は各アプリの SESSION_LOG.md | 2026-08-23 |
| 原状回復精算（ガイドラインの根拠） | **メイン** | **2026-08-30（メインPC・完了）: `restoration-calculator`(8508) の精算に、国交省ガイドラインの根拠（ページ番号＋本文の引用）を出せるようにした。** 一次資料を索引に入れた成果を、AI業務マネージャー以外へ広げる1本目。**★測って分かったこと: このアプリの計算は元からガイドラインどおりだった**（本文から読み取った耐用年数・負担方式で上書きしても**変わった部材が0件**＝`GUIDELINE_OVERRIDES` が空。**金額は1円も変わっていない**）。やったのは計算の修正ではなく **①証明できるようにした**（引用を索引の本文と1文字ずつ照合。改訂で本文が変われば**生成が落ちて気づける**）**②根拠を示せるようにした**（P27・P30 などのページと本文。退去者と揉めたときその場で示せる）**③定めが無い部材を区別できるようにした**（ソフト巾木・ペンキ・その他・諸経費は「ガイドラインに定めなし・当社の既定」と明記）。作りは `bookshelf/make_basis_table.py` が生成し `restoration-calculator/guideline_basis.json` をアプリが読む（**実行時に索引DB＝292MB・メインPCのみ には依存しない**のでサブPCでも動く）。**★オーナー判断が要る1件**: ガイドライン P28 は設備機器を「耐用年数で按分」とするが、**換気扇の耐用年数は本体にも資料8にも載っていない**。数字を作らず従来どおりで動かし、精算書に注意書きを出している。詳細は `restoration-calculator/TODO.md` | 2026-08-30 |
| 特約条項（根拠条文の照合） | サブ | **2026-08-23: e-Gov 法令API で「引用した条文が実在するか」を確かめる層 `law_citations.py` を直下に作り、`tokuyaku-generator`(8513) に組み込んだ**。生成本文から法令引用を拾い、現行条文と突き合わせて ✅実在／⚠️見つからない／❔引けない を表示（本文は書き換えない）。**キー不要**なのでどのPCでも動く。初回は e-Gov が全文を返すため数十秒（民法1.7MB）、以降は `.egov-cache/` で速い。次の一手は「生成時に条文をプロンプトへ流し込む」 | 2026-08-23 |
| 査定／事業計画（商圏データ） | サブ | **2026-08-23: e-Stat の商圏データを直下の共有モジュール `area_stats.py` にまとめ、`realestate-valuation`(8509) と `business-plan-generator`(8533) の両方から使えるようにした**。世帯数・1世帯あたり人員・高齢化率・転入超過・空き家率・借家率・共同住宅率・着工新設貸家・2040年推計を、調査年つきで取得し**そのまま所見・前提に貼れる文**まで作る。**Excel（査定書・計画書）への出力は未実装**（今は画面＋コピー用テキストまで） | 2026-08-23 |
| 全アプリ横断（地雷・重複・テスト） | サブ | **2026-08-23: 全53本を機械点検して3種類の改良を入れた**。①**requests依存の地雷**: 直下のAPIクライアント4本（japanpost/egov/google_maps/appstore）が requests 必須で、本番 worker（launchd の `/usr/bin/python3`）では ImportError になりうる → 互換層 `http_compat.py` を直下に作り4本とも切替（requests を隠した状態で実通信を確認）。②**コピーの一本化**: `pdf_orient.py` が8アプリに同一コピー → 直下1本＋各アプリは委譲（**quote-generator だけは別リポジトリなので実体のコピーのまま**・理由をファイル冒頭に明記）。③**テストの穴**: `restoration-calculator`(24項目)・`settlement-creator`(25項目)・`memorandum-generator`(12書式) に `smoke_test.py` を新規作成。**点検で問題なしだった点**: run.sh のバインドは27本すべて方針どおり／追跡ファイルに生の資格情報なし | 2026-08-23 |
| 謄本パーサの2実装（未着手） | サブ | **2026-08-23 に判明**: 直下 `registry_parser.py`(459行) と `realestate-valuation/services/registry_parser.py`(343行) は**コピーではなく設計の違う2実装**（dictスキーマ vs `RegistryInfo`＋`parse_auto`/`classify`）。統合は出力の型を決めるところからなので**今回は触っていない**。直下だけが「証明書発行日」に対応している。`baikai-generator` は33行の委譲で正しい形 | 2026-08-23 |
| jyuusetsu-research | メイン | **AI重説アシスタント＝完成（2026-08-27・メインPC）。8536で常駐・社内LAN共有・Dropboxに `.url` を配布済み。** 残っていた**印刷イメージの目視**を `print_check.py`（書式→PDF→PNG）で実施し、**紙面でしか分からない不具合6件**を修正（□のチェック欄を所有者名・床面積が潰していた／見出しセルに地積が印字／所在・登記所在・地積が1列ずれ／区分所有で一棟の欄に専有部分の値／建物の所在が空／床面積が新築年月欄へ）。 **追加資料が書面に入るようになった**（管理費・修繕積立金・滞納額・管理組合名・管理会社名・管理形態の7欄＝`services/intake_fill.py`。これらの欄は書式側が入力欄として色を付けていないので、単位「円」を目印に枠を探す）。 **区分建物の家屋番号・種類の取りこぼし**に受け皿（共有 `registry_parser`。正規表現が区分建物の家屋番号に1文字も当たらなかったのも修正）。 **抵当権の土地／建物振り分け**を実物の土地謄本2枚＋紙面で確認（※手元の実物に乙区が無かったので、乙区付きの実物での確認だけ残っている）。 **8517の会社マスタを共通マスタへ寄せた**（媒介契約書ジェネレーターに「自社（共通マスタ）」を読み取り専用で表示）。 **Excel の AppleScript で xlsx→PDF は通る**（過去の「-50 で不可」は構文違い。LibreOffice 不要）。 **plist は `/bin/bash run.sh` を呼ぶ**（launchd から Dropbox の公式書式200本を読むため）。 **次にやるなら**: 追加資料の残り（固定資産評価額・確認済証・石綿/耐震）の欄当て — 1項目ずつ紙面で確かめること | 2026-08-27 |
| tokuyaku-generator | サブ | **特約条項ジェネレーター**（8513・完成・社内LAN共有中）。**2026-08-21: 8513はこのまま恒久的に残す**（畳まない）。ただし `clauses.py` の170項目と本文生成を**共有モジュールへ切り出し**、8513 と新アプリの③タブが同じ1本を読む形にする。**コピーは禁止**（分岐すると片方だけ直した特約が契約書に載る） | 2026-08-21 |
| maisoku-converter | **メイン** | **マイソクコンバーター**（8505・社内LAN共有あり）。**2026-08-21: 帯変えモードの改修は完了**（A4縦対応／本体幅195mm・中央寄せ／白フチ自動カット既定オン／帯とのすき間4mm／帯の文字を実寸で統一）。**重大バグを1件修正: 列幅→ptの換算が2割狂っており、貼った画像が横に1.2倍伸びていた**（TwoCellAnchor→OneCellAnchor＋`_PT_PER_CHAR=6.0`。Excelで実測確認済み）。`smoke_test.py` 全項目OK・`./va.sh check` でUI崩れ0・**オーナーが画面で確認し既定値のままで確定**（紙に刷っての最終確認は未実施）。**★メインPCでやることは1つだけ: `git pull` 後に `launchctl kickstart -k gui/$(id -u)/com.shinsei.maisoku-converter`**（再起動しないと社内に修正前のコードが出続ける）。残っている課題は「帯の建設業免許番号が横向きで担当者欄へはみ出す（以前からの症状）」と「通常モード（AI解析）は用紙合わせ未対応でA4横のまま」 | 2026-08-21 |
| company-mail-archiver | **メイン** | **社内メールアーカイバ（8538）。大京商事の社員5人ぶん 3,656通を保管し、AI業務マネージャー（大京商事）の知識にしている**（塚本3,012／吉浦204／森185／大鹿134／松本121。添付の中身2,704件・ベクトル全通・知識索引に1,555通）。**コードは複製せず** mail-archiver を env で差し替えて動かす。**社員のメールはサーバーから消さない・127.0.0.1固定・会社の壁は company='大京商事株式会社'** を `guards.py` が毎回機械で点検して止める。夜間は00:30に差分だけ取り込む（launchd 登録済み）。**社員を足すときは試運転をせず、いきなり全期間で `--sync`**（試運転すると `last_seen_uid` が進んで古いメールが永久に入らない）。**残るは社員への周知＝オーナー判断待ち** | 2026-09-01 |
| mail-archiver | メイン | **メールアーカイバ（8535・完成運用中）**。メインPCで**常駐**（launchd 2本＝閲覧8535＋毎日2時の取り込み＋1年超のサーバー自動削除）。正規IMAP5アカウント（daikyocorp/shinsei-pm/dream-mama/we-love-kyobashi/shinichi-washimi）＋iCloud/GoogleはMail.app経由据置。**全55,496通・原本46GB個人Dropbox・DBローカル**。**ベクトル意味検索（ローカルe5-small＋専用.venv-embed／Claude精査）**・受信/送信/期間フィルタ・ISO-2022-JP化け修正・添付は📄開くリンク・返信(mailto/削除済みでも可)・スマホはTailscale（`usermac-mini.tailfcc81a.ts.net`）。**注意: 常駐と自動削除はメインPCのみ（サブPCで登録しない・二重削除防止）。plist/venv-embed/.env/DBは各PC別＝gitで渡らない。** **残り: restore.py（未着手）**。**2026-08-31 に未設定だった2アカウント（iCloud `s.washimi@icloud.com` / Gmail `daikyocorp.s@gmail.com`）を追加し、IMAPログインまで実測で確認＝7アカウント全部が揃った**（App用パスワードをキーチェーンへ。**画面で作った項目は「アクセス制御」で「すべてのアプリケーションによるアクセスを許可」が毎回必要**）。**次の朝に初回取り込みの結果を見ること**。**2026-08-31（午後）: 添付の中身を全文検索に入れた**（PDF・旧Office・スキャンはmacOS VisionでOCR。実測7.4件/秒・夜間に小分けで取り込み中／進み具合は `attach_extract.py --stats`）。**あわせてAI検索を『試行するAI』に作り替えた**（0件でも1件でも止めず、期間外し・必須語の削減・言い換えを順に試し、添付の中身から答えの文まで書く。画面の「🤖 AIに探してもらう」） | 2026-08-31 |
| soufu-generator | **メイン** | **送付書ジェネレーター（オーナー個人専用・8518）。2026-08-24: 常駐が社内LANに全公開（`*:8518`）だったのを `127.0.0.1` に閉じた。** 実測で localhost=200・192.168.1.105/.140=接続拒否、画面の送付者4件も確認。デスクトップの `.app` は `open http://localhost:8518` なので影響なし。**はまり所: 引数を変えたら `launchctl kickstart -k` では反映されない**（ロード済み定義で再起動するだけ）。`bootout` → `bootstrap` で入れ直すこと。残タスクなし | 2026-08-24 |
| nyanko-ice | **メイン** | **にゃんこアイス（ゲーム・App Store 未提出）。2026-08-27: 見た目を「アイス屋の店内」に作り替え、書体と音を入れた**（オーナー依頼「フリー素材とか活用してもっと魅力的に」）。空白だった Y=280〜580 を埋め、画面を4層（木の看板／明るい店内／冷凍ショーケース／カウンター）に再構成。**Zen Maru Gothic（SIL OFL）を使用文字だけに絞って同梱**（`tools/fetch-font.py`・32KB×2）。ネコ店長に体・エプロン・コック帽を付けて前足をケースの縁へ。**音はオシレータ直打ちを全廃し、効果音10種＋BGMループ＋オン/オフボタンに**（**ファイルは同梱せず WebAudio で合成**。差し替えたいときは `www/assets/audio/` に置いて `USE_FILES=true` にするだけ）。Console エラー0件・3画面を目視。**2026-08-28（メインPC・午後）: 広告を全部外し、初提出まで完了＝審査待ち**（オーナー判断「一旦、広告なしで出す」）。AdMob の CSS/DOM/JS・プラグイン・Info.plist の2キー・UMP の Pod まで削除し、**ipa の Frameworks が Capacitor と Cordova だけ**であることを実測。ゲームオーバーの「動画を見てコンテニュー」は動画なしの `▶ つづきから` に。**iPadの余白も解消**（`max-width:470px` → `min(100vw,62vh)/720px`。iPhone の見え方は不変を前後比較で確認）。`www/support.html`・`www/privacy.html` を新設して公開（200を実測）。ストア文言（`store-text.md`）・**スクショ iPhone 5枚 + iPad 5枚**・**1.0 / build 2** を ASC に投入 → オーナーが価格・Appのプライバシー・年齢制限を設定して**提出＝`審査待ち`**（API で確認）。Web版(gh-pages)も同日反映済み（`DEPLOY_FOLDERS` に `nyanko-ice:www` を追加＝以後 push で自動）。**★残り: 音が未試聴のまま出した**（気に入らなければ次のビルドで差し替え）。出し直すときは必ず build 3 へ | 2026-08-28 |
| color-gravity | **メイン** | **🛑 2026-08-29〜 提出停止中（4.3(a)）。** 同日 KeyTag/スクラップメモ/にゃんこアイスの3本がまとめて 4.3(a) でリジェクト（API で実測）＝**アカウント全体の出し方を見られている**と考えられるため、**App 記録の作成もアップロードもしない**。まず KeyTag だけ Resolution Center に返信して様子を見る（オーナー判断）。**ビルドとスクショは完成済み**なので再開が決まればその日に出せる。詳細は `color-gravity/RELEASE.md` 冒頭。以下は 2026-08-28 までの記録: **カラー・グラビティ（ゲーム）。2026-08-28: ①見た目・音・手触りを作り込み、②iOSアプリ化して App Store 提出の直前まで進めた。** ①は「にゃんこアイスのときみたいに」というオーナー依頼。**音がゼロだったので効果音10種＋BGM＋🔊を新設**（WebAudio合成・ファイル同梱なし。ゲート音は通った色で音程が変わる）／**Orbitron＋Zen Kaku Gothic New（SIL OFL）を使う文字だけ同梱**（96.4KB）／**リロードで解放ステージが全部消えていたのを localStorage で保存**＋★評価＋タイトル画面／描画（惑星の縞と輪・降着円盤・二重リングのゲート・宝石の結晶・星空3層の視差）と手触り（ゴムと力ゲージ・画面の揺れ・クリアの閃光）。②は Capacitor 8（SPM）で `com.shinsei99.colorgravity`・**最低OS 15.0**・iPhone縦のみ/iPad全方向・アイコンはコードで描画・`www/support.html`と`privacy.html`を新設し `DEPLOY_FOLDERS` を **`color-gravity:www`** へ変更・ストア文言（`store-text.md`。**提出前にApp Storeを実測して4.3(a)の根拠を用意**）・スクショ iPhone5+iPad5・**1.0/build1 を Archive→ipa書き出し（1.58MB）**。**★物理には一切触っていない**ことを `tools/verify_solutions.py`（物理区間を抜き出して node で実行）で担保＝全20面が正解に到達。Console エラー0件。**残りはオーナー作業2件: ①App Store Connect で App 記録を作る**（APIでは新規App を作れず、無いと `altool` が Apple ID を決められない＝実測）**②できたら声をかけてもらえれば、検証→アップロード→文言・スクショの流し込みはこちらで機械実行**。そのあと価格・Appのプライバシー・年齢制限・提出は画面での作業。手順は `color-gravity/RELEASE.md`。**音は未試聴のまま**（気に入らなければ build 2 で差し替え） | 2026-08-28 |
| neko-escape | **メイン** | **にゃんこ大脱出（ゲーム・HTML1枚）。2026-08-28: にゃんこアイス／カラー重力と同じ方針で見た目・音・手触りを作り込んだ**（オーナー依頼「にゃんこアイスを魅力的にしたときみたいに」）。**盤面の絵文字を全廃して canvas の手描きに**（床・ラグ・窓の光・座布団のマス・ねこ・お掃除ロボット・キャットタワー・ソファ・毛糸・さかな・落とし穴。HUDと凡例のアイコンも同じ関数で描くので絵柄が食い違わない）。**音がゼロだったので効果音9種＋BGM＋🔊を新設**（WebAudio合成・ファイル同梱なし・`USE_FILES` で差し替え可）。**Zen Maru Gothic（SIL OFL）を使う文字だけ同梱**（396字・45KB×2・`tools/fetch-font.py`）。**リロードで Stage 1 に戻っていたのを localStorage で保存**し、タイトル画面・ステージセレクト・手数と自己ベストを追加。**ロボットが次に来られるマスを赤い破線で表示**（BFSで動く相手なので見えれば詰まない）。**ルールとステージデータ（全20面）は1文字も変えていない**。はまり所: 猫の影の半径が跳躍中に負になり `ctx.ellipse()` が例外 → **rAF の輪が切れて盤面が固まる**（次フレームを先頭で予約する形に変更）。Console エラー0件（favicon も data URI にして解消）・`va.sh check` 0件・3幅ではみ出し0件・全20面を機械で3手ずつ打って例外0件・5画面を目視。`run.sh`（127.0.0.1:8543）と `?stage=N` を新設。**公開はオーナー判断で「する」に決定 → `deploy.yml` の `DEPLOY_FOLDERS` に `neko-escape` を追加済み**（それまで `CLAUDE.md` は「GitHub Pages」と書いていたが `origin/gh-pages` にフォルダが無く**実際は未公開**だった）。**公開済み**（2026-08-28 夜。https://shinsei99.github.io/project/neko-escape/ が HTTP 200、**公開ファイルの SHA-256 が手元の index.html と一致**することを実測）。**同日夜（オーナー選択で3点追加）: ★評価・新ギミック2種・10面追加＝全30面に**。総当たりで測ったら「適当に押すと負けるが全20面を10分で完走できる浅さ」だったため。★3=最短手数ぴったり／★2=+2手以内（**最短手数は解析器が出した値を `opt` として全30面に貼った＝人が数えていない**）。新ギミックは **ねこのぬけ道**（ねこだけ通れる道）と **スイッチとドア**（踏むと閉じた道が開く）で、21面以降にだけ出る。**21〜30面は `tools/gen-stages.js` が格子から自動生成し、解析器で採点して採用**（21面以降は 最短8.5手・勝ち筋率55.2%。1〜20面は 5.8手・64.5%＝後半のほうが重い）。ルールが node 側（`tools/engine.js`）と本体の2か所になったので **`tools/crosscheck.js` で236手ぶん突き合わせて食い違い0件**（★期待値をわざと1文字ずらすと検出できることも確認＝空振りしない試験）。タブのアイコンもネコの顔に。全30面に解無0・opt 全一致・Console エラー0件・3幅ではみ出し0件・★3が実機で出ることを確認。**★この回の変更は別セッションのコミット `8a55ac3d` に巻き込まれて入っている**（中身は無事。3セッション並行の副作用）。**同日深夜: iOSアプリ化して App Store 提出直前まで**（オーナー依頼）。Capacitor 8(SPM)・`com.daikyo.nekoescape`・1.0/build1・MinimumOSVersion 15.0・**ipa 書き出し済み(870KB)**。本体を `www/` へ移し `deploy.yml` も `neko-escape:www` へ（**同じ push で揃えること**）。アイコン/起動画面は本編と同じネコの顔を自前生成（アルファ無し）。`www/support.html`・`www/privacy.html`・`store-text.md`（**4.3(a)対策で App Store を実測＝完全一致0件・近い名前の『ミケねこ大脱出』はジャンプアクションで別物・『猫×脱出』の棚は謎解き系**）。スクショ iPhone(1290×2796/1284×2778)・iPad(2048×2732) を各5枚。**シミュレータで実際の不具合を2件発見して修正**（タイトルが「20ステージ」のまま／`viewport-fit=cover` が無くヘッダーが Dynamic Island に潜る）。はまり所: `cap add ios` は `DEVELOPMENT_TEAM` を書かない／`-exportArchive` は API キーが要る。**★残りはオーナー: ①ASC で App 記録を作る（APIでは不可） ②価格・プライバシー・年齢制限・提出**（①のあとは `python3 finish-release.py --apply` の1コマンド）。手順は `neko-escape/RELEASE.md`。**残り1件: 音の試聴（鳴ることしか確認できていない。ドアの音も足した）**。**2026-08-29: 世界観を「宇宙の脱出行」へ作り替え＝改題『ネオンエスケープ』**（オーナー指示「ネコじゃなくてスペース戦闘機が敵から逃げる感じの方が良くない？」「敵はヒヨコと同じくネオンのブロック系」「幼稚な感じも無くしてサイバーな感じで全体的に」）。ネコ→**自機（上向きのブロック製戦闘機）**／ロボット→**追撃機**（ネオンタワーと同じ2色市松ブロック）／キャットタワー→**ワープゲート**／ソファ→ブロック壁・毛糸→EMP機雷・さかな→ブースター・落とし穴→特異点・ぬけ道→スリット・スイッチとドア→起動パネルとエナジーゲート。書体を **Zen Maru Gothic（丸）→ Zen Kaku Gothic New＋Orbitron**へ、角丸も詰め、文言とステージ名30本を全面書き替え。**横900×600 → 縦600×900**（座標を90°回して写すだけ＝面の作り直し不要。左→右の脱出が下→上に）。**敵を同時に動かして1手 約1.5秒→約0.5秒**、**警戒ゲージ（2.6秒手が止まると敵だけ1歩）**を追加。**難易度: 実測で「平均最短6.7手・勝ち筋率61%＝打てる手の6割が正解」と簡単だったので、高難度パック31〜40面を生成して全40面に**（最短9〜11手・勝ち筋率44〜59%。★★は+2手→**+1手**）。**敵4台は捨てた**＝398面作って解けたのは3面だけ（初手で詰む）。検証: 突き合わせ311手/食い違い0・Console エラー0・3幅で崩れ0・PCでも1画面に収まることを確認。**★App Store は 4.3(a) で凍結中につき提出しない**（`store-text.md`/`RELEASE.md`/`icon-src` はネコのまま） | 2026-08-29 |
| cyborg-defense | **メイン** | **サイボーグ防衛軍（ゲーム・HTML1枚・GitHub Pages）。2026-08-28: にゃんこアイスと同じ方針で見た目・書体・音を作り替えた**（オーナー依頼「にゃんこアイスを魅力的にしたときみたいに」）。**黒地＋薄いグリッドだけだった画面を4層に**（空と環のある月／遠景の街2列／消失点へ収束する通路の床／自軍拠点）。動かない部分は1枚に焼いてから貼る。**両脇の防壁は装飾ではなく、横移動が `W*0.16` に制限されている理由を絵で示すためのもの**。**音がゼロだったので効果音11種＋BGM＋音のオン/オフを新設**（WebAudio合成・ファイル同梱なし・`USE_FILES` で差し替え可）。**Orbitron＋Zen Kaku Gothic New（どちらも SIL OFL）を使う文字だけ同梱**（77KB・`tools/fetch-font.py`）。**ゲートの3択に「通過後の人数」プレビュー**を出して運ゲー感を解消、コンボ・ウェーブバナー・HPバー・← →キー操作を追加。**遊びの数値は1つも変えていない**。はまり所: 遠近線を濃く引くと床ではなく光のビームに見える／床のスクロール線の半幅計算を間違えて防壁を突き抜けていた／**`va.sh` の常駐ブラウザは1つで別セッションと取り合いになる**（今回は会話側MCPで検証）。Console エラー0件（favicon も data URI 化）・自動操作で約4分の通しプレイ例外0件（最終 SCORE 181）・書体は `document.fonts` で4つとも loaded・音は AudioContext を包んで6秒でオシレータ110生成を実測・4画面を目視。**残り2件はオーナー判断: ①音の試聴（鳴ることしか確認できていない） ②公開（`deploy.yml` の `DEPLOY_FOLDERS` に追加済み＝push で自動更新される）** **同日さらに iOSアプリ化し、App Store 提出直前まで用意した**（オーナー依頼「にゃんこアイスと同様に、アーカイブ・アップ・公開直前まで」）。Capacitor 8(SPM)・`com.daikyo.cyborgdefense`・1.0/build1・iPhone/iPad・縦持ち固定。**ipa を書き出し済み**（Apple Distribution 署名・MinimumOSVersion 15.0・広告SDKなし）。アイコンは「サイボーグの顔」を自前生成（`icon-src/make_icon.py`）。スクショ iPhone6枚+iPad6枚。サポート/プライバシー、ストア文言（4.3(a)調査つき）も用意。**★残りはオーナーが ASC で App 記録を作ることだけ**（`POST /v1/apps` は `does not allow CREATE` と実測＝APIでは作れない。そのため `altool --validate-app` は `Cannot determine the Apple ID` で止まる）。手順は `cyborg-defense/RELEASE.md`。本体を `www/` へ移したので gh-pages の取り出し元は `cyborg-defense:www` | 2026-08-28 |
| piyo-defense | **メイン** | **ひよこ防衛軍（ゲーム・Canvas・GitHub Pages）。2026-08-28: にゃんこアイスと同じ方針で見た目を作り替えた**（オーナー依頼「にゃんこアイスを魅力的にしたみたいに」）。先に撮って診断したところ、**魅力を削いでいたのは素材不足ではなく「画面の2/3が真っ黒な余白」**だった（`_SBG` が地平線側を最暗に置き、ステージ20は `#000000`）。**空を7層に**（空／地平線の光／星雲／星／月／雲2層／流れ星）、**地面を3層に**（遠い丘／村＝家7軒と窓の明かり／草と花）作り替え、**20色すべて地平線側を一番明るく**して真っ黒を廃止。**黒いカラスが空に溶けて見えない問題**（見た目だけでなく遊びにくさ）は `drawCrow` に輪郭光を1枚敷いて解決。タワー設置枠は薄い破線の丸から**浮かぶ雲の足場**へ。**図鑑は未発見を影絵で見せる**形にして収集バーを追加（最終行が戻るボタンに潜っていたのも修正）。**タイトル画面だけ独自の暗い空と平らな緑の地面を持っていた**ので共通の `drawBg`/`drawGround` に統一。HUD右上のコインと「撃破/HS」の重なり、スコアパネルの行間11px、村と重なる footer も修正。**書体は CDN の Kosugi Maru → Zen Maru Gothic（SIL OFL）を使う文字だけ同梱**（70KB×2・`tools/fetch-font.py`）。理由は ①Capacitorで通信できる保証がない ②Kosugi Maru は400しか無く `bold` が合成太字だった。はまり所: **ブラウザのディスクキャッシュで直しが反映されず30分溶かした**（`location.reload(true)` は無視される）／**canvas の `ctx.font` だけでは Web フォントを読み込まない**ので `document.fonts.load()` が要る／図鑑の影絵に種類別の演出が乗って中身がバレていた。Console エラー0件（favicon も data URI 化）・`va.sh check` 0件・書体は `document.fonts.check` で500/900とも true・gstatic への通信0件・速度は `drawBg` 0.51ms / `drawGround` 0.77ms / `draw()` 全体 3.95ms（60fps予算16.7ms）・タイトル/バトル(st1,12,20)/図鑑/ゲームオーバーを目視。`README.md`・`SESSION_LOG.md`・`TODO.md` を新設。**残り2件はオーナー判断: ①音の試聴（効果音10種＋BGMは元からあり今回未変更。鳴ることしか確認できていない） ②公開（`deploy.yml` の `DEPLOY_FOLDERS` に**このアプリは入っていない**ので push しても公開版は変わらない。足すなら `piyo-defense` の1語）**　**同日夜: App Store 提出直前まで用意した（1.0 / build 2）。** アイコンが Capacitor の既定（青い×印）のままだったので、ゲーム本体と同じ `drawChick()` で描き直した（`tools/make-icon.py`。ヘルメットのひよこ・月・村。起動画面も同じ絵）。iPhone を縦向き固定に（iPad は全方向のまま＝縦だけにすると `All interface orientations must be supported` の警告が出る）。`support.html`/`privacy.html`・`store-text.md`（**4.3(a)対策で先に App Store を実測＝「ひよこ防衛軍」は完全一致0件**）・スクショ iPhone 1284×2778 と iPad 2048×2732 を5枚ずつ（`screenshots/shoot.py`）。`ios-build-guard --bump` で build 1→2、Archive→ipa 書き出し完了（6.79MB）。**配信物を実測**: Frameworks は Capacitor と Cordova の2つだけ／画像はアイコン2枚のみ／外部URLはSVGの名前空間だけ／撮影用の細工0件／MinimumOSVersion 15.0。はまり所: **`POST /v1/apps` は403＝App記録はAPIで作れない**（altool は `Cannot determine the Apple ID` で止まる・実測）／**Chrome の `--headless=new --screenshot` が戻ってこない**のでアイコンもスクショも Playwright に寄せた／**スクショの仕込みで弾を手作りすると `update()` が例外→rAFの輪が切れて描画が止まる**（撮ったつもりが直前の画面になる。`frame` が進んでいるか毎回確認するようにした）。**Web公開はオーナー判断で「する」に決定 → `deploy.yml` の `DEPLOY_FOLDERS` に `piyo-defense` を追加済み**（このアプリは gh-pages に存在しなかった＝CLAUDE.md の記載は誤りだった）。**残り: ①`.git/index.lock` が残っていてコミットできない ②ASC で App 記録を作る（人） ③価格・Appのプライバシー・年齢制限・提出（人）**。手順は `piyo-defense/RELEASE.md`。**2026-08-29: UIをネオンに統一**（オーナー指摘「全部ネオン系にできた？」で、このアプリだけ半分だと判明＝敵とボスはネオンなのにタイトル・ボタン・書体・草地・HUDが昔のままだった）。`rrectGrd()` の縁を光らせる1か所の変更で全画面に効かせ、地は `_btnGrd()` で一律に暗く。書体は **Zen Maru Gothic → Zen Kaku Gothic New＋Orbitron**。**ひよこ本体は残す**（主役）。**重なりを3件修正**（START付近＝スコアパネルと接触・実績の最終行が戻るボタンの下・報酬とコインの絵）。**公開の取り出し元を `piyo-defense` → `piyo-defense:www` に切り替え**（直下の控えが古く、公開ページだけ旧題名『ひよこ防衛軍』だった）。検証: Console エラー0・`va.sh check` 全項目0・書体4面ともloaded・6画面を目視。**残り: 音の試聴（人）／App Store 再開時はスクショ撮り直し（凍結中）** | 2026-08-29 |
| brain-dump | サブ | **ブレインダンプ（3002 / Vercel）。2026-08-25: iPhone Safari で音声入力が1分ほどで「録音が空でした」になる不具合を修正**（原因は ① iOS Safari は `ondataavailable` が `onstop` より後に届くことがあり timeslice なしだと Blob が0バイト ② 自動ロックで画面が消えるとマイクが止まる。対処は `rec.start(1000)` の逐次回収＋chunk を最大3秒待ってから Blob 化＋Screen Wake Lock＋トラック `onended` で自動停止）。サイズ超過・タイムアウトではないことは実測で否定済み（59秒=250KB・API 9秒）。lint/build 通過・Chrome の仮想マイクで通し確認済み。**Vercel 本番へ反映済み・オーナーが iPhone 実機で確認して直っていることを確認（2026-08-25）。このアプリの残タスクは無し**（メインPCは `git pull` のみ・常駐していないので再起動不要） | 2026-08-25 |

## ★公開（gh-pages）が止まっていた → **解消済み**（2026-08-28 夜・メインPC）

**復旧済み。原因は2つあった**（担当セッションが対応・`82202295` と `8c761d22`）。

1. `DEPLOY_FOLDERS` に `cyborg-defense:www` を書いたのに `www/` が未コミットだった
   → **`DEPLOY_FOLDERS` に足すフォルダは、同じ push でリポジトリにも入れる**
2. ワークフローが `git commit -m "Deploy: ${{ github.event.head_commit.message }}"` と書いていた
   → Actions が**先に展開してから**シェルが解釈するので、**引用符や改行を含むコミットメッセージで
   デプロイが壊れる**（`error: pathspec 'not' did not match any file(s)`）。任意コードが走る穴でもある。
   **env 経由で渡し、件名1行だけを使う形に修正済み**。長いコミットメッセージを書いても落ちない

復旧後に実測: `cyborg-defense/`（support/privacy とも）・`neko-escape/`・`color-gravity/`・
`nyanko-ice/`・`realestate-calc/`・`mom-counter/`・`scrapmemo-petapeta/` がすべて 200。

以下は経緯の記録。

## （経緯）いま公開（gh-pages）が止まっている（2026-08-28 夜・メインPC）

**原因は1つだけ: `cyborg-defense/www/` が未コミット**。`deploy.yml` の `DEPLOY_FOLDERS` には
`cyborg-defense:www` が入っているのに、リポジトリ側にそのフォルダが無いのでデプロイが落ちる。

```
ERROR: cyborg-defense/www/index.html がありません   （run 33171106148）
```

- **チェックは gh-pages を触る前に落ちる**ので、公開中のページは無傷（古いまま）
- ただし **この1件で、ほかの5本も公開されない**
  （スクラップメモ／ママカウンター／電卓／にゃんこアイス／**にゃんこ大脱出**）
- **直し方は `cyborg-defense/www` をコミットして push するだけ**（ファイルはディスクにある）
- 担当セッションには連絡済み。急ぐなら `DEPLOY_FOLDERS` から `cyborg-defense:www` を
  一時的に外して push すれば、残り5本は公開できる（**外したら戻すのを忘れない**）

## 横断作業（複数アプリにまたがるもの）

- **★ここから: 「APIの取得と整理」の再開点は `API_STATUS.md`（直下）。2026-08-20 に更新済み。**
  **この日に進んだこと**: 日本郵便が**本番**になった／用途地域のバグを修正（`XKT002`）／
  ジオコーディングを Google 併用に（ROOFTOPのときだけ）／`jyuusetsu-research` に
  ストリートビューを実装（**Webキーのリファラ制限で社内画面が403。Console設定が要る＝人の作業**）／
  直下に共通クライアント **`google_maps_api.py`** を新設。
  **e-Stat も appId を登録して実装まで完了**（`.env.estat`）。
  **e-Gov 法令API（キー不要・無料）を使える状態にした** … 共通クライアント `egov_law_api.py`。
  法令名で検索 → 条番号で本文 → キーワード検索まで実測確認（宅建業法35条・借地借家法28条）。
  **組み込み先は未着手**（`legal-crosscheck` / `tokuyaku-generator` / `gyomu-manual`）。
  **App Store Connect API を取得**（`.env.appstore` ＋ `.appstore/AuthKey_35U53KWY5J.p8`）。
  `ios-build-guard.sh` が **App Store の登録済みビルド番号**で判定するようになった。
  ★**サブPCはアーカイブ0件のため従来は誤判定していた**（scrapmemo build8 を「衝突なし」と表示）。
  ★**にゃんこのアイス屋さんは登録ビルドが0件**（「申請中」の記載と食い違っていた
  → **2026-08-24 に決着。未提出が正**でCLAUDE.mdを訂正済み）。
  **保有APIの一覧説明書を作成** … 直下 `API一覧説明書.xlsx`（保有30件／取りに行くリスト17件／決まり9件）。
  **残りの人の作業**: 国税庁 法人番号（発行2週〜1か月・最優先）／
  App Store Connect の .p8／Maps の予算アラートとサーバーキーのIP制限。
  持っているAPIの棚卸し／未取得の一覧／朝いちで出す申請の文面／規約で不可と決めた案／
  見つかった不具合2件（用途地域が取れない・e-Statは呼ぶコードが無い）を1枚にまとめてある。
  **待ち時間のある申請（国税庁は発行2週〜1か月）を先に出すのが得。**
  受け渡しは `./secrets-sync.sh export` 済み（1.0M・28件。`.env.google-maps` と
  `.env.japanpost` の同梱を確認）。**メインPCで import → 確認 → 置き場を削除**


- **日本郵便「郵便番号・デジタルアドレスAPI」— 2026-08-20 に本番へ切り替え済み（サブPC）。**
  本番の資格情報を受領して `.env.japanpost` を差し替え、`JAPANPOST_HOST` の行を削除
  （＝既定の `api.da.pf.japanpost.jp` に向く）。疎通確認: `searchcode 5410053` → 大阪市中央区本町、
  `addresszip`（大阪市中央区・本町）→ level=3 で5件。**テスト用は `searchcode "100"` が2件、
  本番は466件**返るので、どちらに繋がっているかはこれで判る。
  テスト用stubの資格情報は `.env.japanpost.bak-stub` に退避（不要なら消してよい）。
  **★メインPCへは `./secrets-sync.sh export` で運ぶ必要がある**（キー更新のため再exportが要る）。
  以下は取得当時（2026-08-19）の記録:
  実装は直下 **`japanpost_api.py`**（`search_code` / `address_zip` / トークンのキャッシュ付き）。
  認証は OAuth2 `client_credentials`、**ヘッダ `x-forwarded-for` が必須**
  （自分のグローバルIPで通ることを実測。自動判定する作りにしてある）。
  - **本番ホストは `api.da.pf.japanpost.jp`**（モジュールの既定）。テスト用は
    `stub-qz73x.da.pf.japanpost.jp` で、**資格情報ごと別**（テスト用を本番に入れると401）。
    切り替えは `.env.japanpost` の `JAPANPOST_HOST` 1行（**本番が出たらこの行を消す**）
  - 疎通確認済み: `searchcode "100"` → 千代田区 内幸町/大手町。
    `addresszip 13/13101` → level=2 で6件。カナ・ローマ字も返る
  - ~~★人がやること: 本番用の組織・システム登録 → 本番のクライアントID／シークレットの発行~~
    → **2026-08-20 に完了**（差し替え済み）
  - **未確認**: レート制限の具体的な数値。テスト用は「予告なくデータのクリーンアップ・処理中断」
    があり、負荷試験や大量利用は不可
  - 使い道: `soufu-maker` / `kaitori-dm-maker` / `tsuikyaku-crm` の住所正規化。
    **v2.0 では法人名・電話番号・法人番号まで取れる**ので、法人番号APIの前段にもなる

- **Google Maps / ストリートビュー API を取得した（2026-08-19・サブPC）。詳細は `GOOGLE_MAPS_API.md`。**
  プロジェクト **`daikyo-maps-2026`**（**Gemini とは別プロジェクト**。同じだと公開ページに載せた
  キーで Gemini を叩かれるため）。請求先は Gemini と同じ口を流用＝**カード再入力なし**。
  キー2本は **`.env.google-maps`（直下・gitignore・600）**。`secrets-manifest.txt` に登録済み。
  **Geocoding と Street View は実際に叩いて動作確認済み**（本町4-2-12 / 2021-08撮影のパノラマ）。
  - **★メインPCでやること: `./secrets-sync.sh import` で受け取る。**
    置き場 `Dropbox-個人/apps-secrets-handoff/apps-secrets-appurunoMacBook-Air.tar`（980K・27件）。
    **受け取りを確認したら、置き場ごと削除する**（消すのは受け取った人）
  - 未了: サーバー用キーの**IP制限**（事務所の固定IPが不明）／**予算アラートと日次クォータ**／
    Drive API は有効化しただけで **OAuth同意画面の設定が未了**
  - 規約で**不可**になった案: `parking-map` の航空写真トレース（3.2.3(c)(i)）と
    `kaitori-dm-maker` のSV外観AI判定（3.2.3(c)(vii)）。**SVは印刷物にも使えない**
  - ~~次の一手: `jyuusetsu-research` にストリートビュー~~ → **2026-08-20 実装済み**。
    ただし **Webキーのリファラ制限（`https://daikyocorp.co.jp/*` のみ）で社内画面は 403**。
    **人がやること**: Console で「Maps Embed だけに絞ったキー」を新規作成し社内画面用に使う
    （埋め込みは無制限無料なので、漏れても課金されない）。詳細は `jyuusetsu-research/README.md`
  - 直下に共通クライアント **`google_maps_api.py`** を新設（`japanpost_api.py` と同じ置き方）。
    `geocode` / `streetview_metadata` / `streetview_embed_url` / `map_embed_url`。
    **`.gitignore` に `!google_maps_api.py` の許可行が要る**（追加済み）

- **iOSシミュレータを操作する道具を直下に置いた（`simtap.py`・2026-08-19）。**
  `xcrun simctl` にはタップが無いので、画面を見て直ったか確かめられなかった。
  `./simtap.py calib / tap x y / drag x y1 y2 / type "文字" / key return` で操作できる
  （初回に `~/.sim-venv` を自動で作る）。**日本語は `keystroke` だと化ける**ので
  クリップボード経由で入れている。`.gitignore` に `!simtap.py` の許可行も入れた

- ~~**★ 8/19 にやること: KeyLine の Zenn と note を「まとめて」出す**~~ ✅ **2026-08-19 完了（サブPC）**。
  本体はもう公開済み → https://ai-tools-base.vercel.app/works/keyline
  1. **20:47 以降**に `cd ai-tools-base && ./publish.sh zenn` → `./publish.sh status` が ✅ になるまで確認
  2. ✅ を見てから `python3 drafts/note/md2html.py who-has-the-key` → note の本文欄で ⌘V → 投稿
  3. `content/works/keyline.json` の `links` に2本のURLを足して `./publish.sh site`
  **手順の詳細と「なぜ20:47以降なのか」は `ai-tools-base/drafts/PUBLISH.md` の8本目の節にある。**
- **`./publish.sh zenn` は空コミットを作らない。** `articles/` に変更が無いと `git commit` が失敗し、
  `git push` が "Everything up-to-date" で終わって **Zenn のデプロイが走らない**。
  弾かれた1本を再pushするときは、先に `git commit --allow-empty` を叩くこと（8/19に実測）
- **Zenn の上限本数は公開されていない**（2026-08-19に公式FAQで確認）。「1日2本」はこちらの推測だった。
  判定は「直近24時間以内の投稿数」だが、**本数のロジックは不正防止のため非開示**
  （https://zenn.dev/faq/rate-limit）。実際 8/19 は**直近24時間の公開が1本だけ**の状態で
  2本目が弾かれた。**本数で予定を組まず、毎回 `./publish.sh status` とデプロイ履歴で確かめる**
- （参考・8/18の事例）**「pushした本数」ではなく「直近24時間に公開された本数」で数えられる**。
  メインPCが前日pushした `llm-pdf-split-gaps` の反映が 8/18 20:47 だったため、その日の枠を消費し、
  3本目の `ios-nfc-safari-entitlement` が弾かれた。**デプロイ履歴のお知らせ欄に理由が明記される**
  （https://zenn.dev/dashboard/deploys ・要ログイン）。`./publish.sh status` の ⬜ でも検知できる
- **note はこのサブPCからも投稿できる**（8/18に実測）。Chrome拡張が繋がり、noteはログイン済み。
  **拡張が「未接続」と出たら、Chrome を前面に出せば繋がる**（起動していても最前面でないと繋がらない）
- **Zenn 7本 / note 7本で揃っている**（2026-08-19 22:36時点）。残りは `ai-intake-hearing`（Zenn）と
  `nanka-ugokanai`（note）の1組だけ。**Zenn→note の順で、対にして出す**
- **公開状況は推測で書かない。** note は API
  （`https://note.com/api/v2/creators/shinsei99/contents?kind=note&page=N`・1ページ6件）、
  Zenn は `./publish.sh status` で確かめてから書く。2026-08-19 に
  「note は6本とも未公開」という記述が誤りだったことが実測で判明している
- **note の見出し画像は「この画像を挿入」のあとに出る「保存」まで押す。** 押さないと入らない
  （2026-08-19 に2回とりこぼした）。貼ったあとは画像を目で見る（「書類の山」で検索して
  出てきた画像が実際は札束だった）
- **共通 Visual Agent を1つに統合した（2026-08-18・メインPC）。** 2台が別々に作った
  MCP版（`.mcp.json`）と `./va.sh` を、**1つの仕組み・2つの入口**に整理。どちらも消していない。
  - **同じ Google Chrome を headless で開く**ようにしたので、入口が違っても見えるものが食い違わない
  - `./va.sh` の Playwright は `VA_PYTHON` → `agent-platform/.venv` → `.va-venv` → `python3`
    の順に探す。**agent-platform 決め打ちをやめた**ので、あのアプリが無いPCでも動く
  - `./visual-agent-check.sh` が**両方の入口**を点検する（`--mcp` / `--va` で片方だけも可）
  - 説明は `VISUAL_AGENT.md` 1本に集約（CLAUDE.md は要点だけ）
  - **`.mcp.json` はもともと git に入っていた。** サブPCに届いていなかったのは、メインPCが
    27コミットを push していなかったから。→ サブPCは `git pull` だけで入口Aも使えるようになる
  - メインPCで実測: 入口A・入口Bとも「開く→押す→読む→撮る→check」まで成功（2026-08-18）
- **`dev-doctor.py --sync` が出す「gitに入っていないソース候補 1件」＝ `make_app_list.py`。**
  2026-06 の使い捨てスクリプト（社内アプリ一覧のExcelを吐く）で、**社内ホスト名とportを直書き
  している**。リポジトリは公開なので**意図的に git に入れない**。中身は CLAUDE.md のアプリ表が
  正なので、必要になったら作り直す。＝この警告は既知・対応不要（毎回調べ直さない）
- **見つかった未修正のUI崩れ（ai-tools-base・390px幅）**: 比較表が横に484pxはみ出していて
  料金列が読めない（`div.table-scroll` は `overflow-x:auto` だが手がかりが無い）。ロゴも2行に折れる
- **3媒体への公開は、3つともサブPCからできる**（8/18・8/19に実測）。以前「メインPC担当」と
  書いていたが、サブPCにも Chrome拡張・note/Zenn のログイン・Vercel の認証がある。
  **Vercel 本番デプロイも 2026-08-19 にサブPCから成功した。**
  - ただし1回目は `Not authorized` で失敗した。原因は `.vercel/project.json` の `projectName` が
    **旧名 `ai-tools-lab` のまま**だったこと（`.vercel/` は gitignore なので git では直らない）。
    `npx vercel link --yes --project ai-tools-base --scope brain-dump` で直る
  - **`./publish.sh site` の「反映確認」を信用しない。** デプロイが失敗しても既存デプロイに
    当たって HTTP 200 と出る。**`Aliased …` の行と、実際のページの中身で確かめる**
  手順は `ai-tools-base/drafts/PUBLISH.md`、入口は `ai-tools-base/publish.sh`
- ~~ai-tools-base のフォルダ改名の後始末（node_modules 等の移動）~~ → **2026-08-18 完了**
  （`ai-tools-lab` は削除済み。実体は `ai-tools-base/` にある）
- ~~`digital-shosai/.env.local` が不足~~ ✅ **そもそも不要だった**（2026-08-17実測）。完全オンデバイス版
  （pdf.js＋IndexedDB）に作り替えられており `process.env` の参照が0件。旧設計の `.env.local.example` を
  削除し manifest からも外した。→ **機密の不足は0件になった**
- ~~サブPCの launchd 常駐2本（file-finder 8520 / owner-payout-tracker 8519）~~
  ✅ 2026-08-17 に unload 済み。サブPCの launchd 常駐は**0本**（`launchctl list | grep shinsei` が空）。
  画面が要るときは `cd <アプリ> && ./run.sh` で都度起動する（常駐に戻さない）

- ~~CLAUDE.md のスリム化~~ ✅ **2026-08-17にサブPCで実施**。**27,288字 → 14,700字（46%削減）**。
  アプリ個別の補足 12,999字を10本の `<アプリ>/README.md` へ移し、CLAUDE.md にはポインタ一覧だけ残した
  （移動先: gyomu-manual / parking-map / kaitori-dm-maker / psa-collection / agent-platform /
  chatwork-ai-manager / flyer-creator / shorui-cabinet / photo-inpainter / theta-viewer）。
  **`quote-generator` だけは別リポジトリでREADMEが渡らないため CLAUDE.md に残した。**
  なお「`photo-inpainter/` `pdf-organizer/` はフォルダごと無視されてREADMEが渡らない」という
  以前の注意書きは**古い情報だった**（`!photo-inpainter/**` の許可行が既にあり、渡ることを実測）。
- **agent-platform をメインPCで動かすには別途ファイルが要る**（gitに入れていない）:
  `config/`（会社名・免許番号などの発行者情報）、`knowledge/`（学習データ。物件名が混ざる）、
  `.env`（`.env.example` をコピーしてGeminiキーを入れる）。Dropbox等で渡す。
