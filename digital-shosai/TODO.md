# TODO — デジタル書斎（digital-shosai）

port 3001 / ツール分類（社内LAN共有なし・`127.0.0.1`）。**完全オンデバイス**（キー・`.env` は不要）。

## いま着手していること（2026-08-17）

- [x] **広告と本棚スロット制限を撤去**（バナー／インタースティシャル／動画リワード、`profile` ストア、
      `FREE_BOOK_SLOTS`、`LIMIT_REACHED`）。自分専用の道具なので冊数を人為的に絞る意味がない
- [x] **ページ画像を WebP で保存**（`image/png` 固定 → ロスレスWebP優先＋PNGへ自動フォールバック）。
      pdf.jsの描画は輪郭のはっきりした合成画像なので効きが大きい。空いた分 `RENDER_SCALE` を 1.5→2.0 に
- [x] **検索を軽くする**（IndexedDB v2）。`pages` は画像専用にし、テキストは新ストア `pageText` に置く。
      これまでは検索のたびに**画像を抱えたレコードを全件走査**していた
- [x] **蔵書一覧・削除画面**（`/library`）。端末容量は有限なのに消す手段が無かった
- [x] 検索の質: **複数語のAND**、**本で絞り込み**、ヒット件数
- [x] `window.confirm` / `alert` をやめて**画面内の確認パネル**に（ブラウザのダイアログは
      自動操作を止めるうえ、UIとしても唐突）

## iPhone 対応で分かったこと（2026-08-17 実機実測・iOS 18.7 / Safari 26.5.2）

**iPhoneのブラウザは全部Safariの中身。** Chrome(CriOS/152) も `vendor: Apple Computer, Inc.` /
`AppleWebKit/605.1.15` で、機能はSafariと同一だった。→ **フォルダ指定（`showDirectoryPicker`）は
iPhoneでは使えない**（`false`。`showOpenFilePicker` も `false`）。使えるのは `<input type="file">` のみ。

| 項目 | iPhone の実測 | 設計への影響 |
|---|---|---|
| WebPの**書き出し** | **できない**（`toDataURL("image/webp")` がPNGで返る。1ページ583〜670KB） | **画像はMacで作って持ち込む。iPhoneでは生成しない** |
| WebPの表示 | 要確認（Safari 14+ は対応しているはず・未実測） | Macで作ったWebPを表示できる前提 |
| IndexedDB / serviceWorker / storage.persist | すべて使える | 索引の持ち込み・オフライン起動・データ保護は成立 |
| 1ページの描画＋圧縮 | 60〜110ms | 速度は問題にならない |
| 保存容量 | **プライベートブラウズだと保存が壊れる**（下記） | **検知して案内する必要がある** |

### ⚠️ プライベートブラウズでは保存できない（実機で踏んだ）

Safariのプライベートタブで IndexedDB に書こうとすると**エラー内容が `null` のまま失敗**し、
`navigator.storage.estimate()` の上限も 1000MB と小さく申告される（通常モードのChrome(CriOS)では
30MBが0.1秒で書けて上限38.4GBと申告）。**同じエンジンなのに挙動が違うのはブラウズモードのせい。**

→ アプリ側でやること: **保存を試す前に小さな書き込みで判定し、失敗したら
「プライベートブラウズでは保存できません。通常のタブで開いてください」と画面に出す**。
いまは無言で失敗するので、原因が分からない。

## 次にやること

- [ ] **バックアップ（書き出し／読み込み）**。端末内だけなので**端末が壊れると全部消える**。
      外部送信しない方針は守ったまま、ZIP等で1ファイルに書き出せるようにする
- [ ] **保存できない状態の検知と案内**（プライベートブラウズ／ストレージ無効）。上記の実測どおり無言で失敗する
- [ ] **iOSでは画像を生成しない**（WebPが作れずPNGで4倍になる）。iPhoneで直接PDFを取り込むときは
      テキストのみにするか、警告を出す
- [ ] PWA化（manifest＋service worker）。静的書き出しなのでオフライン起動・ホーム画面追加が安い。
      **iPhone運用の前提**（ホーム画面から起動すると保存の扱いが良くなる）
- [ ] 未OCRのPDFの救済（`tesseract.js` でブラウザ内OCR。1ページ数秒かかるので明示的なオプトイン）
- [ ] Capacitor で iOS アプリ化（`out/` をそのまま使う）。**配信用証明書はメインPCにしかない**

## 決めたこと

- **広告は入れない。** 収益化の枠（バナー・インタースティシャル・リワード）は2026-08-17に全削除した。
  再び入れるなら AdMob＋Capacitor化とセットで、別途判断する
- **外部送信はしない。** バックアップも「端末内でファイルに書き出す」方式にする（クラウドに上げない）
