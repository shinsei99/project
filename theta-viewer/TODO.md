# TODO.md — theta-viewer 計画（PDCAのPlan）

記号: `[ ]`未着手 / `[~]`着手中 / `[x]`完了

> 1行で足りるものは1行で書く。様式（目的/実装内容/完了条件/検証方法/状態/関連ファイル）を使うのは
> **複数セッションにまたがる・複数ファイルを触る・外部に影響が出る**改修だけ（直下 `CLAUDE.md` の決まり）。

---

## 現在の状態

**完成・運用中。** メインPCで常時起動（launchd 2本: 画面8512 / FTP API 8523）、
社内LAN共有あり。お客様向けは `https://daikyocorp.co.jp/vr/#/property/<id>` で公開済み。

## いま進行中

- （なし）

## これからやること

- [ ] `package.json` の `firebase` / `@supabase/supabase-js` が**実際に使われているか確認**し、
      使っていなければ依存から外す。`src/firebase.ts` は名前だけの名残で中身はHTTP/FTP呼び出し層
- [ ] メモリ `project_theta_viewer.md` の **FTP APIポートの記述（8522）を 8523 に直す**
      （コードの `PORT = 8523` が正。`project_theta_viewer_port_fix.md` で訂正済みだが本文が古いまま）
- [ ] UIの目視確認（`./va.sh`）を一度通す。このアプリには自動テストが無い

## やらないと決めたこと（理由つき・再検討しないため）

- **BrowserRouter への変更はしない。** HashRouter なのは静的ホスティング（FTP先）で
  リロードしても404にならないようにするため。変えると**お客様に配ったURLが壊れる**
- **AI解析をサーバー側へ移さない。** ブラウザ内推論なので物件写真が外へ出ない。
  この性質は物件写真を扱ううえで利点なので維持する
