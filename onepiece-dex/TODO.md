# ワンピースカード図鑑（onepiece-dex）TODO

port 8537 / 127.0.0.1（自分専用。カード画像は著作権物なので社内LANにも出さない）

## 2026-08-23（サブPC）

| # | やること | 状態 |
|---|---|---|
| 1 | `crawl_official.py` … 公式カードリスト全62シリーズ → `data/cards.db` | ✅ 完了（4,962枚・読めず0件） |
| 2 | `fetch_images.py` … 公式のカード画像 4,962枚（約1.2GB） | ✅ 完了 |
| 3 | `make_thumbs.py` … 一覧用サムネイル180px | ✅ 完了 |
| 4 | ~~マイカ画像への差し替え~~ … **やめた**（下記） | ❌ 不採用 |
| 5 | `build_dex.py` … `dex` / `dex_series` / `dex_features` / `dex_colors` | ✅ 完了 |
| 6 | `check_dex.py` … 枚数・画像・レアリティの整合性検査 | ✅ 完了 |
| 7 | `app.py` … Streamlit（さがす / シリーズ / リーダー） | ✅ 完了 |
| 8 | `run.sh` / `requirements.txt` / `README.md` / `SESSION_LOG.md` | ✅ 完了 |

## 次にやるなら

- **相場価格**（保留中。2026-08-23 オーナー判断「まず公式で完成させ、不足があれば他サイト検討」）。
  マイカの**画像**は直リンクで取れるが、**相場はカードページを開かないと取れない**。
  そのIDはゲーム横断で、`/one-piece-card-game/items/single-card/10` を叩くと
  ポケモンの旧裏カードが返る＝ワンピだけ狙い撃ちできず、ポケカと同じ全ID巡回（数時間）が要る。
  `dex.price` 列は空で用意済みなので、後から足しても画面は壊れない
- **画像の「SAMPLE」透かしは消せない。同じ回り道をしないこと。**
  公式画像（600×838）の透かしを嫌ってマイカ画像に替えようとしたが、
  **マイカの画像にも同じ透かしが入っていて、しかも400×559と低解像度**だった。
  経緯は `README.md` に残してある
- 新弾が出たら `crawl_official.py` → `fetch_myca_images.py` → `fetch_images.py`
  → `make_thumbs.py` → `build_dex.py` の順に流し直す（すべて冪等・追加ぶんだけ落ちる）
- launchd 常時起動は**登録していない**（サブPCは都度起動の役割）。
  メインPCで常駐させるなら `com.shinsei.onepiece-dex` を作る
- PSAカード管理（8527）からの起動は今回やらない（単独1本の方針・2026-08-23オーナー判断）
