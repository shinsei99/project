# 機密・データの運搬対象（**パスだけを書く。値は絶対に書かない**）
#
# gitに出せないが、アプリの動作に要るファイルの一覧。
# `secrets-sync.sh` がこの一覧を見て、tar にまとめる／展開する／不足を出す。
#
# 書き方: 1行1パス（リポジトリ直下からの相対）。`#` はコメント。ディレクトリも書ける。
# ここに載っていないものは運ばれない。**新しく .env を作ったら、ここに追記すること。**

# ── APIキー・トークン
madori-tracer/.secret_key
madori-tracer/.env.local
agent-platform/.env
brain-dump/.env.local
pasha-calo/.env.local
theta-viewer/.env.local
building-manager/.env
ai-ticket-counter/.env
# digital-shosai/.env.local … **不要**（2026-08-17に実測）。完全オンデバイス版に作り替えられており、
#   コード内に process.env の参照が1つも無い。Supabaseキーの .env.local.example は旧設計の名残だったので削除した

# ── 横断（1本のキーを複数アプリから参照する）
# Google Maps Platform のキー2本（公開ページ用＝リファラ制限 / サーバー用＝API種別制限）。
# プロジェクトは daikyo-maps-2026。**Gemini とは別プロジェクト**にしてある（理由は GOOGLE_MAPS_API.md）
.env.google-maps
# 日本郵便「郵便番号・デジタルアドレスAPI」のクライアントID／シークレット。
# ★2026-08-19時点では**未発行**（ポータルで組織・システム登録が要る）。発行されたらここが埋まる。
# 使うコードは直下 japanpost_api.py（gitに入っている）
.env.japanpost

# ── 接続情報（他アプリからも参照される）
# flyer-creator の物件サイト公開もここのFTP情報を読む
theta-viewer/server/ftp-config.json

# ── 差出人・閲覧キーなど、個人情報を含む運用ファイル
kaitori-dm-maker/senders.json
flyer-creator/.stats_key

# ── AIの発行者情報・学習データ（物件名を含むため git に入れていない）
agent-platform/config
agent-platform/knowledge

# ── KeyLine（鍵の台帳・利用者・借主・免許証や名刺の画像）
# ★個人情報を含むので git には絶対に入れない。運ぶのは Dropbox-個人 経由（この一覧の役目）。
#   画像は返却から30日で purge.py が自動削除するため、渡した先でも古いものは残らない。
keyline/data

# ── 保有明細・資産額（PSA）
psa-collection/data/collection.csv
psa-collection/data/orders.json
psa-collection/data/albums.json
psa-collection/data/storage_notes.json

# ※ chatwork-ai-manager は **一切運ばない**（2026-08-18 本人確認）。
#    worker / LINE webhook / ngrok はメインPCのみで動かす決まりで、**サブPCでは触らない**。
#    したがって `data/app.db`（170MB）も secrets も渡さない。
#    ——将来どうしても常駐をもう一方へ移すときだけ、専用の handoff_export.sh / handoff_import.sh を使う
#    （DBの向きが重要で、双方向マージはできないため）。

# ── リポジトリ外にあるPC側の設定（**この一覧では運べない。手で揃える**）
# secrets-sync.sh はリポジトリ直下からの相対パスしか扱えないため、以下は対象外。
# PCをまたぐときは手で確認する。2026-08-17に MCP の取りこぼしが実際に起きた。
#   ~/.claude.json の mcpServers   … MCPサーバーの設定（例: VISUAL_AGENT）。`claude mcp list` で確認
#   ~/.claude/settings.json        … hooks・権限（今は hooks なし）
#   launchctl の disable 状態       … サブPCの常駐OFF（`launchctl print-disabled "gui/$(id -u)"`）
#   ブラウザ側のログイン状態        … note / Zenn / Vercel / Safari(PSA)。運べないので各PCで入る

